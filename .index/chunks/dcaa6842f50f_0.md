# Chunk: dcaa6842f50f_0

- source: `src/agentic_assistants/data/__init__.py`
- lines: 1-66
- chunk: 1/1

```
"""
Data layer module for Agentic Assistants.

This module provides:
- DataLayer: Unified interface for reading/writing data files with caching
- Cache: LRU memory cache with TTL support
- DataSourceManager: Handler-based data source connection management
- DataCatalog: Schema discovery and metadata cataloging

Example:
    >>> from agentic_assistants.data import DataLayer, DataSourceManager
    >>> 
    >>> data_layer = DataLayer()
    >>> df = data_layer.read("data/users.parquet")
    >>> data_layer.write(df, "output/results.parquet")
    >>> 
    >>> # Data source management
    >>> manager = DataSourceManager()
    >>> source = manager.create_source("my-db", "database", {...})
    >>> result = manager.test_connection(source)
"""

from agentic_assistants.data.layer import DataLayer
from agentic_assistants.data.cache import LRUCache, CacheEntry
from agentic_assistants.data.sources import (
    DataSourceManager,
    DataSourceHandler,
    DatabaseHandler,
    FileStoreHandler,
    APIHandler,
    ConnectionResult,
    SchemaInfo,
    TableSchema,
    ColumnSchema,
)
from agentic_assistants.data.catalog import (
    DataCatalog,
    CatalogEntry,
    TableEntry,
    ColumnEntry,
    DataLineage,
    CatalogSearchResult,
)

__all__ = [
    "DataLayer",
    "LRUCache",
    "CacheEntry",
    "DataSourceManager",
    "DataSourceHandler",
    "DatabaseHandler",
    "FileStoreHandler",
    "APIHandler",
    "ConnectionResult",
    "SchemaInfo",
    "TableSchema",
    "ColumnSchema",
    "DataCatalog",
    "CatalogEntry",
    "TableEntry",
    "ColumnEntry",
    "DataLineage",
    "CatalogSearchResult",
]
```
