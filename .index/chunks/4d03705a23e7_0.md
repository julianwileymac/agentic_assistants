# Chunk: 4d03705a23e7_0

- source: `.prompts/scratch/fast_api.md`
- lines: 1-15
- chunk: 1/1

```
<Project_Background>
This project provides a framework for local, hybrid, agentic solutions development using MLOps tools to develop a resource-optimized strategy for cost-effective code development while learning agenticc design patterns. Users will be able to code agentic solutions in an interactive development environment, test them locally on a local, hybrid framework, and save them as experiments in MLOps tooling. This is also to integrate MLOps and AIOps tools into multi-agent frameworks like agno and crew ai. 
</Project_Background>
<Task_Overview>
You are to add a fastAPI interface for the framework and add a customized, web-based IDE (Using Theia) to allow AI driven development with custom extension(s) to allow users to add script and notebook runs to experiments, specify memory for the agentic application run, and document/persist the interaction for enhancing the solution's run. 
</Task_Overview>
<Task_Items>
- Theia implementation is broken, fix the issue causing websockets to close, the webpage to hang, and duplicate processes in node to be spawned on port 3000. Theia may be disabled in favor of jupyterhub in some places, override as needed 
- Add detailed and verbose traces, metrics, and logging events throughout the framework using opentelemetry to enable better debugging
- Use Theia (https://github.com/eclipse-theia/theia/blob/master/doc/Developing.md#prerequisites) as the base for the UI. Node will be the core, add any required components to integrate the python engine with the node UI
- Add global, user, and session configuration objects to control the behavior of the framework and the requisite handler and context management resources. 
- Create an extension to integrate with MLFlow to start/stop experiments, traces, etc from the Theia IDE
- Add session support - save files to user location and add artifact management with tagging and grouping option. Also add a shared location to the configuration to store common artifacts. 
- Enhance data layer and expose in a tab through the UI. 
</Task_Items>
```
