# Chunk: 114e647386ac_5

- source: `src/agentic_assistants/data/rag/chunkers.py`
- lines: 375-468
- chunk: 6/9

```
b_chunk)
            else:
                chunks.append(Chunk(
                    content=section_text,
                    index=len(chunks),
                    start_char=start,
                    end_char=end,
                    metadata=chunk_metadata,
                ))
        
        return self._merge_small_chunks(chunks)


class CodeChunker(Chunker):
    """
    Chunker for source code that respects function/class boundaries.
    
    Attempts to keep functions and classes intact when possible.
    """
    
    # Patterns for common code constructs
    PYTHON_PATTERN = re.compile(
        r'^(class\s+\w+|def\s+\w+|async\s+def\s+\w+)',
        re.MULTILINE
    )
    JS_PATTERN = re.compile(
        r'^(class\s+\w+|function\s+\w+|const\s+\w+\s*=|export\s+(default\s+)?)',
        re.MULTILINE
    )
    
    def __init__(
        self,
        chunk_size: int = 1024,
        chunk_overlap: int = 128,
        language: Optional[str] = None,
        **kwargs,
    ):
        super().__init__(chunk_size, chunk_overlap, **kwargs)
        self.language = language
    
    def _detect_language(self, text: str) -> str:
        """Attempt to detect the programming language."""
        if self.language:
            return self.language
        
        # Simple heuristics
        if "def " in text or "import " in text:
            return "python"
        if "function " in text or "const " in text or "=>" in text:
            return "javascript"
        if "func " in text or "package " in text:
            return "go"
        
        return "unknown"
    
    def chunk(self, text: str, metadata: Optional[Dict[str, Any]] = None) -> List[Chunk]:
        metadata = metadata or {}
        
        language = self._detect_language(text)
        metadata["language"] = language
        
        # Select pattern based on language
        if language == "python":
            pattern = self.PYTHON_PATTERN
        elif language in ("javascript", "typescript"):
            pattern = self.JS_PATTERN
        else:
            # Fall back to text chunker
            return TextChunker(
                chunk_size=self.chunk_size,
                chunk_overlap=self.chunk_overlap,
            ).chunk(text, metadata)
        
        # Find boundaries
        matches = list(pattern.finditer(text))
        
        if not matches:
            return TextChunker(
                chunk_size=self.chunk_size,
                chunk_overlap=self.chunk_overlap,
            ).chunk(text, metadata)
        
        chunks = []
        
        for i, match in enumerate(matches):
            start = match.start()
            end = matches[i + 1].start() if i + 1 < len(matches) else len(text)
            
            block = text[start:end].rstrip()
            
            # Get block name
            block_name = match.group(0).split("(")[0].split(":")[0].strip()
            
```
