# Chunk: d844465bc51b_0

- source: `frontend/packages/agentic-mlflow/lib/browser/mlflow-service.d.ts`
- lines: 1-100
- chunk: 1/2

```
/**
 * MLFlow Service
 *
 * Provides MLFlow experiment and run management functionality.
 */
import { Emitter, Event } from '@theia/core/lib/common';
import { AgenticBackendService, AgenticWebSocketClient } from 'agentic-core/lib/browser';
export declare const MLFlowServiceSymbol: unique symbol;
export interface Experiment {
    experiment_id: string;
    name: string;
    description?: string;
    artifact_location?: string;
    lifecycle_stage: string;
    tags: Record<string, string>;
    creation_time?: string;
    last_update_time?: string;
}
export interface Run {
    run_id: string;
    experiment_id: string;
    run_name?: string;
    status: string;
    start_time?: string;
    end_time?: string;
    artifact_uri?: string;
    metrics: Record<string, number>;
    params: Record<string, string>;
    tags: Record<string, string>;
}
export interface ExperimentsListResponse {
    experiments: Experiment[];
    total: number;
}
export interface RunsListResponse {
    runs: Run[];
    total: number;
}
export declare class MLFlowService {
    protected readonly backendService: AgenticBackendService;
    protected readonly wsClient: AgenticWebSocketClient;
    protected experiments: Experiment[];
    protected activeExperiment: Experiment | undefined;
    protected experimentRuns: Map<string, Run[]>;
    protected readonly onExperimentsChangedEmitter: Emitter<Experiment[]>;
    readonly onExperimentsChanged: Event<Experiment[]>;
    protected readonly onActiveExperimentChangedEmitter: Emitter<Experiment | undefined>;
    readonly onActiveExperimentChanged: Event<Experiment | undefined>;
    protected readonly onRunsChangedEmitter: Emitter<{
        experimentId: string;
        runs: Run[];
    }>;
    readonly onRunsChanged: Event<{
        experimentId: string;
        runs: Run[];
    }>;
    constructor();
    protected initialize(): void;
    protected handleExperimentEvent(message: {
        type: string;
        data: Record<string, unknown>;
    }): void;
    /**
     * List all experiments
     */
    listExperiments(): Promise<Experiment[]>;
    /**
     * Refresh experiments from server
     */
    refreshExperiments(): Promise<void>;
    /**
     * Get a specific experiment
     */
    getExperiment(experimentId: string): Promise<Experiment | undefined>;
    /**
     * Create a new experiment
     */
    createExperiment(name: string, description?: string): Promise<Experiment | undefined>;
    /**
     * Delete an experiment
     */
    deleteExperiment(experimentId: string): Promise<boolean>;
    /**
     * Set the active experiment
     */
    setActiveExperiment(experiment: Experiment | undefined): void;
    /**
     * Get the active experiment
     */
    getActiveExperiment(): Experiment | undefined;
    /**
     * List runs for an experiment
     */
    listRuns(experimentId: string): Promise<Run[]>;
    /**
     * Refresh runs for an experiment
     */
    refreshRuns(experimentId: string): Promise<void>;
    /**
```
