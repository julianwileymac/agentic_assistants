# Chunk: f2b6e772c023_13

- source: `src/agentic_assistants/data/catalog.py`
- lines: 1013-1112
- chunk: 14/20

```
 etc.)
            **kwargs: Additional URLDataset arguments
            
        Returns:
            Created URLDataset instance
        """
        from agentic_assistants.data.datasets import URLDataset
        
        dataset = URLDataset(url=url, content_type=content_type, **kwargs)
        self.add_dataset(name, dataset)
        return dataset
    
    def add_rss_dataset(
        self,
        name: str,
        url: str,
        max_entries: Optional[int] = None,
        **kwargs,
    ) -> "AbstractDataset":
        """
        Add an RSS feed dataset to the catalog.
        
        Args:
            name: Dataset name
            url: RSS feed URL
            max_entries: Maximum entries to fetch
            **kwargs: Additional RSSDataset arguments
            
        Returns:
            Created RSSDataset instance
        """
        from agentic_assistants.data.datasets import RSSDataset
        
        dataset = RSSDataset(url=url, max_entries=max_entries, **kwargs)
        self.add_dataset(name, dataset)
        return dataset
    
    def add_website_dataset(
        self,
        name: str,
        url: str,
        selectors: Dict[str, Any],
        **kwargs,
    ) -> "AbstractDataset":
        """
        Add a website scraping dataset to the catalog.
        
        Args:
            name: Dataset name
            url: Website URL
            selectors: CSS selectors for extraction
            **kwargs: Additional WebsiteDataset arguments
            
        Returns:
            Created WebsiteDataset instance
        """
        from agentic_assistants.data.datasets import WebsiteDataset
        
        dataset = WebsiteDataset(url=url, selectors=selectors, **kwargs)
        self.add_dataset(name, dataset)
        return dataset
    
    def add_kaggle_dataset(
        self,
        name: str,
        dataset_ref: Optional[str] = None,
        competition: Optional[str] = None,
        file_name: Optional[str] = None,
        **kwargs,
    ) -> "AbstractDataset":
        """
        Add a Kaggle dataset to the catalog.
        
        Args:
            name: Dataset name
            dataset_ref: Kaggle dataset reference (username/dataset-name)
            competition: Competition name (alternative to dataset_ref)
            file_name: Specific file to load
            **kwargs: Additional KaggleDataset arguments
            
        Returns:
            Created KaggleDataset instance
        """
        from agentic_assistants.data.datasets import KaggleDataset
        
        dataset = KaggleDataset(
            dataset_ref=dataset_ref,
            competition=competition,
            file_name=file_name,
            **kwargs,
        )
        self.add_dataset(name, dataset)
        return dataset
    
    def add_s3_public_dataset(
        self,
        name: str,
        bucket: str,
        key: str,
```
