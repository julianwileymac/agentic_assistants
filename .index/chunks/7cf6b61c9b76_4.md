# Chunk: 7cf6b61c9b76_4

- source: `webui/src/app/models/training/new/page.tsx`
- lines: 281-355
- chunk: 5/6

```

                  value={[config.gradient_accumulation_steps]}
                  onValueChange={([value]) => setConfig({ ...config, gradient_accumulation_steps: value })}
                  min={1}
                  max={16}
                  step={1}
                />
              </div>
            </div>

            <Separator />

            <div className="flex items-center justify-between">
              <div className="space-y-0.5">
                <Label>BF16 Training</Label>
                <p className="text-sm text-muted-foreground">
                  Use bfloat16 precision for faster training
                </p>
              </div>
              <Switch
                checked={config.bf16}
                onCheckedChange={(checked) => setConfig({ ...config, bf16: checked })}
              />
            </div>

            <div className="flex items-center justify-between">
              <div className="space-y-0.5">
                <Label>Gradient Checkpointing</Label>
                <p className="text-sm text-muted-foreground">
                  Trade compute for memory savings
                </p>
              </div>
              <Switch
                checked={config.gradient_checkpointing}
                onCheckedChange={(checked) => setConfig({ ...config, gradient_checkpointing: checked })}
              />
            </div>
          </CardContent>
        </Card>

        {/* LoRA Settings */}
        {config.method !== "full" && (
          <Card>
            <CardHeader>
              <CardTitle>LoRA Configuration</CardTitle>
              <CardDescription>Configure Low-Rank Adaptation parameters</CardDescription>
            </CardHeader>
            <CardContent className="space-y-4">
              <div className="grid gap-4 md:grid-cols-3">
                <div className="space-y-2">
                  <Label>LoRA Rank (r): {config.lora_r}</Label>
                  <Slider
                    value={[config.lora_r]}
                    onValueChange={([value]) => setConfig({ ...config, lora_r: value })}
                    min={4}
                    max={64}
                    step={4}
                  />
                </div>
                <div className="space-y-2">
                  <Label>LoRA Alpha: {config.lora_alpha}</Label>
                  <Slider
                    value={[config.lora_alpha]}
                    onValueChange={([value]) => setConfig({ ...config, lora_alpha: value })}
                    min={8}
                    max={128}
                    step={8}
                  />
                </div>
                <div className="space-y-2">
                  <Label>LoRA Dropout: {config.lora_dropout}</Label>
                  <Slider
                    value={[config.lora_dropout * 100]}
                    onValueChange={([value]) => setConfig({ ...config, lora_dropout: value / 100 })}
```
