# Chunk: 15e48b33ad7c_2

- source: `src/agentic_assistants/data/shared_storage.py`
- lines: 160-245
- chunk: 3/11

```
[list[str]] = None,
        groups: Optional[list[str]] = None,
        session_id: Optional[str] = None,
        user_id: Optional[str] = None,
        description: Optional[str] = None,
    ) -> SharedArtifactMetadata:
        """
        Share an artifact by copying it to shared storage.
        
        Args:
            source_path: Path to the source file
            name: Optional name (defaults to filename)
            tags: Tags to apply
            groups: Groups to add to
            session_id: Source session ID
            user_id: Source user ID
            description: Optional description
        
        Returns:
            Metadata for the shared artifact
        """
        source = Path(source_path)
        if not source.exists():
            raise FileNotFoundError(f"Source file not found: {source_path}")
        
        # Generate artifact ID and destination path
        artifact_id = str(uuid4())
        artifact_name = name or source.name
        dest_path = self.shared_dir / artifact_id / artifact_name
        dest_path.parent.mkdir(parents=True, exist_ok=True)
        
        # Copy file
        shutil.copy2(source, dest_path)
        
        # Detect MIME type
        mime_type = self._detect_mime_type(source)
        
        # Create metadata
        metadata = SharedArtifactMetadata(
            artifact_id=artifact_id,
            name=artifact_name,
            original_path=str(source.resolve()),
            shared_path=str(dest_path),
            tags=tags or [],
            groups=groups or [],
            source_session_id=session_id,
            source_user_id=user_id,
            created_at=datetime.now(),
            size_bytes=dest_path.stat().st_size,
            mime_type=mime_type,
            description=description,
        )
        
        # Store in index
        self._index[artifact_id] = metadata
        self._save_index()
        
        # Create links in tag/group directories
        self._update_links(metadata)
        
        logger.info(f"Shared artifact: {artifact_name} (id={artifact_id})")
        
        return metadata
    
    def _detect_mime_type(self, path: Path) -> Optional[str]:
        """Detect MIME type from file extension."""
        ext = path.suffix.lower()
        mime_map = {
            ".parquet": "application/x-parquet",
            ".json": "application/json",
            ".jsonl": "application/x-ndjson",
            ".csv": "text/csv",
            ".txt": "text/plain",
            ".md": "text/markdown",
            ".py": "text/x-python",
            ".yaml": "application/x-yaml",
            ".yml": "application/x-yaml",
            ".png": "image/png",
            ".jpg": "image/jpeg",
            ".jpeg": "image/jpeg",
            ".gif": "image/gif",
        }
        return mime_map.get(ext)
    
    def _update_links(self, metadata: SharedArtifactMetadata):
```
