# Chunk: 3de876d90055_1

- source: `webui/src/app/kubernetes/models/page.tsx`
- lines: 109-209
- chunk: 2/7

```
useState({
    name: "",
    model_name: "",
    serving_type: "ollama",
    replicas: 1,
    cpu_request: "500m",
    cpu_limit: "4000m",
    memory_request: "2Gi",
    memory_limit: "8Gi",
    context_length: 4096,
  });

  const [endpointDialog, setEndpointDialog] = React.useState<{
    open: boolean;
    name: string;
    internal?: string;
    external?: string;
  }>({ open: false, name: "" });

  const { trigger: deployModel, isMutating: isDeploying } = useDeployModel();
  const { trigger: deleteModel, isMutating: isDeleting } = useDeleteModelDeployment();
  const { trigger: scaleModel } = useScaleModelDeployment();
  const { trigger: getEndpoint } = useModelEndpoint();
  const { trigger: setAsCodeAssistant } = useSetModelAsCodeAssistant();

  const handleDeploy = async () => {
    try {
      await deployModel(deployForm);
      toast.success(`Deploying ${deployForm.model_name}...`);
      setDeployDialog(false);
      setDeployForm({
        name: "",
        model_name: "",
        serving_type: "ollama",
        replicas: 1,
        cpu_request: "500m",
        cpu_limit: "4000m",
        memory_request: "2Gi",
        memory_limit: "8Gi",
        context_length: 4096,
      });
      mutate();
    } catch (error) {
      toast.error("Failed to deploy model");
    }
  };

  const handleDelete = async (name: string) => {
    if (!confirm(`Are you sure you want to delete ${name}?`)) return;

    try {
      await deleteModel({ name });
      toast.success(`Deleted ${name}`);
      mutate();
    } catch (error) {
      toast.error("Failed to delete model");
    }
  };

  const handleViewEndpoint = async (name: string) => {
    try {
      const result = await getEndpoint({ name });
      setEndpointDialog({
        open: true,
        name,
        internal: result.internal_endpoint,
        external: result.external_endpoint,
      });
    } catch (error) {
      toast.error("Failed to get endpoint");
    }
  };

  const handleUseAsCodeAssistant = async (name: string) => {
    try {
      const result = await setAsCodeAssistant({ name });
      toast.success(result.message);
    } catch (error) {
      toast.error("Failed to configure model");
    }
  };

  return (
    <div className="space-y-6">
      {/* Page Header */}
      <div className="flex items-center justify-between">
        <div>
          <h1 className="text-3xl font-bold tracking-tight">Model Serving</h1>
          <p className="text-muted-foreground">
            Deploy and manage LLM models on your Kubernetes cluster
          </p>
        </div>
        <div className="flex items-center gap-2">
          <Button variant="outline" onClick={() => mutate()}>
            <RefreshCw className="size-4 mr-2" />
            Refresh
          </Button>
          <Button onClick={() => setDeployDialog(true)}>
            <Plus className="size-4 mr-2" />
            Deploy Model
```
