# Chunk: bc7788fa5c9b_3

- source: `src/agentic_assistants/data/corpus/indexer.py`
- lines: 269-300
- chunk: 4/4

```
 term_lower in self._index:
                    should_docs |= set(self._index[term_lower].keys())
            candidates &= should_docs
        
        return list(candidates)
    
    def get_term_documents(self, term: str) -> List[str]:
        """Get all documents containing a term."""
        term_lower = self.processor.process(term)[0] if term else ""
        if term_lower in self._index:
            return list(self._index[term_lower].keys())
        return []
    
    def get_statistics(self) -> Dict[str, Any]:
        """Get index statistics."""
        return {
            "document_count": len(self._documents),
            "unique_terms": len(self._index),
            "avg_doc_length": round(self._avg_doc_length, 2),
            "total_postings": sum(
                sum(len(positions) for positions in docs.values())
                for docs in self._index.values()
            ),
        }
    
    def clear(self) -> None:
        """Clear the index."""
        self._index.clear()
        self._documents.clear()
        self._doc_lengths.clear()
        self._avg_doc_length = 0.0
```
