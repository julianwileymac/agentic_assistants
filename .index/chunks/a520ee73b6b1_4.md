# Chunk: a520ee73b6b1_4

- source: `src/agentic_assistants/data/templates/builtin/web_templates.py`
- lines: 342-371
- chunk: 5/5

```
     default=0,
        ),
    ]
    
    def create_dataset(self, **params) -> ScraperDataset:
        from agentic_assistants.data.datasets.scraper_dataset import TableExtractor
        
        extractor = TableExtractor(
            table_selector=params.get("table_selector", "table"),
            table_index=params.get("table_index", 0),
        )
        
        return ScraperDataset(
            urls=params["url"],
            extractor=extractor,
        )


def get_web_templates() -> List[DataSourceTemplate]:
    """Get all web scraping and RSS templates."""
    return [
        GenericRSSTemplate(),
        HackerNewsRSSTemplate(),
        ArxivRSSTemplate(),
        GenericWebsiteTemplate(),
        WikipediaTemplate(),
        ProductPageTemplate(),
        TableScraperTemplate(),
    ]
```
