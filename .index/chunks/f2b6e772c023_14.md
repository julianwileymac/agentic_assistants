# Chunk: f2b6e772c023_14

- source: `src/agentic_assistants/data/catalog.py`
- lines: 1099-1186
- chunk: 15/20

```
=dataset_ref,
            competition=competition,
            file_name=file_name,
            **kwargs,
        )
        self.add_dataset(name, dataset)
        return dataset
    
    def add_s3_public_dataset(
        self,
        name: str,
        bucket: str,
        key: str,
        **kwargs,
    ) -> "AbstractDataset":
        """
        Add a public S3 bucket dataset to the catalog.
        
        Args:
            name: Dataset name
            bucket: S3 bucket name
            key: Object key
            **kwargs: Additional S3PublicDataset arguments
            
        Returns:
            Created S3PublicDataset instance
        """
        from agentic_assistants.data.datasets import S3PublicDataset
        
        dataset = S3PublicDataset(bucket=bucket, key=key, **kwargs)
        self.add_dataset(name, dataset)
        return dataset
    
    # =========================================================================
    # Indexing
    # =========================================================================
    
    def index_source(self, source_id: str, force: bool = False) -> Dict[str, Any]:
        """
        Index a data source into the catalog.
        
        This discovers the schema and creates catalog entries for
        all tables and columns.
        
        Args:
            source_id: ID of the data source to index
            force: If True, re-index even if already indexed
            
        Returns:
            Dictionary with indexing statistics
        """
        source = self.source_manager.get_source(source_id)
        if not source:
            return {"error": "Source not found", "source_id": source_id}
        
        # Check if already indexed
        existing_tables = self.get_source_tables(source_id)
        if existing_tables and not force:
            return {
                "status": "already_indexed",
                "source_id": source_id,
                "table_count": len(existing_tables),
            }
        
        # Discover schema
        schema = self.source_manager.discover_schema(source)
        
        # Index tables and columns
        stats = {
            "source_id": source_id,
            "source_name": source.name,
            "tables_indexed": 0,
            "columns_indexed": 0,
            "errors": [],
        }
        
        for table in schema.tables:
            try:
                table_entry = self._index_table(source, table)
                stats["tables_indexed"] += 1
                stats["columns_indexed"] += len(table_entry.columns)
            except Exception as e:
                stats["errors"].append(f"Failed to index table {table.name}: {e}")
                logger.error(f"Failed to index table {table.name}: {e}")
        
        logger.info(
            f"Indexed source {source.name}: "
```
