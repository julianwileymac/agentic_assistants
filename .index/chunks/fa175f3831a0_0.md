# Chunk: fa175f3831a0_0

- source: `frontend/packages/agentic-core/lib/browser/agentic-backend-service.d.ts`
- lines: 1-70
- chunk: 1/1

```
/**
 * Agentic Backend Service
 *
 * Provides REST API communication with the Python backend.
 */
import { Emitter, Event } from '@theia/core/lib/common';
export declare const AgenticBackendServiceSymbol: unique symbol;
export interface AgenticConfig {
    backendUrl: string;
    apiVersion: string;
}
export interface ApiResponse<T> {
    data?: T;
    error?: string;
    status: number;
}
export declare class AgenticBackendService {
    protected readonly onConnectionChangedEmitter: Emitter<boolean>;
    readonly onConnectionChanged: Event<boolean>;
    protected config: AgenticConfig;
    protected connected: boolean;
    /**
     * Configure the backend service
     */
    configure(config: Partial<AgenticConfig>): void;
    /**
     * Get the full API URL for an endpoint
     */
    protected getApiUrl(endpoint: string): string;
    /**
     * Check connection to the backend
     */
    checkConnection(): Promise<boolean>;
    /**
     * Check if connected to backend
     */
    isConnected(): boolean;
    /**
     * Make a GET request
     */
    get<T>(endpoint: string, params?: Record<string, string>): Promise<ApiResponse<T>>;
    /**
     * Make a POST request
     */
    post<T>(endpoint: string, body?: unknown): Promise<ApiResponse<T>>;
    /**
     * Make a PATCH request
     */
    patch<T>(endpoint: string, body?: unknown): Promise<ApiResponse<T>>;
    /**
     * Make a DELETE request
     */
    delete<T>(endpoint: string): Promise<ApiResponse<T>>;
    listExperiments(): Promise<ApiResponse<unknown>>;
    getExperiment(id: string): Promise<ApiResponse<unknown>>;
    createExperiment(name: string, description?: string): Promise<ApiResponse<unknown>>;
    listRuns(experimentId: string): Promise<ApiResponse<unknown>>;
    listArtifacts(params?: Record<string, string>): Promise<ApiResponse<unknown>>;
    getArtifact(id: string): Promise<ApiResponse<unknown>>;
    listSessions(): Promise<ApiResponse<unknown>>;
    getSession(id: string): Promise<ApiResponse<unknown>>;
    createSession(name: string, description?: string): Promise<ApiResponse<unknown>>;
    activateSession(id: string): Promise<ApiResponse<unknown>>;
    getConfig(): Promise<ApiResponse<unknown>>;
    updateConfig(section: string, updates: unknown): Promise<ApiResponse<unknown>>;
    listFiles(path?: string): Promise<ApiResponse<unknown>>;
    previewTable(path: string, offset?: number, limit?: number): Promise<ApiResponse<unknown>>;
    getSchema(path: string): Promise<ApiResponse<unknown>>;
}
//# sourceMappingURL=agentic-backend-service.d.ts.map
```
