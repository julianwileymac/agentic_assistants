# Chunk: bc7788fa5c9b_0

- source: `src/agentic_assistants/data/corpus/indexer.py`
- lines: 1-118
- chunk: 1/4

```
"""
Corpus indexing for fast text search and retrieval.
"""

import re
from collections import defaultdict
from dataclasses import dataclass
from typing import Any, Callable, Dict, List, Optional, Set


class TextProcessor:
    """
    Text preprocessing for corpus indexing.
    """
    
    def __init__(
        self,
        lowercase: bool = True,
        remove_punctuation: bool = True,
        remove_numbers: bool = False,
        stem: bool = False,
    ):
        """
        Initialize text processor.
        
        Args:
            lowercase: Convert to lowercase
            remove_punctuation: Remove punctuation
            remove_numbers: Remove numeric tokens
            stem: Apply stemming
        """
        self.lowercase = lowercase
        self.remove_punctuation = remove_punctuation
        self.remove_numbers = remove_numbers
        self.stem = stem
        
        self._stemmer = None
        if stem:
            self._init_stemmer()
    
    def _init_stemmer(self):
        """Initialize NLTK stemmer."""
        try:
            from nltk.stem import PorterStemmer
            self._stemmer = PorterStemmer()
        except ImportError:
            pass
    
    def process(self, text: str) -> List[str]:
        """Process text into tokens."""
        # Lowercase
        if self.lowercase:
            text = text.lower()
        
        # Tokenize
        if self.remove_punctuation:
            tokens = re.findall(r'\b\w+\b', text)
        else:
            tokens = text.split()
        
        # Remove numbers
        if self.remove_numbers:
            tokens = [t for t in tokens if not t.isnumeric()]
        
        # Stem
        if self.stem and self._stemmer:
            tokens = [self._stemmer.stem(t) for t in tokens]
        
        return tokens


@dataclass
class SearchResult:
    """A search result from the corpus index."""
    
    doc_id: str
    score: float
    matches: List[str]
    preview: str = ""


class CorpusIndexer:
    """
    Inverted index for fast corpus search.
    
    Provides:
    - Full-text search
    - Boolean queries
    - TF-IDF ranking
    """
    
    def __init__(
        self,
        processor: Optional[TextProcessor] = None,
    ):
        """
        Initialize corpus indexer.
        
        Args:
            processor: Text processor for tokenization
        """
        self.processor = processor or TextProcessor()
        
        # Inverted index: term -> {doc_id: positions}
        self._index: Dict[str, Dict[str, List[int]]] = defaultdict(dict)
        
        # Document storage
        self._documents: Dict[str, str] = {}
        
        # Statistics
        self._doc_lengths: Dict[str, int] = {}
        self._avg_doc_length: float = 0.0
    
    def index_document(self, doc_id: str, text: str) -> int:
        """
        Index a document.
        
```
