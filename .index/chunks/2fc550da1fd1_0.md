# Chunk: 2fc550da1fd1_0

- source: `frontend/packages/agentic-artifacts/lib/browser/artifact-service.d.ts`
- lines: 1-105
- chunk: 1/2

```
/**
 * Artifact Service
 *
 * Provides artifact management functionality with tagging and grouping.
 */
import { Emitter, Event } from '@theia/core/lib/common';
import { AgenticBackendService, AgenticWebSocketClient } from 'agentic-core/lib/browser';
export declare const ArtifactServiceSymbol: unique symbol;
export interface Artifact {
    id: string;
    name: string;
    path: string;
    size: number;
    mime_type?: string;
    tags: string[];
    groups: string[];
    session_id?: string;
    experiment_id?: string;
    run_id?: string;
    created_at: string;
    updated_at: string;
    description?: string;
    is_shared: boolean;
}
export interface ArtifactsListResponse {
    artifacts: Artifact[];
    total: number;
}
export interface GroupInfo {
    name: string;
    artifact_count: number;
    total_size: number;
}
export interface TagInfo {
    name: string;
    artifact_count: number;
}
export declare class ArtifactService {
    protected readonly backendService: AgenticBackendService;
    protected readonly wsClient: AgenticWebSocketClient;
    protected artifacts: Artifact[];
    protected groups: GroupInfo[];
    protected tags: TagInfo[];
    protected readonly onArtifactsChangedEmitter: Emitter<Artifact[]>;
    readonly onArtifactsChanged: Event<Artifact[]>;
    protected readonly onGroupsChangedEmitter: Emitter<GroupInfo[]>;
    readonly onGroupsChanged: Event<GroupInfo[]>;
    protected readonly onTagsChangedEmitter: Emitter<TagInfo[]>;
    readonly onTagsChanged: Event<TagInfo[]>;
    constructor();
    protected initialize(): void;
    protected handleArtifactEvent(message: {
        type: string;
        data: Record<string, unknown>;
    }): void;
    /**
     * List all artifacts with optional filtering
     */
    listArtifacts(params?: {
        tags?: string[];
        groups?: string[];
        sessionId?: string;
        experimentId?: string;
        sharedOnly?: boolean;
    }): Promise<Artifact[]>;
    /**
     * Refresh artifacts from server
     */
    refreshArtifacts(): Promise<void>;
    /**
     * Get a specific artifact
     */
    getArtifact(artifactId: string): Promise<Artifact | undefined>;
    /**
     * Delete an artifact
     */
    deleteArtifact(artifactId: string): Promise<boolean>;
    /**
     * Update artifact tags
     */
    updateArtifactTags(artifactId: string, addTags: string[], removeTags: string[]): Promise<Artifact | undefined>;
    /**
     * Update artifact groups
     */
    updateArtifactGroups(artifactId: string, addGroups: string[], removeGroups: string[]): Promise<Artifact | undefined>;
    /**
     * Share an artifact
     */
    shareArtifact(artifactId: string): Promise<Artifact | undefined>;
    /**
     * List all groups
     */
    listGroups(): Promise<GroupInfo[]>;
    /**
     * Refresh groups from server
     */
    refreshGroups(): Promise<void>;
    /**
     * List all tags
     */
    listTags(): Promise<TagInfo[]>;
    /**
     * Refresh tags from server
     */
```
