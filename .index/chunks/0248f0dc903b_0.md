# Chunk: 0248f0dc903b_0

- source: `src/agentic_assistants/data/corpus/__init__.py`
- lines: 1-46
- chunk: 1/1

```
"""
Lexicon and Corpora Indexing System.

This module provides tools for indexing custom lexicons and training corpora
for model fine-tuning and vocabulary expansion.

Example:
    >>> from agentic_assistants.data.corpus import CorpusManager, Lexicon
    >>> 
    >>> # Create a corpus
    >>> corpus = CorpusManager("domain_corpus")
    >>> corpus.add_documents(["Document 1...", "Document 2..."])
    >>> 
    >>> # Build a lexicon
    >>> lexicon = corpus.build_lexicon()
"""

from agentic_assistants.data.corpus.manager import (
    CorpusManager,
    Corpus,
    CorpusConfig,
)
from agentic_assistants.data.corpus.lexicon import (
    Lexicon,
    LexiconEntry,
    LexiconBuilder,
)
from agentic_assistants.data.corpus.indexer import (
    CorpusIndexer,
    TextProcessor,
)

__all__ = [
    # Corpus
    "CorpusManager",
    "Corpus",
    "CorpusConfig",
    # Lexicon
    "Lexicon",
    "LexiconEntry",
    "LexiconBuilder",
    # Indexer
    "CorpusIndexer",
    "TextProcessor",
]
```
