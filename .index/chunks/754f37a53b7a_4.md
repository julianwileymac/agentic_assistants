# Chunk: 754f37a53b7a_4

- source: `src/agentic_assistants/data/corpus/manager.py`
- lines: 331-367
- chunk: 5/5

```
     storage_path=str(self.storage_path),
            )
            self._corpora[name] = Corpus(config)
        return self._corpora[name]
    
    def list_corpora(self) -> List[str]:
        """List all corpora."""
        names = set(self._corpora.keys())
        
        # Check disk
        for path in self.storage_path.iterdir():
            if path.is_dir():
                names.add(path.name)
        
        return sorted(names)
    
    def delete_corpus(self, name: str) -> bool:
        """Delete a corpus."""
        import shutil
        
        if name in self._corpora:
            del self._corpora[name]
        
        corpus_path = self.storage_path / name
        if corpus_path.exists():
            shutil.rmtree(corpus_path)
            return True
        return False
    
    def get_statistics(self) -> Dict[str, Any]:
        """Get statistics for all corpora."""
        stats = {}
        for name in self.list_corpora():
            corpus = self.get_corpus(name)
            stats[name] = corpus.get_statistics()
        return stats
```
