# Chunk: 7d4e7aa0546f_0

- source: `frontend/packages/agentic-core/lib/browser/agentic-websocket-client.d.ts`
- lines: 1-86
- chunk: 1/2

```
/**
 * Agentic WebSocket Client
 *
 * Provides real-time event streaming from the Python backend.
 */
/// <reference types="node" />
import { Emitter, Event, Disposable } from '@theia/core/lib/common';
export declare const AgenticWebSocketClientSymbol: unique symbol;
export interface WSMessage {
    id: string;
    type: string;
    timestamp: string;
    data: Record<string, unknown>;
    source: string;
}
export interface WSCommand {
    id?: string;
    command: string;
    params: Record<string, unknown>;
}
export declare enum EventType {
    CONNECTED = "connected",
    DISCONNECTED = "disconnected",
    PING = "ping",
    PONG = "pong",
    EXPERIMENT_CREATED = "experiment.created",
    EXPERIMENT_UPDATED = "experiment.updated",
    EXPERIMENT_DELETED = "experiment.deleted",
    RUN_STARTED = "run.started",
    RUN_ENDED = "run.ended",
    RUN_LOG = "run.log",
    METRICS_LOGGED = "metrics.logged",
    ARTIFACT_CREATED = "artifact.created",
    ARTIFACT_UPDATED = "artifact.updated",
    ARTIFACT_DELETED = "artifact.deleted",
    ARTIFACT_SHARED = "artifact.shared",
    SESSION_CREATED = "session.created",
    SESSION_ACTIVATED = "session.activated",
    SESSION_DELETED = "session.deleted",
    CONFIG_CHANGED = "config.changed",
    ERROR = "error",
    NOTIFICATION = "notification"
}
export declare class AgenticWebSocketClient implements Disposable {
    protected socket: WebSocket | undefined;
    protected connectionId: string | undefined;
    protected reconnectAttempts: number;
    protected maxReconnectAttempts: number;
    protected reconnectDelay: number;
    protected pingInterval: NodeJS.Timeout | undefined;
    protected subscriptions: Set<string>;
    protected wsUrl: string;
    protected readonly onConnectedEmitter: Emitter<string>;
    readonly onConnected: Event<string>;
    protected readonly onDisconnectedEmitter: Emitter<void>;
    readonly onDisconnected: Event<void>;
    protected readonly onMessageEmitter: Emitter<WSMessage>;
    readonly onMessage: Event<WSMessage>;
    protected readonly onErrorEmitter: Emitter<Error>;
    readonly onError: Event<Error>;
    protected readonly onExperimentEventEmitter: Emitter<WSMessage>;
    readonly onExperimentEvent: Event<WSMessage>;
    protected readonly onArtifactEventEmitter: Emitter<WSMessage>;
    readonly onArtifactEvent: Event<WSMessage>;
    protected readonly onSessionEventEmitter: Emitter<WSMessage>;
    readonly onSessionEvent: Event<WSMessage>;
    protected readonly onRunLogEmitter: Emitter<WSMessage>;
    readonly onRunLog: Event<WSMessage>;
    protected readonly onNotificationEmitter: Emitter<WSMessage>;
    readonly onNotification: Event<WSMessage>;
    /**
     * Configure the WebSocket URL
     */
    configure(url: string): void;
    /**
     * Connect to the WebSocket server
     */
    connect(clientId?: string): Promise<void>;
    /**
     * Disconnect from the WebSocket server
     */
    disconnect(): void;
    /**
     * Check if connected
     */
```
