# Chunk: ff8a522876e5_0

- source: `.venv-lab/Lib/site-packages/jupyter_lsp/specs/config/__init__.py`
- lines: 1-12
- chunk: 1/1

```
import json
import pathlib

CONFIGS = pathlib.Path(__file__).parent


def load_config_schema(key):
    """load a keyed filename"""
    return json.loads(
        (CONFIGS / "{}.schema.json".format(key)).read_text(encoding="utf-8")
    )
```
