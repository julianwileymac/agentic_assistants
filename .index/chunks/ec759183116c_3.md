# Chunk: ec759183116c_3

- source: `.venv-lab/Lib/site-packages/pygments/lexers/lean.py`
- lines: 228-242
- chunk: 4/4

```
^/-]+', String.Doc),
            (r'-/', String.Doc, '#pop'),
            (r'[/-]', String.Doc)
        ],
        'string': [
            (r'[^\\"]+', String.Double),
            (r'\\[n"\\\n]', String.Escape),
            ('"', String.Double, '#pop'),
        ],
    }

    def analyse_text(text):
        if re.search(r'^import [A-Z]', text, re.MULTILINE):
            return 0.1
```
