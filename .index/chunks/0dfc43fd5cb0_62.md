# Chunk: 0dfc43fd5cb0_62

- source: `webui/.next/server/chunks/ssr/Documents_GitHub_agentic_assistants_webui_eeccbf6d._.js.map`
- lines: 1-1
- chunk: 63/87

```
mit<Notification, 'id' | 'createdAt'>) => string;\r\n  removeNotification: (id: string) => void;\r\n  clearNotifications: () => void;\r\n}\r\n\r\nexport const useNotificationStore = create<NotificationStore>((set) => ({\r\n  notifications: [],\r\n\r\n  addNotification: (notification) => {\r\n    const id = Math.random().toString(36).substring(7);\r\n    const newNotification: Notification = {\r\n      ...notification,\r\n      id,\r\n      createdAt: new Date(),\r\n    };\r\n\r\n    set((state) => ({\r\n      notifications: [...state.notifications, newNotification],\r\n    }));\r\n\r\n    // Auto-remove after duration\r\n    if (notification.duration !== 0) {\r\n      setTimeout(() => {\r\n        set((state) => ({\r\n          notifications: state.notifications.filter((n) => n.id !== id),\r\n        }));\r\n      }, notification.duration || 5000);\r\n    }\r\n\r\n    return id;\r\n  },\r\n\r\n  removeNotification: (id) =>\r\n    set((state) => ({\r\n      notifications: state.notifications.filter((n) => n.id !== id),\r\n    })),\r\n\r\n  clearNotifications: () => set({ notifications: [] }),\r\n}));\r\n\r\n// ============================================================================\r\n// Recent Activity Store\r\n// ============================================================================\r\n\r\ninterface Activity {\r\n  id: string;\r\n  type: 'project' | 'agent' | 'flow' | 'component' | 'experiment';\r\n  action: 'create' | 'update' | 'delete' | 'run' | 'view';\r\n  resourceId: string;\r\n  resourceName: string;\r\n  timestamp: Date;\r\n}\r\n\r\ninterface ActivityStore {\r\n  activities: Activity[];\r\n  maxActivities: number;\r\n\r\n  addActivity: (activity: Omit<Activity, 'id' | 'timestamp'>) => void;\r\n  clearActivities: () => void;\r\n}\r\n\r\nexport const useActivityStore = create<ActivityStore>()(\r\n  persist(\r\n    (set) => ({\r\n      activities: [],\r\n      maxActivities: 50,\r\n\r\n      addActivity: (activity) =>\r\n        set((state) => {\r\n          const newActivity: Activity = {\r\n            ...activity,\r\n            id: Math.random().toString(36).substring(7),\r\n            timestamp: new Date(),\r\n          };\r\n          const activities = [newActivity, ...state.activities].slice(\r\n            0,\r\n            state.maxActivities\r\n          );\r\n          return { activities };\r\n        }),\r\n\r\n      clearActivities: () => set({ activities: [] }),\r\n    }),\r\n    {\r\n      name: 'activity-storage',\r\n      partialize: (state) => ({\r\n        activities: state.activities.slice(0, 20), // Only persist last 20\r\n      }),\r\n    }\r\n  )\r\n);\r\n\r\n"],"names":[],"mappings":"uCAAa,IAAA,EAAA,EAAA,CAAA,CAAA,OAA4B,EAAE,CAAC,EAAE,EAAE,EAAE,EAAE,EAAE,EAAE,EAAE,KAAK,IAAI,EAAE,SAAS,eAAe,CAAC,EAAE,CAAC,QAAQ,OAAO,CAAC,SAAS,EAAE,CAAC,QAAE,AAAC,OAAM,OAAO,CAAC,GAAG,EAAE,CAAC,EAAE,EAAE,OAAO,CAAC,IAAI,IAAI,EAAM,UAAJ,EAAY,EAAE,GAAG,EAAE,EAAE,GAAG,CAAC,GAAG,CAAC,CAAC,EAAE,EAAE,GAAG,EAAE,GAAG,CAAD,CAAG,SAAS,CAAC
```
