# Chunk: f8d6390d57a9_30

- source: `webui/.next/dev/server/chunks/ssr/[root-of-the-server]__630d161a._.js.map`
- lines: 17-17
- chunk: 31/62

```
arg.component_type);\n      if (arg?.save_component !== undefined) params.set('save_component', String(arg.save_component));\n      const query = params.toString();\n      return apiFetch<GenerateFromNotesResponse>(`${url}${query ? `?${query}` : ''}`, { method: 'POST' });\n    }\n  );\n}\n\nexport function useGenerationChat() {\n  return useSWRMutation(\n    '/api/v1/generate/chat',\n    async (url: string, { arg }: { arg: GenerationChatRequest }) => {\n      return apiFetch<GenerationChatResponse>(url, {\n        method: 'POST',\n        body: JSON.stringify(arg),\n      });\n    }\n  );\n}\n\n// ============================================================================\n// Component Categories API\n// ============================================================================\n\nexport function useComponentCategories() {\n  return useSWR<{\n    categories: { id: string; name: string; description: string; group: string }[];\n    groups: { id: string; name: string }[];\n  }>('/api/v1/components/categories/list', fetcher, defaultSwrConfig);\n}\n\nexport function useInstallBaseComponents() {\n  return useSWRMutation(\n    '/api/v1/components/base/install',\n    async (url: string) => {\n      return apiFetch<{ installed: string[]; skipped: string[]; errors: string[]; total_base_components: number }>(url, { method: 'POST' });\n    }\n  );\n}\n\n// ============================================================================\n// Data Catalog API\n// ============================================================================\n\nexport function useCatalogSearch(query: string | null, params?: {\n  entry_type?: string;\n  source_id?: string;\n  limit?: number;\n}) {\n  const searchParams = new URLSearchParams();\n  if (query) searchParams.set('query', query);\n  if (params?.entry_type) searchParams.set('entry_type', params.entry_type);\n  if (params?.source_id) searchParams.set('source_id', params.source_id);\n  if (params?.limit) searchParams.set('limit', String(params.limit));\n  const queryStr = searchParams.toString();\n  \n  return useSWR<{\n    entries: { id: string; name: string; entry_type: string; source_id: string; description: string; tags: string[] }[];\n    total: number;\n    query: string;\n    filters: Record<string, unknown>;\n  }>(\n    query ? `/api/v1/datasources/catalog/search?${queryStr}` : null,\n    fetcher,\n    defaultSwrConfig\n  );\n}\n\nexport function useCatalogStats() {\n  return useSWR<{\n    total_entries: number;\n    table_count: number;\n    column_count: number;\n    source_count: number;\n    lineage_count: number;\n    entries_by_source: Record<string, number>;\n  }>('/api/v1/datasources/catalog/stats', fetcher, defaultSwrConfig);\n}\n\n// ============================================================================\n// Kubernetes API\n// ============================================================================\n\n// Types\ninterface ClusterInfo {\n  name: string;\n  version: string;\n  node_count: number;\n  r
```
