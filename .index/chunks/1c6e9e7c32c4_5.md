# Chunk: 1c6e9e7c32c4_5

- source: `src/agentic_assistants/data/datasets/scraper_dataset.py`
- lines: 412-492
- chunk: 6/9

```
   load_args=load_args,
            save_args=save_args,
            credentials=credentials,
            metadata=metadata,
        )
        
        self.urls = urls
        self.render_js = render_js
        self.max_pages = max_pages
        self.follow_pagination = follow_pagination
        self.cache_dir = Path(cache_dir) if cache_dir else Path("./data/scraper_cache")
        
        # Setup extractor
        if extractor:
            self.extractor = extractor
        elif selectors:
            self.extractor = CSSExtractor(selectors)
        else:
            # Default extractor - get text content
            self.extractor = CSSExtractor({"content": "body"})
        
        # Results tracking
        self._results: List[ScrapeResult] = []
    
    def _get_parser(self, html: str):
        """Get BeautifulSoup parser."""
        try:
            from bs4 import BeautifulSoup
            return BeautifulSoup(html, "html.parser")
        except ImportError:
            raise DatasetError(
                "beautifulsoup4 is required. Install with: pip install beautifulsoup4"
            )
    
    def _fetch_html(self, url: str) -> tuple[str, int, float]:
        """Fetch HTML from URL."""
        if self.render_js:
            return self._fetch_with_playwright(url)
        else:
            return self._fetch_with_httpx(url)
    
    def _fetch_with_httpx(self, url: str) -> tuple[str, int, float]:
        """Fetch without JS rendering."""
        try:
            import httpx
        except ImportError:
            raise DatasetError("httpx is required. Install with: pip install httpx")
        
        timeout = self._load_args.get("timeout", 30)
        user_agent = self._load_args.get("user_agent", "AgenticBot/1.0")
        
        headers = {"User-Agent": user_agent}
        
        start = time.time()
        with httpx.Client(timeout=timeout, follow_redirects=True) as client:
            response = client.get(url, headers=headers)
            response.raise_for_status()
        elapsed = (time.time() - start) * 1000
        
        return response.text, response.status_code, elapsed
    
    def _fetch_with_playwright(self, url: str) -> tuple[str, int, float]:
        """Fetch with JS rendering using Playwright."""
        try:
            from playwright.sync_api import sync_playwright
        except ImportError:
            raise DatasetError(
                "playwright is required for JS rendering. "
                "Install with: pip install playwright && playwright install"
            )
        
        timeout = self._load_args.get("timeout", 30) * 1000
        
        start = time.time()
        with sync_playwright() as p:
            browser = p.chromium.launch(headless=True)
            page = browser.new_page()
            
            try:
                response = page.goto(url, wait_until="networkidle", timeout=timeout)
```
