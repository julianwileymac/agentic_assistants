# Chunk: 33161323811e_8

- source: `.venv-lab/Lib/site-packages/pygments/lexers/html.py`
- lines: 654-671
- chunk: 9/9

```
s*)', bygroups(Name.Attribute, Operator, Text),
             'attr'),
            (r'[\w:-]+', Name.Attribute),
            (r'(/?)(\s*)(>)', bygroups(Punctuation, Text, Punctuation), '#pop'),
        ],
        'name': [
            (r'[\w-]+', Name.Attribute),
            (r'[:@.]', Punctuation),
            (r'(\[)([^\]]*?)(\])', bygroups(Comment.Preproc,
             using(JavascriptLexer), Comment.Preproc)),
        ],
        'attr-directive': [
            (r'(["\'])(.*?)(\1)', bygroups(String,
             using(JavascriptLexer), String), '#pop'),
            (r'[^\s>]+', using(JavascriptLexer), '#pop'),
        ],
    }
```
