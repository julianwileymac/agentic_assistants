# Chunk: 4e38ddc67619_3

- source: `webui/src/lib/types.ts`
- lines: 336-471
- chunk: 4/6

```
;
  created_at: string;
  updated_at: string;
}

export type IndexingStatus = 'idle' | 'indexing' | 'completed' | 'failed';

export interface IndexingResult {
  status: string;
  project_id: string;
  version?: string;
  files_processed?: number;
  files_skipped?: number;
  chunks_indexed?: number;
  duration_seconds?: number;
  errors?: string[];
  error?: string;
}

// ============================================================================
// Git Configuration Types
// ============================================================================

export interface GitConfig {
  project_id: string;
  remote_url: string | null;
  branch: string;
  auto_sync: boolean;
  ssh_key_ref: string | null;
}

// ============================================================================
// Pipeline Types (Kedro-inspired)
// ============================================================================

export interface PipelineNode {
  name: string;
  inputs: string[];
  outputs: string[];
  tags: string[];
}

export interface PipelineInfo {
  name: string;
  node_count: number;
  inputs: string[];
  outputs: string[];
  nodes: PipelineNode[];
}

export interface PipelineRunRequest {
  runner: 'sequential' | 'parallel' | 'thread';
  from_nodes?: string[];
  to_nodes?: string[];
  tags?: string[];
  async_run: boolean;
}

export interface PipelineRunResponse {
  run_id: string;
  pipeline_name: string;
  success: boolean;
  duration_seconds: number;
  node_count: number;
  nodes_executed: number;
  errors: string[];
  status: PipelineRunStatus;
}

export type PipelineRunStatus = 'pending' | 'running' | 'completed' | 'failed';

export interface PipelineRunStatusInfo {
  run_id: string;
  pipeline_name: string;
  status: PipelineRunStatus;
  progress: number;
  nodes_completed: number;
  nodes_total: number;
  errors: string[];
  started_at: string | null;
  completed_at: string | null;
}

export interface PipelineVisualization {
  nodes: PipelineVisNode[];
  edges: PipelineVisEdge[];
}

export interface PipelineVisNode {
  id: string;
  type: 'input' | 'process' | 'output';
  label: string;
  tags?: string[];
  position: { x: number; y: number };
}

export interface PipelineVisEdge {
  id: string;
  source: string;
  target: string;
}

// ============================================================================
// Data Layer Types
// ============================================================================

export type DataLayerName = 
  | '01_raw'
  | '02_intermediate'
  | '03_primary'
  | '04_feature'
  | '05_model_input'
  | '06_model_output'
  | '07_reporting'
  | '08_external';

export interface DataLayerInfo {
  name: DataLayerName;
  description: string;
  path: string;
  dataset_count: number;
  total_size_bytes: number;
  exists: boolean;
}

export interface DatasetInfo {
  name: string;
  type: string;
  filepath: string;
  layer?: DataLayerName;
  versioned: boolean;
  versions?: string[];
  schema?: DatasetSchema;
}
```
