# Chunk: d73284592ccb_116

- source: `webui/.next/dev/server/chunks/ssr/[root-of-the-server]__630d161a._.js`
- lines: 3397-3477
- chunk: 117/138

```
              activeModal: null
            }),
        setSearchQuery: (query)=>set({
                searchQuery: query
            })
    }), {
    name: 'ui-storage',
    partialize: (state)=>({
            sidebarCollapsed: state.sidebarCollapsed,
            theme: state.theme
        })
}));
const useSelectionStore = (0, __TURBOPACK__imported__module__$5b$project$5d2f$Documents$2f$GitHub$2f$agentic_assistants$2f$webui$2f$node_modules$2f$zustand$2f$esm$2f$react$2e$mjs__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["create"])((set)=>({
        selectedProjects: new Set(),
        selectedAgents: new Set(),
        selectedFlows: new Set(),
        selectedComponents: new Set(),
        toggleProject: (id)=>set((state)=>{
                const newSet = new Set(state.selectedProjects);
                if (newSet.has(id)) {
                    newSet.delete(id);
                } else {
                    newSet.add(id);
                }
                return {
                    selectedProjects: newSet
                };
            }),
        toggleAgent: (id)=>set((state)=>{
                const newSet = new Set(state.selectedAgents);
                if (newSet.has(id)) {
                    newSet.delete(id);
                } else {
                    newSet.add(id);
                }
                return {
                    selectedAgents: newSet
                };
            }),
        toggleFlow: (id)=>set((state)=>{
                const newSet = new Set(state.selectedFlows);
                if (newSet.has(id)) {
                    newSet.delete(id);
                } else {
                    newSet.add(id);
                }
                return {
                    selectedFlows: newSet
                };
            }),
        toggleComponent: (id)=>set((state)=>{
                const newSet = new Set(state.selectedComponents);
                if (newSet.has(id)) {
                    newSet.delete(id);
                } else {
                    newSet.add(id);
                }
                return {
                    selectedComponents: newSet
                };
            }),
        selectAllProjects: (ids)=>set({
                selectedProjects: new Set(ids)
            }),
        selectAllAgents: (ids)=>set({
                selectedAgents: new Set(ids)
            }),
        selectAllFlows: (ids)=>set({
                selectedFlows: new Set(ids)
            }),
        selectAllComponents: (ids)=>set({
                selectedComponents: new Set(ids)
            }),
        clearSelection: ()=>set({
                selectedProjects: new Set(),
                selectedAgents: new Set(),
                selectedFlows: new Set(),
                selectedComponents: new Set()
            })
    }));
```
