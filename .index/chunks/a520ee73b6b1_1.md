# Chunk: a520ee73b6b1_1

- source: `src/agentic_assistants/data/templates/builtin/web_templates.py`
- lines: 92-186
- chunk: 2/5

```
   "best": "https://hnrss.org/best",
            "ask": "https://hnrss.org/ask",
            "show": "https://hnrss.org/show",
            "jobs": "https://hnrss.org/jobs",
        }
        
        url = feed_urls.get(feed_type, feed_urls["frontpage"])
        
        return RSSDataset(
            url=url,
            max_entries=params.get("max_entries", 30),
        )


class ArxivRSSTemplate(DataSourceTemplate):
    """Template for arXiv paper feeds."""
    
    id = "arxiv-rss"
    name = "arXiv RSS"
    description = "Subscribe to arXiv paper feeds by category"
    category = TemplateCategory.FEED
    dataset_type = "rss"
    documentation_url = "https://arxiv.org/help/rss"
    tags = ["arxiv", "papers", "research", "ml", "ai"]
    
    parameters = [
        TemplateParameter(
            name="category",
            description="arXiv category",
            param_type="string",
            required=True,
            example="cs.LG",
        ),
        TemplateParameter(
            name="max_entries",
            description="Maximum entries to fetch",
            param_type="integer",
            required=False,
            default=50,
        ),
    ]
    
    examples = [
        {"description": "Machine Learning papers", "params": {"category": "cs.LG"}},
        {"description": "AI papers", "params": {"category": "cs.AI"}},
        {"description": "NLP papers", "params": {"category": "cs.CL"}},
    ]
    
    def create_dataset(self, **params) -> RSSDataset:
        category = params["category"]
        url = f"https://export.arxiv.org/rss/{category}"
        
        return RSSDataset(
            url=url,
            max_entries=params.get("max_entries", 50),
        )


class GenericWebsiteTemplate(DataSourceTemplate):
    """Template for scraping generic websites."""
    
    id = "website-scraper"
    name = "Website Scraper"
    description = "Scrape content from any website using CSS selectors"
    category = TemplateCategory.WEB_SCRAPING
    dataset_type = "website"
    tags = ["scraping", "website", "html", "css"]
    
    parameters = [
        TemplateParameter(
            name="url",
            description="Website URL",
            param_type="string",
            required=True,
            pattern=r"^https?://",
        ),
        TemplateParameter(
            name="title_selector",
            description="CSS selector for title",
            param_type="string",
            required=False,
            default="h1",
        ),
        TemplateParameter(
            name="content_selector",
            description="CSS selector for main content",
            param_type="string",
            required=False,
            default="article, main, .content",
        ),
        TemplateParameter(
            name="render_js",
            description="Render JavaScript (requires playwright)",
            param_type="boolean",
```
