# Chunk: ede33c1e9246_3

- source: `src/agentic_assistants/data/datasets/url_dataset.py`
- lines: 226-307
- chunk: 4/8

```
.endswith(".csv"):
            return "csv"
        elif path.endswith(".html") or path.endswith(".htm"):
            return "html"
        elif path.endswith(".xml"):
            return "xml"
        elif path.endswith(".txt"):
            return "text"
        
        # Default to text
        return "text"
    
    def _parse_response(self, content: bytes, content_type: str, encoding: str = "utf-8") -> Any:
        """Parse response content based on content type."""
        if content_type == "json":
            import json
            return json.loads(content.decode(encoding))
        
        elif content_type == "csv":
            try:
                import pandas as pd
                from io import StringIO
                
                csv_args = {
                    k: v for k, v in self._load_args.items()
                    if k in ["delimiter", "sep", "header", "names", "index_col", 
                             "usecols", "dtype", "parse_dates", "encoding"]
                }
                return pd.read_csv(StringIO(content.decode(encoding)), **csv_args)
            except ImportError:
                # Return as list of dicts if pandas not available
                import csv
                from io import StringIO
                reader = csv.DictReader(StringIO(content.decode(encoding)))
                return list(reader)
        
        elif content_type == "html":
            return content.decode(encoding)
        
        elif content_type == "xml":
            try:
                import xml.etree.ElementTree as ET
                return ET.fromstring(content)
            except Exception:
                return content.decode(encoding)
        
        elif content_type == "text":
            return content.decode(encoding)
        
        elif content_type == "binary":
            return content
        
        else:
            return content.decode(encoding)
    
    def _get_cache_path(self) -> Path:
        """Get the cache file path for this URL."""
        url_hash = hashlib.sha256(self.url.encode()).hexdigest()[:16]
        return self.cache_dir / f"{url_hash}.cache"
    
    def _load_from_cache(self) -> Optional[URLFetchResult]:
        """Load cached response if available and valid."""
        if not self.cache_responses:
            return None
        
        cache_path = self._get_cache_path()
        if not cache_path.exists():
            return None
        
        try:
            import json
            with open(cache_path, "r") as f:
                cached = json.load(f)
            
            # Check cache expiry
            cache_ttl = self._load_args.get("cache_ttl_seconds", 3600)
            cached_at = datetime.fromisoformat(cached["fetched_at"])
            if (datetime.utcnow() - cached_at).total_seconds() > cache_ttl:
                return None
            
            return URLFetchResult(
```
