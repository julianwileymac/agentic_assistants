# Chunk: ce7643ecc378_3

- source: `.venv-lab/Lib/site-packages/pip/_internal/metadata/importlib/_dists.py`
- lines: 221-230
- chunk: 4/4

```
age.Message.get_all() may return a leading \n
            # in case a long header was wrapped.
            req = get_requirement(req_string.strip())
            if not req.marker:
                yield req
            elif not extras and req.marker.evaluate({"extra": ""}):
                yield req
            elif any(req.marker.evaluate(context) for context in contexts):
                yield req
```
