# Chunk: ede33c1e9246_7

- source: `src/agentic_assistants/data/datasets/url_dataset.py`
- lines: 501-530
- chunk: 8/8

```
._last_modified:
                headers["If-Modified-Since"] = self._last_modified
            
            with httpx.Client(timeout=10) as client:
                response = client.head(self.url, headers=headers)
                return response.status_code != 304
        except Exception:
            return True
    
    def clear_cache(self) -> bool:
        """Clear cached response for this URL."""
        cache_path = self._get_cache_path()
        if cache_path.exists():
            cache_path.unlink()
            return True
        return False
    
    def _describe(self) -> Dict[str, Any]:
        """Describe the dataset configuration."""
        return {
            "url": self.url,
            "method": self.method,
            "content_type": self.content_type,
            "cache_responses": self.cache_responses,
            "headers": {k: "***" if "auth" in k.lower() else v for k, v in self.headers.items()},
        }
    
    def __repr__(self) -> str:
        return f"URLDataset(url={self.url!r}, content_type={self.content_type!r})"
```
