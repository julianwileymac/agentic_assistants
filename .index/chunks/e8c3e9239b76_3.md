# Chunk: e8c3e9239b76_3

- source: `src/agentic_assistants/data/rag/ingestion_pipeline.py`
- lines: 234-317
- chunk: 4/8

```
fig.vectordb.db_path),
                    embedding_model=config.vectordb.embedding_model,
                )
            except Exception as e:
                logger.warning(f"Failed to initialize vector store: {e}")
        
        return self._vector_store
    
    def _fetch_content(
        self,
        source: str,
        source_type: str,
        **kwargs,
    ) -> str:
        """Fetch content from a source."""
        if source_type == "url":
            from agentic_assistants.data.datasets import URLDataset
            dataset = URLDataset(url=source, content_type="text", **kwargs)
            return str(dataset.load())
        
        elif source_type == "website":
            from agentic_assistants.data.datasets import WebsiteDataset
            dataset = WebsiteDataset(url=source, **kwargs)
            result = dataset.load()
            # Combine scraped content
            if isinstance(result, dict):
                return " ".join(str(v) for v in result.values() if v)
            return str(result)
        
        elif source_type == "rss":
            from agentic_assistants.data.datasets import RSSDataset
            dataset = RSSDataset(url=source, **kwargs)
            result = dataset.load()
            # Combine entry content
            entries = result.get("entries", [])
            return "\n\n".join(
                f"{e.get('title', '')}\n{e.get('content', e.get('summary', ''))}"
                for e in entries
            )
        
        elif source_type == "file":
            path = Path(source)
            if path.exists():
                return path.read_text()
            raise FileNotFoundError(f"File not found: {source}")
        
        elif source_type == "text":
            return source
        
        else:
            raise ValueError(f"Unsupported source type: {source_type}")
    
    def _filter_chunks(self, chunks: List[Chunk]) -> List[Chunk]:
        """Filter chunks based on quality and deduplication."""
        filtered = []
        
        for chunk in chunks:
            # Quality filter
            quality_score = chunk.metadata.get("quality_score", 1.0)
            if quality_score < self.config.min_quality_score:
                continue
            
            # Deduplication
            if self.config.deduplicate:
                content_hash = chunk.metadata.get("content_hash")
                if content_hash:
                    if content_hash in self._seen_hashes:
                        continue
                    self._seen_hashes.add(content_hash)
            
            filtered.append(chunk)
        
        return filtered
    
    def _save_to_files(
        self,
        chunks: List[Chunk],
        source: str,
        collection: str,
    ) -> str:
        """Save chunks to file artifacts."""
        import json
        
```
