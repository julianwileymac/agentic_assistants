# Chunk: 1c6e9e7c32c4_0

- source: `src/agentic_assistants/data/datasets/scraper_dataset.py`
- lines: 1-107
- chunk: 1/9

```
"""
Scraper Dataset with pluggable extractors.

This module provides a flexible scraping framework with support for
custom extractors, multiple page handling, and structured output.

Example:
    >>> from agentic_assistants.data.datasets import ScraperDataset, Extractor
    >>> 
    >>> # Define custom extractor
    >>> class ProductExtractor(Extractor):
    ...     def extract(self, soup, url):
    ...         return {
    ...             "name": soup.select_one("h1.product-name").text,
    ...             "price": soup.select_one("span.price").text,
    ...         }
    >>> 
    >>> dataset = ScraperDataset(
    ...     urls=["https://shop.example.com/product/1"],
    ...     extractor=ProductExtractor()
    ... )
    >>> products = dataset.load()
"""

import hashlib
import time
from abc import ABC, abstractmethod
from dataclasses import dataclass, field
from datetime import datetime
from pathlib import Path
from typing import Any, Callable, Dict, Iterator, List, Optional, Type, Union
from urllib.parse import urljoin, urlparse

from agentic_assistants.data.datasets.base import (
    AbstractDataset,
    DatasetError,
    DatasetMetadata,
)
from agentic_assistants.utils.logging import get_logger

logger = get_logger(__name__)


class Extractor(ABC):
    """
    Abstract base class for content extractors.
    
    Extractors define how to extract structured data from HTML pages.
    """
    
    @abstractmethod
    def extract(self, soup, url: str) -> Dict[str, Any]:
        """
        Extract data from a parsed HTML page.
        
        Args:
            soup: BeautifulSoup object
            url: Source URL
            
        Returns:
            Dictionary of extracted data
        """
        pass
    
    def should_follow(self, url: str, soup) -> bool:
        """
        Determine if a link should be followed for multi-page scraping.
        
        Args:
            url: URL to potentially follow
            soup: BeautifulSoup object of current page
            
        Returns:
            True if the URL should be followed
        """
        return False
    
    def get_next_page(self, soup, current_url: str) -> Optional[str]:
        """
        Get the URL of the next page for pagination.
        
        Args:
            soup: BeautifulSoup object
            current_url: Current page URL
            
        Returns:
            Next page URL or None
        """
        return None


class CSSExtractor(Extractor):
    """
    Extractor using CSS selectors.
    
    Example:
        >>> extractor = CSSExtractor({
        ...     "title": "h1.title",
        ...     "prices": {"selector": "span.price", "multiple": True},
        ...     "link": {"selector": "a.detail", "attribute": "href"},
        ... })
    """
    
    def __init__(
        self,
        selectors: Dict[str, Union[str, Dict[str, Any]]],
```
