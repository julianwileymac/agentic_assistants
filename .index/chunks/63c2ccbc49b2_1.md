# Chunk: 63c2ccbc49b2_1

- source: `.venv-lab/Lib/site-packages/nbconvert/exporters/qt_screenshot.py`
- lines: 76-93
- chunk: 2/2

```
       )
                page_layout = QPageLayout(page_size, QPageLayout.Portrait, QtCore.QMarginsF())

            self.page().printToPdf(self.output_file, pageLayout=page_layout)

        def export_png(self):
            """Export to png."""
            self.grab().save(self.output_file, "PNG")
            self.app.quit()  # type:ignore[union-attr]
            self.get_data()

        def get_data(self):
            """Get output data."""
            if os.path.exists(self.output_file):
                with open(self.output_file, "rb") as f:
                    self.data = f.read()
                os.unlink(self.output_file)
```
