# Chunk: d9e2ae92514d_0

- source: `.venv-lab/Lib/site-packages/setuptools/tests/integration/test_pbr.py`
- lines: 1-21
- chunk: 1/1

```
import subprocess

import pytest


@pytest.mark.uses_network
def test_pbr_integration(pbr_package, venv):
    """Ensure pbr packages install."""
    cmd = [
        'python',
        '-m',
        'pip',
        '-v',
        'install',
        '--no-build-isolation',
        pbr_package,
    ]
    venv.run(cmd, stderr=subprocess.STDOUT)
    out = venv.run(["python", "-c", "import mypkg.hello"])
    assert "Hello world!" in out
```
