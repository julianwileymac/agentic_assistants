# Chunk: 456d9c708749_0

- source: `src/agentic_assistants/data/datasets/kaggle_dataset.py`
- lines: 1-99
- chunk: 1/6

```
"""
Kaggle Dataset for downloading datasets from Kaggle.

This module provides a dataset implementation for fetching datasets
from Kaggle using the Kaggle API.

Example:
    >>> from agentic_assistants.data.datasets import KaggleDataset
    >>> 
    >>> # Download a dataset
    >>> dataset = KaggleDataset(
    ...     dataset_ref="username/dataset-name",
    ...     file_name="data.csv"
    ... )
    >>> df = dataset.load()
    >>> 
    >>> # Download competition data
    >>> dataset = KaggleDataset(
    ...     competition="titanic",
    ...     file_name="train.csv"
    ... )
"""

import os
import zipfile
from datetime import datetime
from pathlib import Path
from typing import Any, Dict, List, Optional, Union

from agentic_assistants.data.datasets.base import (
    AbstractDataset,
    DatasetError,
    DatasetMetadata,
)
from agentic_assistants.utils.logging import get_logger

logger = get_logger(__name__)


class KaggleDataset(AbstractDataset[Any]):
    """
    Dataset for downloading data from Kaggle.
    
    This dataset supports:
    - Dataset downloads (username/dataset-name)
    - Competition data downloads
    - Automatic file extraction
    - Multiple file formats (CSV, JSON, Parquet)
    - Caching of downloaded files
    
    Requires Kaggle API credentials in ~/.kaggle/kaggle.json or environment variables:
    - KAGGLE_USERNAME
    - KAGGLE_KEY
    
    Attributes:
        dataset_ref: Kaggle dataset reference (username/dataset-name)
        competition: Competition name (alternative to dataset_ref)
        file_name: Specific file to load from the dataset
        download_dir: Directory to store downloaded files
        
    Example:
        >>> # Load a specific file from a dataset
        >>> dataset = KaggleDataset(
        ...     dataset_ref="zillow/zecon",
        ...     file_name="Zip_zhvi_uc_sfrcondo_tier_0.33_0.67_sm_sa_month.csv"
        ... )
        >>> df = dataset.load()
        
        >>> # Load competition data
        >>> dataset = KaggleDataset(
        ...     competition="house-prices-advanced-regression-techniques",
        ...     file_name="train.csv"
        ... )
        >>> df = dataset.load()
    """
    
    DEFAULT_LOAD_ARGS: Dict[str, Any] = {
        "force_download": False,
        "unzip": True,
    }
    
    def __init__(
        self,
        dataset_ref: Optional[str] = None,
        competition: Optional[str] = None,
        file_name: Optional[str] = None,
        download_dir: Optional[str] = None,
        load_args: Optional[Dict[str, Any]] = None,
        save_args: Optional[Dict[str, Any]] = None,
        credentials: Optional[Dict[str, Any]] = None,
        metadata: Optional[DatasetMetadata] = None,
        **kwargs,
    ):
        """
        Initialize the Kaggle dataset.
        
        Args:
            dataset_ref: Kaggle dataset reference (username/dataset-name)
```
