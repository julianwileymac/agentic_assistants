# Chunk: 8d5067b3fd33_1

- source: `src/agentic_assistants/data/rag/annotators.py`
- lines: 93-185
- chunk: 2/7

```
urn matches if matches else ["uncategorized"]
    
    def _llm_classify(self, text: str) -> List[str]:
        """Classify using LLM."""
        if not self.llm_fn:
            raise ValueError("LLM function required")
        
        categories_list = ", ".join(self.categories)
        
        if self.multi_label:
            prompt = f"""Classify the following text into one or more of these categories: {categories_list}

Return only the category names, comma-separated.

Text:
{text[:2000]}

Categories:"""
        else:
            prompt = f"""Classify the following text into exactly one of these categories: {categories_list}

Return only the category name, nothing else.

Text:
{text[:2000]}

Category:"""
        
        response = self.llm_fn(prompt).strip()
        
        classifications = [c.strip() for c in response.split(",")]
        # Filter to valid categories
        classifications = [c for c in classifications if c in self.categories]
        
        return classifications if classifications else ["uncategorized"]
    
    def annotate(self, chunk: Chunk, context: Dict[str, Any]) -> Chunk:
        if self.use_llm and self.llm_fn:
            categories = self._llm_classify(chunk.content)
        else:
            categories = self._keyword_classify(chunk.content)
        
        chunk.metadata["categories"] = categories
        return chunk


class SentimentAnnotator(Annotator):
    """
    Analyze sentiment of chunk content.
    
    Can use simple lexicon-based analysis or LLM.
    """
    
    name = "sentiment"
    
    # Simple sentiment lexicons
    POSITIVE_WORDS = {
        "good", "great", "excellent", "amazing", "wonderful", "fantastic",
        "awesome", "outstanding", "brilliant", "superb", "love", "best",
        "perfect", "beautiful", "happy", "positive", "success", "win",
        "improve", "growth", "benefit", "advantage", "opportunity",
    }
    
    NEGATIVE_WORDS = {
        "bad", "terrible", "awful", "horrible", "poor", "worst",
        "hate", "fail", "failure", "problem", "issue", "error",
        "wrong", "negative", "loss", "damage", "risk", "threat",
        "difficult", "hard", "disappointing", "frustrating",
    }
    
    def __init__(
        self,
        use_llm: bool = False,
        llm_fn: Optional[Callable] = None,
        return_score: bool = True,
    ):
        """
        Initialize sentiment annotator.
        
        Args:
            use_llm: Use LLM for sentiment analysis
            llm_fn: Custom LLM function
            return_score: Return numeric score (-1 to 1)
        """
        self.use_llm = use_llm
        self.llm_fn = llm_fn
        self.return_score = return_score
    
    def _lexicon_sentiment(self, text: str) -> Tuple[str, float]:
        """Calculate sentiment using lexicon."""
        words = set(re.findall(r'\b\w+\b', text.lower()))
        
```
