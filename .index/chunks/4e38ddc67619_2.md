# Chunk: 4e38ddc67619_2

- source: `webui/src/lib/types.ts`
- lines: 231-350
- chunk: 3/6

```
boolean;
  created_at: string;
  updated_at: string;
  tags: string[];
  metadata: Record<string, unknown>;
}

export type DataSourceType = 'database' | 'file_store' | 'api';
export type DataSourceStatus = 'active' | 'inactive' | 'error';

export interface ConnectionTestResult {
  success: boolean;
  message: string;
  latency_ms?: number;
  details: Record<string, unknown>;
}

export interface SchemaInfo {
  source_id: string;
  source_type: string;
  tables: TableSchema[];
  discovered_at: string;
  metadata: Record<string, unknown>;
}

export interface TableSchema {
  name: string;
  columns: ColumnSchema[];
  row_count?: number;
  size_bytes?: number;
  metadata: Record<string, unknown>;
}

export interface ColumnSchema {
  name: string;
  data_type: string;
  nullable: boolean;
  primary_key: boolean;
  description?: string;
  metadata: Record<string, unknown>;
}

// ============================================================================
// Service Resource Types
// ============================================================================

export interface ServiceResource {
  id: string;
  name: string;
  service_type: ServiceType;
  endpoint_url: string;
  description: string;
  health_endpoint: string | null;
  auth_type: AuthType | null;
  credentials_ref: string | null;
  config_yaml: string;
  is_global: boolean;
  project_id: string | null;
  status: ServiceResourceStatus;
  last_health_check: string | null;
  created_at: string;
  updated_at: string;
  tags: string[];
  metadata: Record<string, unknown>;
}

export type ServiceType = 
  | 'web_ui' 
  | 'api_endpoint' 
  | 'file_store' 
  | 'background_service' 
  | 'database' 
  | 'ml_deployment';

export type AuthType = 'none' | 'api_key' | 'oauth' | 'basic';
export type ServiceResourceStatus = 'healthy' | 'unhealthy' | 'unknown';

// ============================================================================
// Project Resource Types
// ============================================================================

export interface ProjectResource {
  id: string;
  project_id: string;
  resource_type: 'datasource' | 'service';
  resource_id: string;
  alias: string | null;
  config_overrides: string;
  created_at: string;
}

// ============================================================================
// Indexing Types
// ============================================================================

export interface IndexingState {
  id: string;
  project_id: string;
  collection_name: string;
  version: string;
  status: IndexingStatus;
  file_count: number;
  chunk_count: number;
  last_indexed: string | null;
  error_message: string | null;
  indexing_config: string;
  created_at: string;
  updated_at: string;
}

export type IndexingStatus = 'idle' | 'indexing' | 'completed' | 'failed';

export interface IndexingResult {
  status: string;
  project_id: string;
  version?: string;
  files_processed?: number;
  files_skipped?: number;
  chunks_indexed?: number;
```
