# Chunk: 9889b2416690_1

- source: `frontend/packages/agentic-data-viewer/lib/browser/data-service.js`
- lines: 67-164
- chunk: 2/3

```
ndService.get('/data/file', { path });
        return response.data;
    }
    /**
     * Get table schema
     */
    async getSchema(path) {
        const response = await this.backendService.get('/data/schema', { path });
        return response.data;
    }
    /**
     * Preview a table
     */
    async previewTable(path, offset = 0, limit = 100) {
        const response = await this.backendService.get('/data/preview', {
            path,
            offset: String(offset),
            limit: String(limit)
        });
        return response.data;
    }
    /**
     * Read file content
     */
    async readFile(path, format) {
        const params = { path };
        if (format)
            params.format = format;
        const response = await this.backendService.get('/data/read', params);
        return response.data;
    }
    /**
     * Query a table
     */
    async queryTable(path, options) {
        const params = { path };
        if (options === null || options === void 0 ? void 0 : options.columns)
            params.columns = options.columns.join(',');
        if (options === null || options === void 0 ? void 0 : options.filterExpr)
            params.filter_expr = options.filterExpr;
        if (options === null || options === void 0 ? void 0 : options.sortBy)
            params.sort_by = options.sortBy;
        if ((options === null || options === void 0 ? void 0 : options.ascending) !== undefined)
            params.ascending = String(options.ascending);
        if (options === null || options === void 0 ? void 0 : options.limit)
            params.limit = String(options.limit);
        const response = await this.backendService.get('/data/query', params);
        return response.data;
    }
    /**
     * Get cache statistics
     */
    async getCacheStats() {
        const response = await this.backendService.get('/data/cache/stats');
        return response.data;
    }
    /**
     * Clear the data cache
     */
    async clearCache() {
        const response = await this.backendService.post('/data/cache/clear');
        return response.status === 200;
    }
    /**
     * Convert a file to another format
     */
    async convertFile(sourcePath, targetPath, targetFormat) {
        const params = {
            source_path: sourcePath,
            target_path: targetPath
        };
        if (targetFormat)
            params.target_format = targetFormat;
        const response = await this.backendService.post('/data/convert', params);
        return response.status === 200;
    }
    /**
     * Get supported formats
     */
    async getSupportedFormats() {
        const response = await this.backendService.get('/data/supported-formats');
        return response.data;
    }
    /**
     * Get current path
     */
    getCurrentPath() {
        return this.currentPath;
    }
    /**
     * Get cached files
     */
    getFiles() {
        return this.files;
    }
    /**
     * Check if a file is tabular (parquet, csv)
```
