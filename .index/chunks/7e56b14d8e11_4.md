# Chunk: 7e56b14d8e11_4

- source: `src/agentic_assistants/data/datasets/website_dataset.py`
- lines: 306-387
- chunk: 5/7

```
f)
            
            # Filter by pattern if specified
            if self.follow_links:
                if not re.search(self.follow_links, absolute_url):
                    continue
            
            links.append(absolute_url)
        
        return list(set(links))
    
    def _fetch_html(self, url: str) -> tuple[str, int, float]:
        """Fetch HTML from URL."""
        if self.render_js:
            return self._fetch_with_playwright(url)
        else:
            return self._fetch_with_httpx(url)
    
    def _fetch_with_httpx(self, url: str) -> tuple[str, int, float]:
        """Fetch HTML using httpx (no JS rendering)."""
        try:
            import httpx
        except ImportError:
            raise DatasetError("httpx is required. Install with: pip install httpx")
        
        timeout = self._load_args.get("timeout", 30)
        verify_ssl = self._load_args.get("verify_ssl", True)
        user_agent = self._load_args.get("user_agent", "AgenticBot/1.0")
        
        headers = {"User-Agent": user_agent}
        
        start_time = time.time()
        
        with httpx.Client(timeout=timeout, verify=verify_ssl) as client:
            response = client.get(url, headers=headers, follow_redirects=True)
            response.raise_for_status()
        
        elapsed_ms = (time.time() - start_time) * 1000
        
        return response.text, response.status_code, elapsed_ms
    
    def _fetch_with_playwright(self, url: str) -> tuple[str, int, float]:
        """Fetch HTML using playwright (with JS rendering)."""
        try:
            from playwright.sync_api import sync_playwright
        except ImportError:
            raise DatasetError(
                "playwright is required for JS rendering. "
                "Install with: pip install playwright && playwright install"
            )
        
        timeout = self._load_args.get("timeout", 30) * 1000  # ms
        
        start_time = time.time()
        
        with sync_playwright() as p:
            browser = p.chromium.launch(headless=True)
            page = browser.new_page()
            
            try:
                response = page.goto(url, wait_until="networkidle", timeout=timeout)
                status_code = response.status if response else 200
                html = page.content()
            finally:
                browser.close()
        
        elapsed_ms = (time.time() - start_time) * 1000
        
        return html, status_code, elapsed_ms
    
    def _scrape_page(self, url: str) -> ScrapedPage:
        """Scrape a single page."""
        # Fetch HTML
        html, status_code, elapsed_ms = self._fetch_html(url)
        
        # Parse HTML
        soup = self._get_parser(html)
        
        # Get base element if specified
        base_element = None
        if self.base_selector:
```
