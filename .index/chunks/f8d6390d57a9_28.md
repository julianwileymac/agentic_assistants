# Chunk: f8d6390d57a9_28

- source: `webui/.next/dev/server/chunks/ssr/[root-of-the-server]__630d161a._.js.map`
- lines: 17-17
- chunk: 29/62

```
=========================\n// Project Git Configuration API\n// ============================================================================\n\nexport function useProjectGitConfig(projectId: string | null) {\n  return useSWR<GitConfig>(\n    projectId ? `/api/v1/projects/${projectId}/git` : null,\n    fetcher,\n    defaultSwrConfig\n  );\n}\n\nexport function useUpdateProjectGitConfig(projectId: string) {\n  return useSWRMutation(\n    `/api/v1/projects/${projectId}/git`,\n    async (url: string, { arg }: { arg: Partial<GitConfig> }) => {\n      return apiFetch<GitConfig>(url, {\n        method: 'PUT',\n        body: JSON.stringify(arg),\n      });\n    }\n  );\n}\n\nexport function useSyncProjectGit(projectId: string) {\n  return useSWRMutation(\n    `/api/v1/projects/${projectId}/git/sync`,\n    async (url: string) => {\n      return apiFetch<{ status: string; project_id: string; remote_url: string; branch: string }>(url, { method: 'POST' });\n    }\n  );\n}\n\n// ============================================================================\n// Project Indexing API\n// ============================================================================\n\nexport function useProjectIndexingState(projectId: string | null) {\n  return useSWR<IndexingState>(\n    projectId ? `/api/v1/projects/${projectId}/index` : null,\n    fetcher,\n    defaultSwrConfig\n  );\n}\n\nexport function useProjectIndexingStatus(projectId: string | null) {\n  return useSWR<{\n    project_id: string;\n    status: string;\n    version: string;\n    file_count: number;\n    chunk_count: number;\n    last_indexed: string | null;\n    needs_reindex: boolean;\n  }>(\n    projectId ? `/api/v1/projects/${projectId}/index/status` : null,\n    fetcher,\n    defaultSwrConfig\n  );\n}\n\nexport function useTriggerProjectIndexing(projectId: string) {\n  return useSWRMutation(\n    `/api/v1/projects/${projectId}/index`,\n    async (url: string, { arg }: { arg?: { force?: boolean; path?: string } }) => {\n      return apiFetch<IndexingResult>(url, {\n        method: 'POST',\n        body: JSON.stringify(arg || {}),\n      });\n    }\n  );\n}\n\n// ============================================================================\n// Project Resources API\n// ============================================================================\n\nexport function useProjectResources(projectId: string | null, resourceType?: string) {\n  const query = resourceType ? `?resource_type=${resourceType}` : '';\n  \n  return useSWR<{ project_id: string; resources: { id: string; resource_type: string; resource_id: string; alias: string | null; created_at: string }[]; count: number }>(\n    projectId ? `/api/v1/projects/${projectId}/resources${query}` : null,\n    fetcher,\n    defaultSwrConfig\n  );\n}\n\nexport function useLinkProjectResource(projectId: string) {\n  return useSWRMutation(\n    `/api/v1/projects/${projectId}/resources/link`,\n    async (url: string, { arg }: { arg: { resource_type: string; resource_id: 
```
