# Chunk: 644fe8d87060_2

- source: `src/agentic_assistants/data/templates/base.py`
- lines: 154-243
- chunk: 3/5

```
str] = field(default_factory=list)
    
    # State
    _parameter_map: Dict[str, TemplateParameter] = field(default_factory=dict, repr=False)
    
    def __post_init__(self):
        """Build parameter lookup map."""
        self._parameter_map = {p.name: p for p in self.parameters}
    
    def get_parameter(self, name: str) -> Optional[TemplateParameter]:
        """Get a parameter definition by name."""
        return self._parameter_map.get(name)
    
    def validate_params(self, params: Dict[str, Any]) -> tuple[bool, List[str]]:
        """
        Validate all parameters.
        
        Returns:
            Tuple of (is_valid, list of error messages)
        """
        errors = []
        
        # Check for unknown parameters
        known_params = set(self._parameter_map.keys())
        provided_params = set(params.keys())
        unknown = provided_params - known_params
        if unknown:
            errors.append(f"Unknown parameters: {unknown}")
        
        # Validate each parameter
        for param in self.parameters:
            value = params.get(param.name, param.default)
            valid, error = param.validate(value)
            if not valid:
                errors.append(error)
        
        return len(errors) == 0, errors
    
    def get_effective_params(self, params: Dict[str, Any]) -> Dict[str, Any]:
        """Get parameters with defaults filled in."""
        effective = {}
        for param in self.parameters:
            if param.name in params:
                effective[param.name] = params[param.name]
            elif param.default is not None:
                effective[param.name] = param.default
        return effective
    
    @abstractmethod
    def create_dataset(self, **params) -> AbstractDataset:
        """
        Create a dataset instance from parameters.
        
        Args:
            **params: Parameter values
            
        Returns:
            Configured dataset instance
        """
        pass
    
    def create(self, **params) -> AbstractDataset:
        """
        Create a dataset with validation.
        
        This is the main entry point for creating datasets from templates.
        
        Args:
            **params: Parameter values
            
        Returns:
            Configured dataset instance
            
        Raises:
            ValueError: If parameters are invalid
        """
        valid, errors = self.validate_params(params)
        if not valid:
            raise ValueError(f"Invalid parameters: {errors}")
        
        effective_params = self.get_effective_params(params)
        
        logger.info(f"Creating dataset from template '{self.id}'")
        return self.create_dataset(**effective_params)
    
    def to_dict(self) -> Dict[str, Any]:
        """Convert template to dictionary for serialization."""
        return {
            "id": self.id,
```
