# Chunk: a520ee73b6b1_3

- source: `src/agentic_assistants/data/templates/builtin/web_templates.py`
- lines: 261-351
- chunk: 4/5

```

            required=True,
            pattern=r"^https?://",
        ),
        TemplateParameter(
            name="name_selector",
            description="CSS selector for product name",
            param_type="string",
            required=False,
            default="h1",
        ),
        TemplateParameter(
            name="price_selector",
            description="CSS selector for price",
            param_type="string",
            required=False,
            default=".price, [data-price], .product-price",
        ),
        TemplateParameter(
            name="description_selector",
            description="CSS selector for description",
            param_type="string",
            required=False,
            default=".description, .product-description",
        ),
        TemplateParameter(
            name="image_selector",
            description="CSS selector for product image",
            param_type="string",
            required=False,
            default="img.product-image, .gallery img",
        ),
    ]
    
    def create_dataset(self, **params) -> WebsiteDataset:
        selectors = {
            "name": params.get("name_selector", "h1"),
            "price": params.get("price_selector", ".price"),
            "description": params.get("description_selector", ".description"),
            "image": {
                "selector": params.get("image_selector", "img.product-image"),
                "attribute": "src",
            },
        }
        
        return WebsiteDataset(
            url=params["url"],
            selectors=selectors,
        )


class TableScraperTemplate(DataSourceTemplate):
    """Template for scraping HTML tables."""
    
    id = "table-scraper"
    name = "HTML Table Scraper"
    description = "Extract data from HTML tables"
    category = TemplateCategory.WEB_SCRAPING
    dataset_type = "scraper"
    tags = ["table", "html", "scraping", "data"]
    
    parameters = [
        TemplateParameter(
            name="url",
            description="Page URL containing table",
            param_type="string",
            required=True,
            pattern=r"^https?://",
        ),
        TemplateParameter(
            name="table_selector",
            description="CSS selector for the table",
            param_type="string",
            required=False,
            default="table",
        ),
        TemplateParameter(
            name="table_index",
            description="Which table to extract (0-based index)",
            param_type="integer",
            required=False,
            default=0,
        ),
    ]
    
    def create_dataset(self, **params) -> ScraperDataset:
        from agentic_assistants.data.datasets.scraper_dataset import TableExtractor
        
        extractor = TableExtractor(
            table_selector=params.get("table_selector", "table"),
```
