# Chunk: ac7954ec963d_0

- source: `notebooks/05_huggingface_basics.ipynb`
- lines: 1-87
- chunk: 1/2

```
{
  "cells": [
    {
      "cell_type": "markdown",
      "metadata": {},
      "source": [
        "# HuggingFace Basics\n",
        "\n",
        "This notebook demonstrates using HuggingFace Transformers and Hub with the Agentic framework.\n",
        "\n",
        "## Contents\n",
        "1. **Setup and Configuration** - Initialize the adapter\n",
        "2. **Using Pipelines** - Quick inference with pipelines\n",
        "3. **Model Management** - Download and load models\n",
        "4. **Custom Generation** - Fine-grained control over generation\n",
        "5. **MLFlow Tracking** - Track experiments with HuggingFace models\n"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "outputs": [],
      "source": [
        "# Import dependencies\n",
        "from agentic_assistants import AgenticConfig\n",
        "from agentic_assistants.adapters import HuggingFaceAdapter\n",
        "from agentic_assistants.core import MLFlowTracker\n",
        "\n",
        "# Initialize configuration\n",
        "config = AgenticConfig()\n",
        "\n",
        "# Create the HuggingFace adapter\n",
        "adapter = HuggingFaceAdapter(config)\n",
        "\n",
        "# Create tracker for experiments\n",
        "tracker = MLFlowTracker(config)\n",
        "\n",
        "print(f\"MLFlow enabled: {tracker.enabled}\")\n",
        "print(f\"Tracking URI: {config.mlflow.tracking_uri}\")\n",
        "print(f\"Cache directory: {adapter.cache_dir}\")\n"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "outputs": [],
      "source": [
        "# Create a text generation pipeline\n",
        "generator = adapter.create_pipeline(\n",
        "    \"text-generation\",\n",
        "    model=\"gpt2\",\n",
        "    device=\"cpu\"  # Use 'cuda' if GPU available\n",
        ")\n",
        "\n",
        "# Run the pipeline with tracking\n",
        "result = adapter.run_pipeline(\n",
        "    generator,\n",
        "    \"The future of AI is\",\n",
        "    experiment_name=\"huggingface-demo\",\n",
        "    run_name=\"text-generation-v1\",\n",
        "    max_length=50,\n",
        ")\n",
        "\n",
        "print(\"Generated text:\")\n",
        "print(result[0]['generated_text'])\n"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "outputs": [],
      "source": [
        "# Load a model and tokenizer for custom generation\n",
        "model = adapter.load_model(\"distilgpt2\", model_class=\"AutoModelForCausalLM\")\n",
        "tokenizer = adapter.load_tokenizer(\"distilgpt2\")\n",
        "\n",
        "# Generate with custom parameters\n",
        "generated = adapter.generate(\n",
        "    model,\n",
        "    tokenizer,\n",
        "    prompt=\"In a world where AI agents collaborate,\",\n",
        "    max_new_tokens=50,\n",
        "    temperature=0.8,\n",
```
