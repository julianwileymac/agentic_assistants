# Chunk: 3b91c2ba8dd0_1

- source: `src/agentic_assistants/data/sources.py`
- lines: 86-187
- chunk: 2/13

```
etadata: Dict[str, Any] = field(default_factory=dict)
    
    def to_dict(self) -> Dict[str, Any]:
        return {
            "name": self.name,
            "columns": [c.to_dict() for c in self.columns],
            "row_count": self.row_count,
            "size_bytes": self.size_bytes,
            "metadata": self.metadata,
        }


@dataclass
class ColumnSchema:
    """Schema for a column or field."""
    name: str
    data_type: str
    nullable: bool = True
    primary_key: bool = False
    description: Optional[str] = None
    metadata: Dict[str, Any] = field(default_factory=dict)
    
    def to_dict(self) -> Dict[str, Any]:
        return {
            "name": self.name,
            "data_type": self.data_type,
            "nullable": self.nullable,
            "primary_key": self.primary_key,
            "description": self.description,
            "metadata": self.metadata,
        }


# ============================================================================
# Handler Base Class
# ============================================================================

class DataSourceHandler(ABC):
    """
    Abstract base class for data source handlers.
    
    Each handler type (database, file_store, api) implements this interface
    to provide connection testing, schema discovery, and data access.
    """
    
    @property
    @abstractmethod
    def source_type(self) -> str:
        """Return the source type this handler supports."""
        pass
    
    @property
    @abstractmethod
    def supported_drivers(self) -> List[str]:
        """Return list of supported drivers/providers."""
        pass
    
    @abstractmethod
    def test_connection(self, config: Dict[str, Any]) -> ConnectionResult:
        """
        Test if the connection configuration is valid.
        
        Args:
            config: Connection configuration dictionary
            
        Returns:
            ConnectionResult with success status and details
        """
        pass
    
    @abstractmethod
    def discover_schema(self, config: Dict[str, Any]) -> SchemaInfo:
        """
        Discover the schema of the data source.
        
        Args:
            config: Connection configuration dictionary
            
        Returns:
            SchemaInfo with tables and columns
        """
        pass
    
    def validate_config(self, config: Dict[str, Any]) -> tuple[bool, str]:
        """
        Validate connection configuration.
        
        Args:
            config: Configuration to validate
            
        Returns:
            Tuple of (is_valid, error_message)
        """
        return True, ""


# ============================================================================
# Database Handler
# ============================================================================

class DatabaseHandler(DataSourceHandler):
```
