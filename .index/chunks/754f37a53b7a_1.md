# Chunk: 754f37a53b7a_1

- source: `src/agentic_assistants/data/corpus/manager.py`
- lines: 97-178
- chunk: 2/5

```
              source=meta.get("source", ""),
                        added_at=datetime.fromisoformat(meta["added_at"]),
                        word_count=meta.get("word_count", 0),
                        char_count=meta.get("char_count", 0),
                        tags=meta.get("tags", []),
                        metadata=meta.get("metadata", {}),
                    )
                    # Rebuild hash set
                    import hashlib
                    text = self._documents.get(doc_id, "")
                    text_hash = hashlib.sha256(text.encode()).hexdigest()
                    self._hashes.add(text_hash)
    
    def _save(self) -> None:
        """Save corpus to storage."""
        # Save documents
        docs_file = self._storage_path / "documents.jsonl"
        with open(docs_file, "w") as f:
            for doc_id, text in self._documents.items():
                f.write(json.dumps({"id": doc_id, "text": text}) + "\n")
        
        # Save metadata
        meta_file = self._storage_path / "metadata.json"
        meta_data = {}
        for doc_id, meta in self._metadata.items():
            meta_data[doc_id] = {
                "source": meta.source,
                "added_at": meta.added_at.isoformat(),
                "word_count": meta.word_count,
                "char_count": meta.char_count,
                "tags": meta.tags,
                "metadata": meta.metadata,
            }
        with open(meta_file, "w") as f:
            json.dump(meta_data, f, indent=2)
    
    def add_document(
        self,
        text: str,
        doc_id: Optional[str] = None,
        source: str = "",
        tags: Optional[List[str]] = None,
        metadata: Optional[Dict[str, Any]] = None,
    ) -> Optional[str]:
        """
        Add a document to the corpus.
        
        Args:
            text: Document text
            doc_id: Optional document ID
            source: Source of the document
            tags: Document tags
            metadata: Additional metadata
            
        Returns:
            Document ID if added, None if rejected
        """
        import hashlib
        
        # Validate length
        if len(text) < self.config.min_doc_length:
            logger.debug(f"Document too short ({len(text)} chars)")
            return None
        
        if len(text) > self.config.max_doc_length:
            text = text[:self.config.max_doc_length]
        
        # Check for duplicates
        text_hash = hashlib.sha256(text.encode()).hexdigest()
        if self.config.deduplicate and text_hash in self._hashes:
            logger.debug("Duplicate document rejected")
            return None
        
        # Generate ID if needed
        if doc_id is None:
            doc_id = text_hash[:16]
        
        # Add document
        self._documents[doc_id] = text
        self._hashes.add(text_hash)
        
```
