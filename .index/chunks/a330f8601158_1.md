# Chunk: a330f8601158_1

- source: `.venv-lab/Lib/site-packages/zmq/green/poll.py`
- lines: 88-102
- chunk: 2/2

```
wlist, xlist)
                except gevent.Timeout as t:
                    if t is not _bug_timeout:
                        raise
                finally:
                    _bug_timeout.cancel()

        except gevent.Timeout as t:
            if t is not tout:
                raise
            return []
        finally:
            if timeout > 0:
                tout.cancel()
```
