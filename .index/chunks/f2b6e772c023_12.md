# Chunk: f2b6e772c023_12

- source: `src/agentic_assistants/data/catalog.py`
- lines: 926-1022
- chunk: 13/20

```
 Optional[str] = None) -> List[Dict[str, Any]]:
        """
        List available dataset templates.
        
        Args:
            category: Filter by category (api, dataset, web_scraping, etc.)
            
        Returns:
            List of template info dictionaries
        """
        from agentic_assistants.data.templates import get_registry
        
        registry = get_registry()
        
        if category:
            template_ids = registry.list_by_category(category)
        else:
            template_ids = registry.list_all()
        
        return [
            registry.get(tid).to_dict()
            for tid in template_ids
            if registry.get(tid) is not None
        ]
    
    def search_templates(
        self,
        query: str = "",
        category: Optional[str] = None,
        tags: Optional[List[str]] = None,
    ) -> List[Dict[str, Any]]:
        """
        Search for templates.
        
        Args:
            query: Search string (matches id, name, description)
            category: Filter by category
            tags: Filter by tags
            
        Returns:
            List of matching template info dictionaries
        """
        from agentic_assistants.data.templates import get_registry
        
        registry = get_registry()
        templates = registry.search(query=query, category=category, tags=tags)
        
        return [t.to_dict() for t in templates]
    
    def get_template_info(self, template_id: str) -> Optional[Dict[str, Any]]:
        """
        Get detailed information about a template.
        
        Args:
            template_id: Template ID
            
        Returns:
            Template info dictionary or None if not found
        """
        from agentic_assistants.data.templates import get_registry
        
        registry = get_registry()
        template = registry.get(template_id)
        
        if template:
            return template.to_dict()
        return None
    
    # =========================================================================
    # Remote Data Sources
    # =========================================================================
    
    def add_url_dataset(
        self,
        name: str,
        url: str,
        content_type: str = "auto",
        **kwargs,
    ) -> "AbstractDataset":
        """
        Add a URL dataset to the catalog.
        
        Convenience method for creating URL-based datasets.
        
        Args:
            name: Dataset name
            url: URL to fetch
            content_type: Expected content type (json, csv, etc.)
            **kwargs: Additional URLDataset arguments
            
        Returns:
            Created URLDataset instance
        """
        from agentic_assistants.data.datasets import URLDataset
        
        dataset = URLDataset(url=url, content_type=content_type, **kwargs)
```
