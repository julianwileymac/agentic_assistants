# Chunk: 754f37a53b7a_0

- source: `src/agentic_assistants/data/corpus/manager.py`
- lines: 1-102
- chunk: 1/5

```
"""
Corpus management for training text collections.
"""

import json
from dataclasses import dataclass, field
from datetime import datetime
from pathlib import Path
from typing import Any, Dict, Iterator, List, Optional

from agentic_assistants.utils.logging import get_logger

logger = get_logger(__name__)


@dataclass
class CorpusConfig:
    """Configuration for corpus management."""
    
    name: str = "default"
    storage_path: str = "./data/corpora"
    
    # Processing settings
    min_doc_length: int = 50
    max_doc_length: int = 100000
    deduplicate: bool = True
    
    # Tokenization
    lowercase: bool = True
    remove_punctuation: bool = False
    
    # Statistics
    track_statistics: bool = True


@dataclass
class DocumentMeta:
    """Metadata for a corpus document."""
    
    doc_id: str
    source: str = ""
    added_at: datetime = field(default_factory=datetime.utcnow)
    word_count: int = 0
    char_count: int = 0
    tags: List[str] = field(default_factory=list)
    metadata: Dict[str, Any] = field(default_factory=dict)


class Corpus:
    """
    A collection of text documents for training.
    
    Provides methods for:
    - Adding and removing documents
    - Iterating over documents
    - Computing statistics
    - Exporting for training
    """
    
    def __init__(self, config: CorpusConfig):
        """
        Initialize a corpus.
        
        Args:
            config: Corpus configuration
        """
        self.config = config
        self.name = config.name
        
        self._storage_path = Path(config.storage_path) / config.name
        self._storage_path.mkdir(parents=True, exist_ok=True)
        
        self._documents: Dict[str, str] = {}
        self._metadata: Dict[str, DocumentMeta] = {}
        self._hashes: set = set()
        
        # Load existing corpus
        self._load()
    
    def _load(self) -> None:
        """Load corpus from storage."""
        docs_file = self._storage_path / "documents.jsonl"
        meta_file = self._storage_path / "metadata.json"
        
        if docs_file.exists():
            with open(docs_file, "r") as f:
                for line in f:
                    doc = json.loads(line)
                    self._documents[doc["id"]] = doc["text"]
        
        if meta_file.exists():
            with open(meta_file, "r") as f:
                data = json.load(f)
                for doc_id, meta in data.items():
                    self._metadata[doc_id] = DocumentMeta(
                        doc_id=doc_id,
                        source=meta.get("source", ""),
                        added_at=datetime.fromisoformat(meta["added_at"]),
                        word_count=meta.get("word_count", 0),
                        char_count=meta.get("char_count", 0),
                        tags=meta.get("tags", []),
```
