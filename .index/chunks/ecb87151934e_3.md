# Chunk: ecb87151934e_3

- source: `src/agentic_assistants/data/templates/builtin/api_templates.py`
- lines: 256-315
- chunk: 4/4

```
"float",
            required=True,
            min_value=-90,
            max_value=90,
            example=52.52,
        ),
        TemplateParameter(
            name="longitude",
            description="Location longitude",
            param_type="float",
            required=True,
            min_value=-180,
            max_value=180,
            example=13.41,
        ),
        TemplateParameter(
            name="hourly",
            description="Hourly variables to include",
            param_type="string",
            required=False,
            default="temperature_2m,precipitation",
        ),
        TemplateParameter(
            name="forecast_days",
            description="Number of forecast days",
            param_type="integer",
            required=False,
            default=7,
            min_value=1,
            max_value=16,
        ),
    ]
    
    def create_dataset(self, **params) -> URLDataset:
        url = "https://api.open-meteo.com/v1/forecast"
        
        url_params = {
            "latitude": params["latitude"],
            "longitude": params["longitude"],
            "hourly": params.get("hourly", "temperature_2m"),
            "forecast_days": params.get("forecast_days", 7),
        }
        
        return URLDataset(
            url=url,
            content_type="json",
            params=url_params,
        )


def get_api_templates() -> List[DataSourceTemplate]:
    """Get all API templates."""
    return [
        GitHubAPITemplate(),
        HuggingFaceAPITemplate(),
        OpenAIAPITemplate(),
        JSONPlaceholderTemplate(),
        WeatherAPITemplate(),
    ]
```
