# Chunk: ecb87151934e_2

- source: `src/agentic_assistants/data/templates/builtin/api_templates.py`
- lines: 175-267
- chunk: 3/4

```
 TemplateParameter(
            name="api_key",
            description="OpenAI API key",
            param_type="string",
            required=True,
            secret=True,
        ),
    ]
    
    def create_dataset(self, **params) -> URLDataset:
        endpoint = params["endpoint"]
        if "{model_id}" in endpoint and params.get("model_id"):
            endpoint = endpoint.replace("{model_id}", params["model_id"])
        
        url = f"https://api.openai.com/v1/{endpoint}"
        
        return URLDataset(
            url=url,
            content_type="json",
            auth={"type": "bearer", "token": params["api_key"]},
        )


class JSONPlaceholderTemplate(DataSourceTemplate):
    """Template for JSONPlaceholder fake API (testing/demo)."""
    
    id = "jsonplaceholder"
    name = "JSONPlaceholder"
    description = "Fake online REST API for testing and prototyping"
    category = TemplateCategory.API
    dataset_type = "url"
    documentation_url = "https://jsonplaceholder.typicode.com/"
    tags = ["testing", "demo", "api", "json"]
    
    parameters = [
        TemplateParameter(
            name="resource",
            description="Resource type",
            param_type="string",
            required=True,
            choices=["posts", "comments", "albums", "photos", "todos", "users"],
            default="posts",
        ),
        TemplateParameter(
            name="id",
            description="Resource ID (optional)",
            param_type="integer",
            required=False,
        ),
    ]
    
    examples = [
        {"description": "Get all posts", "params": {"resource": "posts"}},
        {"description": "Get user by ID", "params": {"resource": "users", "id": 1}},
    ]
    
    def create_dataset(self, **params) -> URLDataset:
        resource = params["resource"]
        url = f"https://jsonplaceholder.typicode.com/{resource}"
        
        if params.get("id"):
            url = f"{url}/{params['id']}"
        
        return URLDataset(url=url, content_type="json")


class WeatherAPITemplate(DataSourceTemplate):
    """Template for Open-Meteo weather API (free, no auth required)."""
    
    id = "open-meteo"
    name = "Open-Meteo Weather API"
    description = "Free weather forecast and historical data API"
    category = TemplateCategory.API
    dataset_type = "url"
    documentation_url = "https://open-meteo.com/en/docs"
    tags = ["weather", "forecast", "api", "free"]
    
    parameters = [
        TemplateParameter(
            name="latitude",
            description="Location latitude",
            param_type="float",
            required=True,
            min_value=-90,
            max_value=90,
            example=52.52,
        ),
        TemplateParameter(
            name="longitude",
            description="Location longitude",
            param_type="float",
            required=True,
```
