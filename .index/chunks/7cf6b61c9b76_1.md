# Chunk: 7cf6b61c9b76_1

- source: `webui/src/app/models/training/new/page.tsx`
- lines: 75-158
- chunk: 2/6

```
fy({
          base_model: config.base_model,
          output_name: config.output_name,
          method: config.method,
          dataset_id: config.dataset_id,
          num_epochs: config.num_epochs,
          batch_size: config.batch_size,
          learning_rate: config.learning_rate,
          max_seq_length: config.max_seq_length,
          gradient_accumulation_steps: config.gradient_accumulation_steps,
          bf16: config.bf16,
          gradient_checkpointing: config.gradient_checkpointing,
          description: config.description,
          lora_config: config.method !== "full" ? {
            r: config.lora_r,
            lora_alpha: config.lora_alpha,
            lora_dropout: config.lora_dropout,
          } : undefined,
        }),
      });

      if (!response.ok) {
        const data = await response.json();
        throw new Error(data.detail || "Failed to create training job");
      }

      const job = await response.json();
      router.push(`/models/training/${job.id}`);
    } catch (err: any) {
      setError(err.message);
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="flex flex-col gap-6 p-6 max-w-4xl mx-auto">
      {/* Header */}
      <div className="flex items-center gap-4">
        <Link href="/models">
          <Button variant="ghost" size="icon">
            <ArrowLeft className="h-4 w-4" />
          </Button>
        </Link>
        <div>
          <h1 className="text-2xl font-bold">New Training Job</h1>
          <p className="text-muted-foreground">
            Configure and start a new model training job
          </p>
        </div>
      </div>

      {error && (
        <div className="bg-destructive/10 text-destructive p-4 rounded-md">
          {error}
        </div>
      )}

      <form onSubmit={handleSubmit} className="space-y-6">
        {/* Basic Settings */}
        <Card>
          <CardHeader>
            <CardTitle>Basic Settings</CardTitle>
            <CardDescription>Configure the model and training method</CardDescription>
          </CardHeader>
          <CardContent className="space-y-4">
            <div className="grid gap-4 md:grid-cols-2">
              <div className="space-y-2">
                <Label htmlFor="output_name">Model Name *</Label>
                <Input
                  id="output_name"
                  placeholder="my-custom-llama"
                  value={config.output_name}
                  onChange={(e) => setConfig({ ...config, output_name: e.target.value })}
                  required
                />
              </div>
              <div className="space-y-2">
                <Label htmlFor="dataset_id">Training Dataset *</Label>
                <Input
                  id="dataset_id"
                  placeholder="Dataset ID or path"
                  value={config.dataset_id}
```
