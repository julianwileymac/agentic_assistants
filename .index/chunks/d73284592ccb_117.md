# Chunk: d73284592ccb_117

- source: `webui/.next/dev/server/chunks/ssr/[root-of-the-server]__630d161a._.js`
- lines: 3467-3544
- chunk: 118/138

```

                selectedComponents: new Set(ids)
            }),
        clearSelection: ()=>set({
                selectedProjects: new Set(),
                selectedAgents: new Set(),
                selectedFlows: new Set(),
                selectedComponents: new Set()
            })
    }));
const useDraftStore = (0, __TURBOPACK__imported__module__$5b$project$5d2f$Documents$2f$GitHub$2f$agentic_assistants$2f$webui$2f$node_modules$2f$zustand$2f$esm$2f$react$2e$mjs__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["create"])((set, get)=>({
        drafts: new Map(),
        setDraft: (key, draft)=>set((state)=>{
                const newDrafts = new Map(state.drafts);
                newDrafts.set(key, draft);
                return {
                    drafts: newDrafts
                };
            }),
        getDraft: (key)=>get().drafts.get(key),
        removeDraft: (key)=>set((state)=>{
                const newDrafts = new Map(state.drafts);
                newDrafts.delete(key);
                return {
                    drafts: newDrafts
                };
            }),
        markDirty: (key)=>set((state)=>{
                const draft = state.drafts.get(key);
                if (draft) {
                    const newDrafts = new Map(state.drafts);
                    newDrafts.set(key, {
                        ...draft,
                        isDirty: true
                    });
                    return {
                        drafts: newDrafts
                    };
                }
                return state;
            }),
        markClean: (key)=>set((state)=>{
                const draft = state.drafts.get(key);
                if (draft) {
                    const newDrafts = new Map(state.drafts);
                    newDrafts.set(key, {
                        ...draft,
                        isDirty: false,
                        savedAt: new Date()
                    });
                    return {
                        drafts: newDrafts
                    };
                }
                return state;
            }),
        clearAllDrafts: ()=>set({
                drafts: new Map()
            }),
        hasDirtyDrafts: ()=>{
            const drafts = get().drafts;
            for (const draft of drafts.values()){
                if (draft.isDirty) return true;
            }
            return false;
        }
    }));
const useNotificationStore = (0, __TURBOPACK__imported__module__$5b$project$5d2f$Documents$2f$GitHub$2f$agentic_assistants$2f$webui$2f$node_modules$2f$zustand$2f$esm$2f$react$2e$mjs__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["create"])((set)=>({
        notifications: [],
        addNotification: (notification)=>{
            const id = Math.random().toString(36).substring(7);
            const newNotification = {
                ...notification,
                id,
                createdAt: new Date()
            };
            set((state)=>({
```
