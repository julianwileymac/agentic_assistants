# Chunk: bc7788fa5c9b_2

- source: `src/agentic_assistants/data/corpus/indexer.py`
- lines: 192-277
- chunk: 3/4

```
ns)
                    doc_length = self._doc_lengths.get(doc_id, 1)
                    
                    # BM25-like scoring
                    k1 = 1.2
                    b = 0.75
                    
                    length_norm = 1 - b + b * (doc_length / self._avg_doc_length)
                    tf_norm = (tf * (k1 + 1)) / (tf + k1 * length_norm)
                    
                    score = tf_norm * idf
                    doc_scores[doc_id] += score
                    doc_matches[doc_id].add(term)
        
        # Sort by score
        sorted_docs = sorted(
            doc_scores.items(),
            key=lambda x: x[1],
            reverse=True
        )[:top_k]
        
        # Build results
        results = []
        for doc_id, score in sorted_docs:
            text = self._documents.get(doc_id, "")
            preview = text[:preview_length] + "..." if len(text) > preview_length else text
            
            results.append(SearchResult(
                doc_id=doc_id,
                score=score,
                matches=list(doc_matches[doc_id]),
                preview=preview,
            ))
        
        return results
    
    def boolean_search(
        self,
        must_have: Optional[List[str]] = None,
        should_have: Optional[List[str]] = None,
        must_not: Optional[List[str]] = None,
    ) -> List[str]:
        """
        Boolean search over the index.
        
        Args:
            must_have: All terms must be present
            should_have: At least one term should be present
            must_not: None of these terms should be present
            
        Returns:
            List of matching document IDs
        """
        candidates = set(self._documents.keys())
        
        # Apply must_have
        if must_have:
            for term in must_have:
                term_lower = term.lower()
                if term_lower in self._index:
                    candidates &= set(self._index[term_lower].keys())
                else:
                    candidates = set()
                    break
        
        # Apply must_not
        if must_not:
            for term in must_not:
                term_lower = term.lower()
                if term_lower in self._index:
                    candidates -= set(self._index[term_lower].keys())
        
        # Apply should_have (filter if specified)
        if should_have:
            should_docs = set()
            for term in should_have:
                term_lower = term.lower()
                if term_lower in self._index:
                    should_docs |= set(self._index[term_lower].keys())
            candidates &= should_docs
        
        return list(candidates)
    
    def get_term_documents(self, term: str) -> List[str]:
        """Get all documents containing a term."""
```
