# Chunk: 4e9a4c0e690b_2

- source: `webui/src/components/layout/header.tsx`
- lines: 149-166
- chunk: 3/3

```
r />
            {isAuthenticated ? (
              <DropdownMenuItem onClick={() => logout()}>
                Log out
              </DropdownMenuItem>
            ) : (
              <DropdownMenuItem asChild>
                <a href="/login">Log in</a>
              </DropdownMenuItem>
            )}
          </DropdownMenuContent>
        </DropdownMenu>
      </div>
    </header>
  );
}

```
