# Chunk: c75ee782b9c8_7

- source: `src/agentic_assistants/data/training/tagging.py`
- lines: 537-559
- chunk: 8/8

```
._tags),
            "total_assignments": len(self._assignments),
            "by_category": {},
            "most_used": [],
        }
        
        # By category
        for category in TagCategory:
            category_tags = [t for t in self._tags.values() if t.category == category]
            stats["by_category"][category.value] = {
                "count": len(category_tags),
                "total_usage": sum(t.usage_count for t in category_tags),
            }
        
        # Most used tags
        sorted_tags = sorted(self._tags.values(), key=lambda t: t.usage_count, reverse=True)
        stats["most_used"] = [
            {"id": t.id, "name": t.name, "usage_count": t.usage_count}
            for t in sorted_tags[:10]
        ]
        
        return stats
```
