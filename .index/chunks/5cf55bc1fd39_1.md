# Chunk: 5cf55bc1fd39_1

- source: `.venv-lab/Lib/site-packages/IPython/utils/frame.py`
- lines: 82-95
- chunk: 2/2

```
val(expr,cf.f_globals,cf.f_locals)))


# deactivate it by uncommenting the following line, which makes it a no-op
#def debugx(expr,pre_msg=''): pass


def extract_module_locals(depth: int = 0) -> tuple[Any, Any]:
    """Returns (module, locals) of the function `depth` frames away from the caller"""
    f = sys._getframe(depth + 1)
    global_ns = f.f_globals
    module = sys.modules[global_ns['__name__']]
    return (module, f.f_locals)
```
