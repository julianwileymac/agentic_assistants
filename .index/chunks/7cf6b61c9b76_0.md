# Chunk: 7cf6b61c9b76_0

- source: `webui/src/app/models/training/new/page.tsx`
- lines: 1-83
- chunk: 1/6

```
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";
import { Slider } from "@/components/ui/slider";
import { Switch } from "@/components/ui/switch";
import { Separator } from "@/components/ui/separator";
import { Badge } from "@/components/ui/badge";
import { ArrowLeft, Play, Info } from "lucide-react";
import Link from "next/link";

const POPULAR_BASE_MODELS = [
  { id: "meta-llama/Llama-3.2-1B", name: "Llama 3.2 1B", params: "1B" },
  { id: "meta-llama/Llama-3.2-3B", name: "Llama 3.2 3B", params: "3B" },
  { id: "meta-llama/Llama-3.1-8B", name: "Llama 3.1 8B", params: "8B" },
  { id: "Qwen/Qwen2.5-1.5B", name: "Qwen 2.5 1.5B", params: "1.5B" },
  { id: "Qwen/Qwen2.5-7B", name: "Qwen 2.5 7B", params: "7B" },
  { id: "mistralai/Mistral-7B-v0.3", name: "Mistral 7B v0.3", params: "7B" },
  { id: "google/gemma-2-2b", name: "Gemma 2 2B", params: "2B" },
  { id: "microsoft/phi-3-mini-4k-instruct", name: "Phi-3 Mini", params: "3.8B" },
];

const TRAINING_METHODS = [
  { id: "lora", name: "LoRA", description: "Low-Rank Adaptation - efficient fine-tuning" },
  { id: "qlora", name: "QLoRA", description: "Quantized LoRA - 4-bit quantization" },
  { id: "full", name: "Full Fine-tune", description: "Train all parameters" },
];

export default function NewTrainingJobPage() {
  const router = useRouter();
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  // Form state
  const [config, setConfig] = useState({
    output_name: "",
    base_model: "",
    method: "lora",
    dataset_id: "",
    num_epochs: 3,
    batch_size: 4,
    learning_rate: 0.0002,
    max_seq_length: 2048,
    gradient_accumulation_steps: 4,
    bf16: true,
    gradient_checkpointing: true,
    description: "",
    // LoRA specific
    lora_r: 8,
    lora_alpha: 16,
    lora_dropout: 0.05,
  });

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setLoading(true);
    setError(null);

    try {
      const response = await fetch("/api/v1/training/jobs", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          base_model: config.base_model,
          output_name: config.output_name,
          method: config.method,
          dataset_id: config.dataset_id,
          num_epochs: config.num_epochs,
          batch_size: config.batch_size,
          learning_rate: config.learning_rate,
```
