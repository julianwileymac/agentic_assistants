# Chunk: 4e38ddc67619_4

- source: `webui/src/lib/types.ts`
- lines: 453-585
- chunk: 5/6

```
me;
  description: string;
  path: string;
  dataset_count: number;
  total_size_bytes: number;
  exists: boolean;
}

export interface DatasetInfo {
  name: string;
  type: string;
  filepath: string;
  layer?: DataLayerName;
  versioned: boolean;
  versions?: string[];
  schema?: DatasetSchema;
}

export interface DatasetSchema {
  columns: string[];
  dtypes: Record<string, string>;
  row_count?: number;
}

// ============================================================================
// Catalog Types
// ============================================================================

export interface CatalogEntry {
  id: string;
  name: string;
  entry_type: 'source' | 'table' | 'column';
  source_id: string;
  description: string;
  tags: string[];
  metadata: Record<string, unknown>;
  created_at: string;
  updated_at: string;
}

export interface CatalogSearchResult {
  entries: CatalogEntry[];
  total: number;
  query: string;
}

export interface DataLineage {
  id: string;
  source_entry_id: string;
  target_entry_id: string;
  relationship_type: 'derived_from' | 'transforms_to' | 'joins_with';
  transformation?: string;
  metadata: Record<string, unknown>;
}

// ============================================================================
// Component Generation Types
// ============================================================================

export interface GeneratePromptRequest {
  notes: string;
  component_type: ComponentCategory;
  additional_context?: string;
}

export interface GeneratePromptResponse {
  original_notes: string;
  optimized_prompt: string;
  component_type: string;
  suggestions: string[];
}

export interface GenerateComponentRequest {
  prompt: string;
  component_type: ComponentCategory;
  name?: string;
  language: string;
}

export interface GeneratedComponent {
  name: string;
  category: ComponentCategory;
  code: string;
  description: string;
  usage_example: string;
  language: string;
  tags: string[];
}

export interface GenerateFromNotesRequest {
  notes: string;
  component_type: ComponentCategory;
  name?: string;
  language: string;
  save_component: boolean;
}

export interface GenerateFromNotesResponse {
  optimized_prompt: string;
  component: GeneratedComponent;
  saved: boolean;
  component_id?: string;
}

export interface GenerationChatMessage {
  role: 'user' | 'assistant' | 'system';
  content: string;
}

export interface GenerationChatRequest {
  messages: GenerationChatMessage[];
  component_type: ComponentCategory;
}

export interface GenerationChatResponse {
  message: GenerationChatMessage;
  component_preview?: GeneratedComponent;
  suggestions: string[];
}

// ============================================================================
// API Response Types
// ============================================================================

export interface ApiResponse<T> {
  data?: T;
  error?: string;
  status: number;
}

export interface PaginatedResponse<T> {
  items: T[];
```
