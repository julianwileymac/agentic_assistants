# Chunk: 7077f8331ec4_1

- source: `src/agentic_assistants/data/filesystem.py`
- lines: 93-178
- chunk: 2/5

```
edentials for a protocol.
        
        Args:
            protocol: Protocol name (e.g., 's3', 'azure')
            credentials: Credentials dictionary
        """
        protocol = self.PROTOCOL_ALIASES.get(protocol, protocol)
        self._credentials[protocol] = credentials
        
        # Invalidate cached filesystem for this protocol
        if protocol in self._filesystems:
            del self._filesystems[protocol]
        
        logger.debug(f"Registered credentials for protocol: {protocol}")
    
    def register_config(self, name: str, config: FilesystemConfig) -> None:
        """
        Register a named filesystem configuration.
        
        Args:
            name: Configuration name
            config: Filesystem configuration
        """
        self._configs[name] = config
        logger.debug(f"Registered filesystem config: {name}")
    
    def get_config(self, name: str) -> Optional[FilesystemConfig]:
        """Get a named filesystem configuration."""
        return self._configs.get(name)
    
    def parse_path(self, path: str) -> tuple[str, str]:
        """
        Parse a path into protocol and path components.
        
        Args:
            path: Full path (e.g., 's3://bucket/key' or '/local/path')
            
        Returns:
            Tuple of (protocol, path)
        """
        if "://" in path:
            parsed = urlparse(path)
            protocol = self.PROTOCOL_ALIASES.get(parsed.scheme, parsed.scheme)
            # For cloud storage, include netloc (bucket/container) in path
            if parsed.netloc:
                clean_path = f"{parsed.netloc}{parsed.path}"
            else:
                clean_path = parsed.path
            return protocol, clean_path
        else:
            return "file", path
    
    def get_filesystem(
        self,
        path_or_protocol: str,
        **kwargs,
    ) -> Any:
        """
        Get a filesystem instance for the given path or protocol.
        
        Args:
            path_or_protocol: Path with protocol or protocol name
            **kwargs: Additional arguments for filesystem initialization
            
        Returns:
            fsspec filesystem instance
        """
        try:
            import fsspec
        except ImportError:
            raise ImportError("fsspec not installed. Run: pip install fsspec")
        
        # Determine protocol
        if "://" in path_or_protocol:
            protocol, _ = self.parse_path(path_or_protocol)
        else:
            protocol = self.PROTOCOL_ALIASES.get(path_or_protocol, path_or_protocol)
        
        # Check cache
        cache_key = f"{protocol}_{hash(frozenset(kwargs.items()))}"
        if cache_key in self._filesystems:
            return self._filesystems[cache_key]
        
        # Get credentials for this protocol
        creds = self._credentials.get(protocol, {})
```
