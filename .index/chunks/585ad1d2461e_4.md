# Chunk: 585ad1d2461e_4

- source: `src/agentic_assistants/data/datasets/rss_dataset.py`
- lines: 321-403
- chunk: 5/6

```
redentials['username']}:{self._credentials['password']}"
                headers["Authorization"] = f"Basic {base64.b64encode(creds.encode()).decode()}"
        
        # Fetch feed
        try:
            import httpx
            
            with httpx.Client(timeout=timeout) as client:
                response = client.get(self.url, headers=headers)
                response.raise_for_status()
                feed_content = response.text
        except ImportError:
            # Fall back to feedparser's built-in fetching
            feed_content = None
        except Exception as e:
            raise DatasetError(f"Failed to fetch feed: {e}")
        
        # Parse feed
        if feed_content:
            parsed = feedparser.parse(feed_content)
        else:
            parsed = feedparser.parse(self.url)
        
        if parsed.bozo and parsed.bozo_exception:
            logger.warning(f"Feed parsing warning: {parsed.bozo_exception}")
        
        return parsed
    
    def _load(self) -> Dict[str, Any]:
        """Load and parse the RSS feed."""
        parsed = self._fetch_feed()
        
        # Parse feed info
        self._feed_info = self._parse_feed_info(parsed.feed)
        
        # Parse entries
        entries = [self._parse_entry(e) for e in parsed.entries]
        
        # Filter entries
        entries = self._filter_entries(entries)
        
        # Update state for change detection
        self._last_fetch = datetime.utcnow()
        self._last_entry_ids = {e.id for e in entries}
        
        # Update metadata
        self._metadata.custom["feed_title"] = self._feed_info.title
        self._metadata.custom["feed_url"] = self.url
        self._metadata.custom["entry_count"] = len(entries)
        self._metadata.custom["fetched_at"] = self._last_fetch.isoformat()
        
        logger.info(f"Fetched {len(entries)} entries from: {self._feed_info.title}")
        
        return {
            "feed": self._feed_info.to_dict(),
            "entries": [e.to_dict() for e in entries],
            "fetched_at": self._last_fetch.isoformat(),
        }
    
    def _save(self, data: Any) -> None:
        """RSS datasets are read-only."""
        raise DatasetError("RSSDataset does not support saving.")
    
    def exists(self) -> bool:
        """Check if the feed URL is accessible."""
        try:
            import httpx
            with httpx.Client(timeout=10) as client:
                response = client.head(self.url)
                return response.status_code < 400
        except Exception:
            return False
    
    def get_feed_info(self) -> Optional[FeedInfo]:
        """Get feed metadata from last fetch."""
        return self._feed_info
    
    def get_new_entries(self) -> List[Dict[str, Any]]:
        """
        Get entries that are new since the last fetch.
        
        Returns:
```
