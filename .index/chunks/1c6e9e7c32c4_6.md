# Chunk: 1c6e9e7c32c4_6

- source: `src/agentic_assistants/data/datasets/scraper_dataset.py`
- lines: 483-566
- chunk: 7/9

```
) * 1000
        
        start = time.time()
        with sync_playwright() as p:
            browser = p.chromium.launch(headless=True)
            page = browser.new_page()
            
            try:
                response = page.goto(url, wait_until="networkidle", timeout=timeout)
                status = response.status if response else 200
                html = page.content()
            finally:
                browser.close()
        
        elapsed = (time.time() - start) * 1000
        return html, status, elapsed
    
    def _scrape_url(self, url: str) -> List[ScrapeResult]:
        """Scrape a single URL (possibly with pagination)."""
        results = []
        current_url = url
        pages_scraped = 0
        
        while current_url and pages_scraped < self.max_pages:
            try:
                # Fetch HTML
                html, status, elapsed = self._fetch_html(current_url)
                
                # Parse HTML
                soup = self._get_parser(html)
                
                # Extract data
                data = self.extractor.extract(soup, current_url)
                
                result = ScrapeResult(
                    url=current_url,
                    data=data,
                    status_code=status,
                    elapsed_ms=elapsed,
                )
                results.append(result)
                
                logger.info(f"Scraped: {current_url} ({elapsed:.0f}ms)")
                
                # Check for next page
                if self.follow_pagination:
                    current_url = self.extractor.get_next_page(soup, current_url)
                else:
                    current_url = None
                
                pages_scraped += 1
                
                # Rate limiting
                if current_url:
                    delay = self._load_args.get("delay_between_requests", 1.0)
                    if delay > 0:
                        time.sleep(delay)
                        
            except Exception as e:
                logger.error(f"Failed to scrape {current_url}: {e}")
                results.append(ScrapeResult(
                    url=current_url,
                    data={},
                    error=str(e),
                ))
                break
        
        return results
    
    def _load(self) -> List[Dict[str, Any]]:
        """Load data by scraping all URLs."""
        self._results = []
        delay = self._load_args.get("delay_between_requests", 1.0)
        
        for i, url in enumerate(self.urls):
            # Scrape URL (handles pagination internally)
            url_results = self._scrape_url(url)
            self._results.extend(url_results)
            
            # Rate limiting between different URLs
            if i < len(self.urls) - 1 and delay > 0:
                time.sleep(delay)
        
```
