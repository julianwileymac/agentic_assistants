# Chunk: 3de876d90055_0

- source: `webui/src/app/kubernetes/models/page.tsx`
- lines: 1-122
- chunk: 1/7

```
"use client";

import * as React from "react";
import {
  Box,
  Plus,
  RefreshCw,
  MoreHorizontal,
  Trash2,
  Scale,
  Link as LinkIcon,
  Zap,
  CheckCircle,
  XCircle,
  Clock,
  Loader2,
  Server,
  Cpu,
  HardDrive,
} from "lucide-react";

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Skeleton } from "@/components/ui/skeleton";
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "@/components/ui/table";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";
import { toast } from "sonner";
import {
  useModelDeployments,
  useDeployModel,
  useDeleteModelDeployment,
  useScaleModelDeployment,
  useModelEndpoint,
  useSetModelAsCodeAssistant,
} from "@/lib/api";

function StatusBadge({ status }: { status: string }) {
  const variants: Record<string, "default" | "secondary" | "destructive" | "outline"> = {
    Running: "default",
    Pending: "secondary",
    Scaling: "secondary",
    Failed: "destructive",
  };

  const icons: Record<string, React.ReactNode> = {
    Running: <CheckCircle className="size-3" />,
    Pending: <Clock className="size-3" />,
    Scaling: <Loader2 className="size-3 animate-spin" />,
    Failed: <XCircle className="size-3" />,
  };

  return (
    <Badge variant={variants[status] || "outline"} className="gap-1">
      {icons[status]}
      {status}
    </Badge>
  );
}

function ServingTypeBadge({ type }: { type: string }) {
  const colors: Record<string, string> = {
    ollama: "bg-green-500/10 text-green-500",
    vllm: "bg-blue-500/10 text-blue-500",
    tgi: "bg-purple-500/10 text-purple-500",
  };

  return (
    <span className={`px-2 py-0.5 rounded text-xs font-medium uppercase ${colors[type] || "bg-gray-500/10 text-gray-500"}`}>
      {type}
    </span>
  );
}

export default function ModelsPage() {
  const { data: models, isLoading, mutate } = useModelDeployments();

  const [deployDialog, setDeployDialog] = React.useState(false);
  const [deployForm, setDeployForm] = React.useState({
    name: "",
    model_name: "",
    serving_type: "ollama",
    replicas: 1,
    cpu_request: "500m",
    cpu_limit: "4000m",
    memory_request: "2Gi",
    memory_limit: "8Gi",
    context_length: 4096,
  });

  const [endpointDialog, setEndpointDialog] = React.useState<{
```
