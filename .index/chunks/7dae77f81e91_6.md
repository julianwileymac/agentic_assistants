# Chunk: 7dae77f81e91_6

- source: `.venv-lab/Lib/site-packages/psutil/_psosx.py`
- lines: 539-550
- chunk: 7/7

```
upposed to return it anyway)
        return PROC_STATUSES.get(code, '?')

    @wrap_exceptions
    def threads(self):
        rawlist = cext.proc_threads(self.pid)
        retlist = []
        for thread_id, utime, stime in rawlist:
            ntuple = ntp.pthread(thread_id, utime, stime)
            retlist.append(ntuple)
        return retlist
```
