# Chunk: 4a9d07425f4b_2

- source: `src/agentic_assistants/data/datasets/pandas_datasets.py`
- lines: 175-261
- chunk: 3/5

```
__(filepath, **kwargs)
        self._as_dataframe = as_dataframe
    
    def _load(self) -> Union[pd.DataFrame, Dict, list]:
        """Load JSON file."""
        if self._as_dataframe:
            return pd.read_json(self.filepath, **self._load_args)
        else:
            import json
            with open(self.filepath, "r", encoding="utf-8") as f:
                return json.load(f)
    
    def _save(self, data: Union[pd.DataFrame, Dict, list]) -> None:
        """Save data to JSON file."""
        if isinstance(data, pd.DataFrame):
            data.to_json(self._filepath, **self._save_args)
        else:
            import json
            with open(self._filepath, "w", encoding="utf-8") as f:
                json.dump(data, f, indent=self._save_args.get("indent", 2))


class ExcelDataset(AbstractDataset[pd.DataFrame]):
    """
    Dataset for Excel files (.xlsx, .xls) using Pandas.
    
    Example:
        >>> dataset = ExcelDataset(
        ...     filepath="data/report.xlsx",
        ...     load_args={"sheet_name": "Sheet1"},
        ... )
        >>> df = dataset.load()
    """
    
    DEFAULT_LOAD_ARGS: Dict[str, Any] = {"sheet_name": 0}
    DEFAULT_SAVE_ARGS: Dict[str, Any] = {"index": False, "sheet_name": "Sheet1"}
    
    def __init__(
        self,
        filepath: Union[str, Path],
        engine: Optional[str] = None,
        **kwargs,
    ):
        """
        Initialize ExcelDataset.
        
        Args:
            filepath: Path to the Excel file
            engine: Engine for reading/writing ('openpyxl', 'xlrd', etc.)
            **kwargs: Additional arguments passed to parent
        """
        super().__init__(filepath, **kwargs)
        self._engine = engine
    
    def _load(self) -> pd.DataFrame:
        """Load Excel file into a pandas DataFrame."""
        load_args = {**self._load_args}
        if self._engine:
            load_args["engine"] = self._engine
        return pd.read_excel(self.filepath, **load_args)
    
    def _save(self, data: pd.DataFrame) -> None:
        """Save pandas DataFrame to Excel file."""
        save_args = {**self._save_args}
        if self._engine:
            save_args["engine"] = self._engine
        data.to_excel(self._filepath, **save_args)
    
    def _update_metadata_on_load(self, data: pd.DataFrame) -> None:
        """Update metadata with schema information."""
        super()._update_metadata_on_load(data)
        
        self._metadata.schema = {
            "columns": list(data.columns),
            "dtypes": {col: str(dtype) for col, dtype in data.dtypes.items()},
        }
        self._metadata.row_count = len(data)


class SQLDataset(AbstractDataset[pd.DataFrame]):
    """
    Dataset for SQL queries using Pandas and SQLAlchemy.
    
    Example:
        >>> dataset = SQLDataset(
        ...     sql="SELECT * FROM users WHERE active = 1",
```
