# Chunk: 456d9c708749_3

- source: `src/agentic_assistants/data/datasets/kaggle_dataset.py`
- lines: 223-302
- chunk: 4/6

```
wnload_path}")
    
    def _load_file(self, file_path: Path) -> Any:
        """Load data from the file based on extension."""
        suffix = file_path.suffix.lower()
        
        if suffix == ".csv":
            try:
                import pandas as pd
                csv_args = {
                    k: v for k, v in self._load_args.items()
                    if k in ["sep", "delimiter", "header", "names", "index_col",
                             "usecols", "dtype", "parse_dates", "encoding"]
                }
                return pd.read_csv(file_path, **csv_args)
            except ImportError:
                import csv
                with open(file_path, "r") as f:
                    return list(csv.DictReader(f))
        
        elif suffix == ".parquet":
            try:
                import pandas as pd
                return pd.read_parquet(file_path)
            except ImportError:
                raise DatasetError("pandas is required for parquet files")
        
        elif suffix == ".json":
            import json
            with open(file_path, "r") as f:
                return json.load(f)
        
        elif suffix in [".xlsx", ".xls"]:
            try:
                import pandas as pd
                return pd.read_excel(file_path)
            except ImportError:
                raise DatasetError("pandas and openpyxl are required for Excel files")
        
        else:
            # Return raw content
            with open(file_path, "rb") as f:
                return f.read()
    
    def _load(self) -> Any:
        """Load data from Kaggle."""
        api = self._get_kaggle_api()
        
        # Download dataset
        force_download = self._load_args.get("force_download", False)
        download_path = self._download_dataset(api, force=force_download)
        
        # Find and load file
        file_path = self._find_file(download_path)
        data = self._load_file(file_path)
        
        # Update metadata
        self._metadata.custom["kaggle_ref"] = self.dataset_ref or self.competition
        self._metadata.custom["file_name"] = file_path.name
        self._metadata.custom["file_path"] = str(file_path)
        self._metadata.custom["downloaded_at"] = datetime.utcnow().isoformat()
        
        logger.info(f"Loaded Kaggle file: {file_path.name}")
        return data
    
    def _save(self, data: Any) -> None:
        """Kaggle datasets are read-only."""
        raise DatasetError(
            "KaggleDataset does not support saving back to Kaggle. "
            "Use a different dataset type to save locally."
        )
    
    def exists(self) -> bool:
        """Check if the dataset/file exists locally."""
        download_path = self._get_download_path()
        if not download_path.exists():
            return False
        
        if self.file_name:
```
