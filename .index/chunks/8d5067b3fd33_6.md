# Chunk: 8d5067b3fd33_6

- source: `src/agentic_assistants/data/rag/annotators.py`
- lines: 504-555
- chunk: 7/7

```
s together."""
    
    name = "chained"
    
    def __init__(self, annotators: List[Annotator]):
        """
        Initialize chained annotator.
        
        Args:
            annotators: List of annotators to apply in order
        """
        self.annotators = annotators
    
    def annotate(self, chunk: Chunk, context: Dict[str, Any]) -> Chunk:
        for annotator in self.annotators:
            chunk = annotator.annotate(chunk, context)
        return chunk


def get_annotator(name: str, **kwargs) -> Annotator:
    """
    Get an annotator by name.
    
    Args:
        name: Annotator name
        **kwargs: Arguments for the annotator
        
    Returns:
        Configured Annotator instance
    """
    annotators = {
        "classification": ClassificationAnnotator,
        "sentiment": SentimentAnnotator,
        "topics": TopicAnnotator,
        "language": LanguageAnnotator,
        "quality": QualityAnnotator,
    }
    
    annotator_class = annotators.get(name.lower())
    if not annotator_class:
        raise ValueError(f"Unknown annotator: {name}")
    
    return annotator_class(**kwargs)


def create_default_annotator() -> ChainedAnnotator:
    """Create a default annotator chain."""
    return ChainedAnnotator([
        LanguageAnnotator(),
        QualityAnnotator(),
    ])
```
