# Chunk: 7077f8331ec4_2

- source: `src/agentic_assistants/data/filesystem.py`
- lines: 170-252
- chunk: 3/5

```
     
        # Check cache
        cache_key = f"{protocol}_{hash(frozenset(kwargs.items()))}"
        if cache_key in self._filesystems:
            return self._filesystems[cache_key]
        
        # Get credentials for this protocol
        creds = self._credentials.get(protocol, {})
        fs_kwargs = {**creds, **kwargs}
        
        # Protocol-specific initialization
        if protocol == "file":
            fs = fsspec.filesystem("file", **fs_kwargs)
        
        elif protocol == "s3":
            # Try to get credentials from environment if not provided
            if "key" not in fs_kwargs and "anon" not in fs_kwargs:
                fs_kwargs.setdefault("key", os.environ.get("AWS_ACCESS_KEY_ID"))
                fs_kwargs.setdefault("secret", os.environ.get("AWS_SECRET_ACCESS_KEY"))
                fs_kwargs.setdefault("token", os.environ.get("AWS_SESSION_TOKEN"))
            fs = fsspec.filesystem("s3", **fs_kwargs)
        
        elif protocol == "abfs":
            # Azure Blob Storage
            if "connection_string" not in fs_kwargs:
                fs_kwargs.setdefault(
                    "connection_string",
                    os.environ.get("AZURE_STORAGE_CONNECTION_STRING"),
                )
            fs = fsspec.filesystem("abfs", **fs_kwargs)
        
        elif protocol == "gcs":
            # Google Cloud Storage
            if "token" not in fs_kwargs:
                fs_kwargs.setdefault(
                    "token",
                    os.environ.get("GOOGLE_APPLICATION_CREDENTIALS"),
                )
            fs = fsspec.filesystem("gcs", **fs_kwargs)
        
        else:
            # Generic filesystem
            fs = fsspec.filesystem(protocol, **fs_kwargs)
        
        # Cache the filesystem
        self._filesystems[cache_key] = fs
        
        return fs
    
    def open(
        self,
        path: str,
        mode: str = "rb",
        **kwargs,
    ) -> Any:
        """
        Open a file from any supported filesystem.
        
        Args:
            path: Full path to the file
            mode: File mode
            **kwargs: Additional arguments
            
        Returns:
            File-like object
        """
        protocol, clean_path = self.parse_path(path)
        fs = self.get_filesystem(protocol, **kwargs)
        return fs.open(clean_path, mode=mode)
    
    def exists(self, path: str, **kwargs) -> bool:
        """Check if a path exists."""
        protocol, clean_path = self.parse_path(path)
        fs = self.get_filesystem(protocol, **kwargs)
        return fs.exists(clean_path)
    
    def ls(self, path: str, detail: bool = False, **kwargs) -> List:
        """List directory contents."""
        protocol, clean_path = self.parse_path(path)
        fs = self.get_filesystem(protocol, **kwargs)
        return fs.ls(clean_path, detail=detail)
    
```
