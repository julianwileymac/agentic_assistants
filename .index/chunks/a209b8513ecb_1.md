# Chunk: a209b8513ecb_1

- source: `.venv-lab/Lib/site-packages/pip/_internal/commands/inspect.py`
- lines: 78-93
- chunk: 2/2

```
_location is not None:
                res["direct_url"] = {
                    "url": path_to_url(editable_project_location),
                    "dir_info": {
                        "editable": True,
                    },
                }
        # installer
        installer = dist.installer
        if dist.installer:
            res["installer"] = installer
        # requested
        if dist.installed_with_dist_info:
            res["requested"] = dist.requested
        return res
```
