# Chunk: 6d00b34a93b6_0

- source: `frontend/packages/agentic-mlflow/src/browser/mlflow-menu.ts`
- lines: 1-53
- chunk: 1/1

```
/**
 * MLFlow Menu Contribution
 * 
 * Provides menu items for MLFlow commands.
 */

import { injectable } from '@theia/core/shared/inversify';
import { MenuContribution, MenuModelRegistry, MenuPath, MAIN_MENU_BAR } from '@theia/core/lib/common';
import { MLFlowCommands } from './mlflow-commands';

export namespace MLFlowMenus {
    export const AGENTIC: MenuPath = [...MAIN_MENU_BAR, '7_agentic'];
    export const MLFLOW: MenuPath = [...AGENTIC, '1_experiments', '1_mlflow'];
}

@injectable()
export class MLFlowMenuContribution implements MenuContribution {

    registerMenus(menus: MenuModelRegistry): void {
        // Add MLFlow commands to the Experiments submenu
        menus.registerMenuAction(MLFlowMenus.MLFLOW, {
            commandId: MLFlowCommands.REFRESH_EXPERIMENTS.id,
            order: '1'
        });

        menus.registerMenuAction(MLFlowMenus.MLFLOW, {
            commandId: MLFlowCommands.CREATE_EXPERIMENT.id,
            order: '2'
        });

        menus.registerMenuAction(MLFlowMenus.MLFLOW, {
            commandId: MLFlowCommands.DELETE_EXPERIMENT.id,
            order: '3'
        });

        menus.registerMenuAction(MLFlowMenus.MLFLOW, {
            commandId: MLFlowCommands.START_RUN.id,
            order: '4'
        });

        menus.registerMenuAction(MLFlowMenus.MLFLOW, {
            commandId: MLFlowCommands.END_RUN.id,
            order: '5'
        });

        menus.registerMenuAction(MLFlowMenus.MLFLOW, {
            commandId: MLFlowCommands.OPEN_MLFLOW_UI.id,
            order: '10'
        });
    }
}

```
