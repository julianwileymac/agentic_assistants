# Chunk: 3de876d90055_2

- source: `webui/src/app/kubernetes/models/page.tsx`
- lines: 201-285
- chunk: 3/7

```
nter gap-2">
          <Button variant="outline" onClick={() => mutate()}>
            <RefreshCw className="size-4 mr-2" />
            Refresh
          </Button>
          <Button onClick={() => setDeployDialog(true)}>
            <Plus className="size-4 mr-2" />
            Deploy Model
          </Button>
        </div>
      </div>

      {/* Serving Types Info */}
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <Card>
          <CardContent className="pt-6">
            <div className="flex items-center gap-4">
              <div className="p-3 rounded-xl bg-green-500/10">
                <Server className="size-6 text-green-500" />
              </div>
              <div>
                <p className="font-semibold">Ollama</p>
                <p className="text-xs text-muted-foreground">
                  Local-first, easy model management
                </p>
              </div>
            </div>
          </CardContent>
        </Card>
        <Card>
          <CardContent className="pt-6">
            <div className="flex items-center gap-4">
              <div className="p-3 rounded-xl bg-blue-500/10">
                <Zap className="size-6 text-blue-500" />
              </div>
              <div>
                <p className="font-semibold">vLLM</p>
                <p className="text-xs text-muted-foreground">
                  High-performance inference engine
                </p>
              </div>
            </div>
          </CardContent>
        </Card>
        <Card>
          <CardContent className="pt-6">
            <div className="flex items-center gap-4">
              <div className="p-3 rounded-xl bg-purple-500/10">
                <Box className="size-6 text-purple-500" />
              </div>
              <div>
                <p className="font-semibold">TGI</p>
                <p className="text-xs text-muted-foreground">
                  HuggingFace Text Generation Inference
                </p>
              </div>
            </div>
          </CardContent>
        </Card>
      </div>

      {/* Models Table */}
      <Card>
        <CardHeader>
          <CardTitle>Deployed Models</CardTitle>
          <CardDescription>
            {models?.length || 0} model(s) deployed to the cluster
          </CardDescription>
        </CardHeader>
        <CardContent>
          {isLoading ? (
            <div className="space-y-4">
              {[1, 2].map((i) => (
                <Skeleton key={i} className="h-16 w-full" />
              ))}
            </div>
          ) : models && models.length > 0 ? (
            <Table>
              <TableHeader>
                <TableRow>
                  <TableHead>Name</TableHead>
                  <TableHead>Model</TableHead>
                  <TableHead>Type</TableHead>
                  <TableHead>Status</TableHead>
```
