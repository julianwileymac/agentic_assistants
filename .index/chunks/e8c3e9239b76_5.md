# Chunk: e8c3e9239b76_5

- source: `src/agentic_assistants/data/rag/ingestion_pipeline.py`
- lines: 389-471
- chunk: 6/8

```
          collection: Vector store collection name
            metadata: Additional metadata to attach to chunks
            **kwargs: Additional arguments for the data source
            
        Returns:
            IngestionResult with statistics
        """
        import time
        
        start_time = time.time()
        collection = collection or self.config.collection
        errors = []
        
        # Build context
        context = {
            "source": source,
            "source_type": source_type,
            "collection": collection,
            **(metadata or {}),
        }
        
        try:
            # Step 1: Fetch content
            logger.info(f"Fetching content from {source_type}: {source[:100]}...")
            content = self._fetch_content(source, source_type, **kwargs)
            
            if not content:
                return IngestionResult(
                    success=False,
                    source=source,
                    source_type=source_type,
                    chunks_created=0,
                    chunks_stored=0,
                    chunks_filtered=0,
                    errors=["No content fetched"],
                )
            
            # Step 2: Chunk content
            logger.info("Chunking content...")
            chunks = self._chunker.chunk(content, metadata)
            chunks_created = len(chunks)
            
            if not chunks:
                return IngestionResult(
                    success=False,
                    source=source,
                    source_type=source_type,
                    chunks_created=0,
                    chunks_stored=0,
                    chunks_filtered=0,
                    errors=["No chunks created"],
                )
            
            # Step 3: Augment chunks
            logger.info("Augmenting chunks...")
            chunks = self._augmenters.augment_batch(chunks, context)
            
            # Step 4: Annotate chunks
            logger.info("Annotating chunks...")
            chunks = self._annotators.annotate_batch(chunks, context)
            
            # Step 5: Filter chunks
            logger.info("Filtering chunks...")
            chunks = self._filter_chunks(chunks)
            chunks_filtered = chunks_created - len(chunks)
            
            # Step 6: Store chunks
            file_path = None
            chunks_stored = 0
            
            if self.config.store_as_files:
                file_path = self._save_to_files(chunks, source, collection)
            
            if self.config.store_in_vectordb:
                chunks_stored = self._store_in_vectordb(chunks, collection)
            else:
                chunks_stored = len(chunks)
            
            duration = time.time() - start_time
            
            return IngestionResult(
                success=True,
```
