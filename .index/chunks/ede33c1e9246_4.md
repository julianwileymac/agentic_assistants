# Chunk: ede33c1e9246_4

- source: `src/agentic_assistants/data/datasets/url_dataset.py`
- lines: 300-380
- chunk: 5/8

```

            cache_ttl = self._load_args.get("cache_ttl_seconds", 3600)
            cached_at = datetime.fromisoformat(cached["fetched_at"])
            if (datetime.utcnow() - cached_at).total_seconds() > cache_ttl:
                return None
            
            return URLFetchResult(
                content=cached["content"],
                status_code=cached["status_code"],
                headers=cached["headers"],
                url=cached["url"],
                fetched_at=cached_at,
                content_type=cached.get("content_type"),
                encoding=cached.get("encoding"),
            )
        except Exception as e:
            logger.debug(f"Failed to load cache: {e}")
            return None
    
    def _save_to_cache(self, result: URLFetchResult) -> None:
        """Save response to cache."""
        if not self.cache_responses:
            return
        
        try:
            import json
            
            self.cache_dir.mkdir(parents=True, exist_ok=True)
            cache_path = self._get_cache_path()
            
            # Serialize content
            content = result.content
            if isinstance(content, bytes):
                import base64
                content = base64.b64encode(content).decode()
            elif hasattr(content, "to_dict"):
                content = content.to_dict()
            
            cached = {
                "content": content,
                "status_code": result.status_code,
                "headers": result.headers,
                "url": result.url,
                "fetched_at": result.fetched_at.isoformat(),
                "content_type": result.content_type,
                "encoding": result.encoding,
            }
            
            with open(cache_path, "w") as f:
                json.dump(cached, f)
                
        except Exception as e:
            logger.debug(f"Failed to save cache: {e}")
    
    def _fetch(self) -> URLFetchResult:
        """Fetch data from URL with retry logic."""
        try:
            import httpx
        except ImportError:
            raise DatasetError("httpx is required. Install with: pip install httpx")
        
        timeout = self._load_args.get("timeout", 30)
        retries = self._load_args.get("retries", 3)
        retry_delay = self._load_args.get("retry_delay", 1.0)
        verify_ssl = self._load_args.get("verify_ssl", True)
        
        # Build headers
        all_headers = {**self.headers, **self._get_auth_headers()}
        
        # Add conditional headers for cache validation
        if self._etag:
            all_headers["If-None-Match"] = self._etag
        if self._last_modified:
            all_headers["If-Modified-Since"] = self._last_modified
        
        last_error = None
        
        for attempt in range(retries):
            try:
                start_time = time.time()
```
