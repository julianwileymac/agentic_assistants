# Chunk: bc5f93f507e6_131

- source: `webui/.next/dev/static/chunks/Documents_GitHub_agentic_assistants_webui_src_09956f45._.js`
- lines: 4273-4350
- chunk: 132/153

```
         apiKey
            });
            // Store in localStorage for API client
            if ("TURBOPACK compile-time truthy", 1) {
                localStorage.setItem('api_key', apiKey);
            }
        },
        logout: ()=>{
            set({
                isAuthenticated: false,
                apiKey: undefined,
                user: undefined
            });
            if ("TURBOPACK compile-time truthy", 1) {
                localStorage.removeItem('api_key');
            }
        }
    }), {
    name: 'auth-storage',
    partialize: (state)=>({
            isAuthenticated: state.isAuthenticated,
            apiKey: state.apiKey,
            user: state.user
        })
}));
const useUIStore = (0, __TURBOPACK__imported__module__$5b$project$5d2f$Documents$2f$GitHub$2f$agentic_assistants$2f$webui$2f$node_modules$2f$zustand$2f$esm$2f$react$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["create"])()((0, __TURBOPACK__imported__module__$5b$project$5d2f$Documents$2f$GitHub$2f$agentic_assistants$2f$webui$2f$node_modules$2f$zustand$2f$esm$2f$middleware$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["persist"])((set)=>({
        sidebarOpen: true,
        sidebarCollapsed: false,
        theme: 'dark',
        activeModal: null,
        searchQuery: '',
        toggleSidebar: ()=>set((state)=>({
                    sidebarOpen: !state.sidebarOpen
                })),
        setSidebarCollapsed: (collapsed)=>set({
                sidebarCollapsed: collapsed
            }),
        setTheme: (theme)=>set({
                theme
            }),
        openModal: (modal)=>set({
                activeModal: modal
            }),
        closeModal: ()=>set({
                activeModal: null
            }),
        setSearchQuery: (query)=>set({
                searchQuery: query
            })
    }), {
    name: 'ui-storage',
    partialize: (state)=>({
            sidebarCollapsed: state.sidebarCollapsed,
            theme: state.theme
        })
}));
const useSelectionStore = (0, __TURBOPACK__imported__module__$5b$project$5d2f$Documents$2f$GitHub$2f$agentic_assistants$2f$webui$2f$node_modules$2f$zustand$2f$esm$2f$react$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__["create"])((set)=>({
        selectedProjects: new Set(),
        selectedAgents: new Set(),
        selectedFlows: new Set(),
        selectedComponents: new Set(),
        toggleProject: (id)=>set((state)=>{
                const newSet = new Set(state.selectedProjects);
                if (newSet.has(id)) {
                    newSet.delete(id);
                } else {
                    newSet.add(id);
                }
                return {
                    selectedProjects: newSet
                };
            }),
        toggleAgent: (id)=>set((state)=>{
                const newSet = new Set(state.selectedAgents);
                if (newSet.has(id)) {
                    newSet.delete(id);
                } else {
```
