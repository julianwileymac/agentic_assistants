# Chunk: 4d719a9d6d32_0

- source: `frontend/packages/agentic-core/lib/browser/agentic-menu.js`
- lines: 1-60
- chunk: 1/2

```
"use strict";
/**
 * Agentic Menu Contribution
 *
 * Provides menu items for Agentic functionality.
 */
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.AgenticMenuContribution = exports.AgenticMenus = void 0;
const inversify_1 = require("@theia/core/shared/inversify");
const common_1 = require("@theia/core/lib/common");
const agentic_commands_1 = require("./agentic-commands");
var AgenticMenus;
(function (AgenticMenus) {
    AgenticMenus.AGENTIC = [...common_1.MAIN_MENU_BAR, '7_agentic'];
    AgenticMenus.AGENTIC_EXPERIMENTS = [...AgenticMenus.AGENTIC, '1_experiments'];
    AgenticMenus.AGENTIC_SESSIONS = [...AgenticMenus.AGENTIC, '2_sessions'];
    AgenticMenus.AGENTIC_SYSTEM = [...AgenticMenus.AGENTIC, '3_system'];
})(AgenticMenus = exports.AgenticMenus || (exports.AgenticMenus = {}));
let AgenticMenuContribution = class AgenticMenuContribution {
    registerMenus(menus) {
        // Register the main Agentic menu
        menus.registerSubmenu(AgenticMenus.AGENTIC, 'Agentic', {
            order: '7'
        });
        // Experiments submenu
        menus.registerSubmenu(AgenticMenus.AGENTIC_EXPERIMENTS, 'Experiments');
        menus.registerMenuAction(AgenticMenus.AGENTIC_EXPERIMENTS, {
            commandId: agentic_commands_1.AgenticCommands.LIST_EXPERIMENTS.id,
            order: '1'
        });
        menus.registerMenuAction(AgenticMenus.AGENTIC_EXPERIMENTS, {
            commandId: agentic_commands_1.AgenticCommands.CREATE_EXPERIMENT.id,
            order: '2'
        });
        // Sessions submenu
        menus.registerSubmenu(AgenticMenus.AGENTIC_SESSIONS, 'Sessions');
        menus.registerMenuAction(AgenticMenus.AGENTIC_SESSIONS, {
            commandId: agentic_commands_1.AgenticCommands.LIST_SESSIONS.id,
            order: '1'
        });
        menus.registerMenuAction(AgenticMenus.AGENTIC_SESSIONS, {
            commandId: agentic_commands_1.AgenticCommands.CREATE_SESSION.id,
            order: '2'
        });
        // System submenu
        menus.registerSubmenu(AgenticMenus.AGENTIC_SYSTEM, 'System');
        menus.registerMenuAction(AgenticMenus.AGENTIC_SYSTEM, {
            commandId: agentic_commands_1.AgenticCommands.CHECK_CONNECTION.id,
            order: '1'
        });
        menus.registerMenuAction(AgenticMenus.AGENTIC_SYSTEM, {
            commandId: agentic_commands_1.AgenticCommands.SHOW_CONFIG.id,
            order: '2'
```
