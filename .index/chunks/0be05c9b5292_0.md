# Chunk: 0be05c9b5292_0

- source: `.prompts/scratch/vector_store.md`
- lines: 1-21
- chunk: 1/1

```
<Project_Overview>
This project aims to create a multi-agent application for loading local git repositories into a persistent vector store to augment agentic code assistant solutions.
</Project_Overview>
<Task_Overview>
Your task is to build the agentic architecture using crewai. You are to write any modules that are needed and then create the main code block as both as script and as a notebook.
</Task_Overview>
<Task_Items>
- Add any modules needed to implement the core classes of crew ai, document them all 
- Embed opentelemetry tracing into the application and log verbosely
- Input to run will be a local path pointing to a folder with one or more code projects. The crew must analyze, augment, annotate, and store the code and underlying logic, along with any supporting documentation, into the persistent vector store
- Add core support for storing objects into a persistent vector store. Use LanceDB and ChromaDB as the primary stores. Integrate deployment with the existing framework scripts
- Parameterize the vector store embeddings. 
- Treat each run as an MLFlow experiment
- Create template resources and any helper classes, methods, or functions for common agentic design patterns 
- Ensure these are accessible during runtime via the jupyter lab instance
</Task_Items>
<Requirements>
- Current functionality must be preserved
- Add logging and tracing where possible
- Provide as much core functionality as possible, use cases take second priority 
</Requirements>
```
