# Chunk: 93212946cb5b_2

- source: `frontend/packages/agentic-core/src/browser/agentic-commands.ts`
- lines: 138-157
- chunk: 3/3

```
messageService.error('Failed to create session: ' + response.error);
                }
            }
        });

        registry.registerCommand(AgenticCommands.RECONNECT_WEBSOCKET, {
            execute: async () => {
                try {
                    this.wsClient.disconnect();
                    await this.wsClient.connect();
                    this.messageService.info('WebSocket reconnected');
                } catch (error) {
                    this.messageService.error('Failed to reconnect WebSocket: ' + error);
                }
            }
        });
    }
}

```
