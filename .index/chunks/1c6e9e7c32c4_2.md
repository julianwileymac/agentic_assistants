# Chunk: 1c6e9e7c32c4_2

- source: `src/agentic_assistants/data/datasets/scraper_dataset.py`
- lines: 173-263
- chunk: 3/9

```
tion/ld+json"]',
        ...     json_path="$.product"
        ... )
    """
    
    def __init__(
        self,
        script_selector: str = 'script[type="application/ld+json"]',
        json_path: Optional[str] = None,
        variable_name: Optional[str] = None,
    ):
        """
        Initialize JSON extractor.
        
        Args:
            script_selector: CSS selector for script tag containing JSON
            json_path: JSONPath expression to extract specific data
            variable_name: JavaScript variable name containing data
        """
        self.script_selector = script_selector
        self.json_path = json_path
        self.variable_name = variable_name
    
    def extract(self, soup, url: str) -> Dict[str, Any]:
        """Extract JSON data from page."""
        import json
        import re
        
        result = {}
        
        # Try to find JSON in script tag
        script = soup.select_one(self.script_selector)
        
        if script:
            try:
                data = json.loads(script.string)
                
                # Apply JSONPath if specified
                if self.json_path:
                    try:
                        from jsonpath_ng import parse
                        expr = parse(self.json_path)
                        matches = [m.value for m in expr.find(data)]
                        data = matches[0] if len(matches) == 1 else matches
                    except ImportError:
                        logger.warning("jsonpath-ng not installed, returning full JSON")
                
                if isinstance(data, dict):
                    result = data
                else:
                    result["data"] = data
                    
            except json.JSONDecodeError as e:
                logger.warning(f"Failed to parse JSON: {e}")
        
        # Try to find JavaScript variable
        if self.variable_name:
            for script in soup.find_all("script"):
                if script.string:
                    pattern = rf"{self.variable_name}\s*=\s*(\{{.*?\}}|\[.*?\])"
                    match = re.search(pattern, script.string, re.DOTALL)
                    if match:
                        try:
                            data = json.loads(match.group(1))
                            result[self.variable_name] = data
                        except json.JSONDecodeError:
                            pass
        
        return result


class TableExtractor(Extractor):
    """
    Extractor for HTML tables.
    
    Example:
        >>> extractor = TableExtractor(
        ...     table_selector="table.data-table",
        ...     header_row=0
        ... )
    """
    
    def __init__(
        self,
        table_selector: str = "table",
        table_index: int = 0,
        header_row: int = 0,
        skip_rows: int = 0,
    ):
        """
```
