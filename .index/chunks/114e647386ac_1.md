# Chunk: 114e647386ac_1

- source: `src/agentic_assistants/data/rag/chunkers.py`
- lines: 93-176
- chunk: 2/9

```
          index=buffer.index,
                    start_char=buffer.start_char,
                    end_char=chunk.end_char,
                    metadata={**buffer.metadata, **chunk.metadata},
                )
                
                if combined_chunk.length >= self.min_chunk_size:
                    merged.append(combined_chunk)
                    buffer = None
                else:
                    buffer = combined_chunk
        
        if buffer is not None:
            merged.append(buffer)
        
        # Reindex
        for i, chunk in enumerate(merged):
            chunk.index = i
        
        return merged


class TextChunker(Chunker):
    """
    Simple text chunker that splits on character boundaries with overlap.
    
    This is a basic chunker suitable for general text content.
    """
    
    def __init__(
        self,
        chunk_size: int = 1024,
        chunk_overlap: int = 128,
        separator: str = "\n\n",
        **kwargs,
    ):
        super().__init__(chunk_size, chunk_overlap, **kwargs)
        self.separator = separator
    
    def chunk(self, text: str, metadata: Optional[Dict[str, Any]] = None) -> List[Chunk]:
        metadata = metadata or {}
        
        if len(text) <= self.chunk_size:
            return [Chunk(
                content=text,
                index=0,
                start_char=0,
                end_char=len(text),
                metadata=metadata,
            )]
        
        chunks = []
        
        # First, try to split on separator
        parts = text.split(self.separator)
        
        current_chunk = ""
        current_start = 0
        
        for part in parts:
            potential_chunk = current_chunk + (self.separator if current_chunk else "") + part
            
            if len(potential_chunk) <= self.chunk_size:
                current_chunk = potential_chunk
            else:
                if current_chunk:
                    chunks.append(Chunk(
                        content=current_chunk,
                        index=len(chunks),
                        start_char=current_start,
                        end_char=current_start + len(current_chunk),
                        metadata=metadata.copy(),
                    ))
                    
                    # Calculate new start with overlap
                    if self.chunk_overlap > 0 and len(current_chunk) > self.chunk_overlap:
                        overlap_text = current_chunk[-self.chunk_overlap:]
                        current_start = current_start + len(current_chunk) - self.chunk_overlap
                        current_chunk = overlap_text + self.separator + part
                    else:
                        current_start = current_start + len(current_chunk) + len(self.separator)
                        current_chunk = part
                else:
```
