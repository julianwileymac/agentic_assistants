# Chunk: 3de876d90055_6

- source: `webui/src/app/kubernetes/models/page.tsx`
- lines: 462-488
- chunk: 7/7

```
ilable"}
              </div>
            </div>
            {endpointDialog.external && (
              <div>
                <Label className="text-xs text-muted-foreground">External Endpoint</Label>
                <div className="mt-1 p-3 bg-muted rounded-lg font-mono text-sm break-all">
                  {endpointDialog.external}
                </div>
              </div>
            )}
            <p className="text-xs text-muted-foreground">
              Use the internal endpoint for workloads running inside the cluster.
              Configure an Ingress for external access.
            </p>
          </div>
          <DialogFooter>
            <Button variant="outline" onClick={() => setEndpointDialog({ ...endpointDialog, open: false })}>
              Close
            </Button>
          </DialogFooter>
        </DialogContent>
      </Dialog>
    </div>
  );
}
```
