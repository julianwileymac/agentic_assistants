# Chunk: ae806c06b504_0

- source: `frontend/packages/agentic-mlflow/lib/browser/experiments-tree-widget.d.ts`
- lines: 1-42
- chunk: 1/1

```
/**
 * Experiments Tree Widget
 *
 * Provides a tree view for browsing MLFlow experiments and runs.
 */
import { TreeWidget, TreeNode, CompositeTreeNode, SelectableTreeNode, ExpandableTreeNode, TreeModel, TreeProps } from '@theia/core/lib/browser/tree';
import { ContextMenuRenderer } from '@theia/core/lib/browser';
import { MessageService } from '@theia/core';
import { MLFlowService, Experiment, Run } from './mlflow-service';
export declare const EXPERIMENTS_TREE_WIDGET_ID = "experiments-tree-widget";
export interface ExperimentNode extends CompositeTreeNode, SelectableTreeNode, ExpandableTreeNode {
    experiment: Experiment;
}
export interface RunNode extends SelectableTreeNode {
    run: Run;
}
export declare namespace ExperimentNode {
    function is(node: TreeNode | undefined): node is ExperimentNode;
}
export declare namespace RunNode {
    function is(node: TreeNode | undefined): node is RunNode;
}
export declare class ExperimentsTreeWidget extends TreeWidget {
    readonly props: TreeProps;
    readonly model: TreeModel;
    readonly contextMenuRenderer: ContextMenuRenderer;
    static readonly ID = "experiments-tree-widget";
    static readonly LABEL = "Experiments";
    protected readonly mlflowService: MLFlowService;
    protected readonly messageService: MessageService;
    constructor(props: TreeProps, model: TreeModel, contextMenuRenderer: ContextMenuRenderer);
    protected init(): void;
    refresh(): Promise<void>;
    protected refreshTree(): void;
    protected createRootNode(experiments: Experiment[]): CompositeTreeNode;
    protected createExperimentNode(experiment: Experiment, parent: CompositeTreeNode): ExperimentNode;
    protected createRunNode(run: Run, parent: ExperimentNode): RunNode;
    protected updateExperimentRuns(experimentId: string, runs: Run[]): void;
    protected handleNodeExpansion(node: ExpandableTreeNode): Promise<void>;
    protected toNodeIcon(node: TreeNode): string;
}
//# sourceMappingURL=experiments-tree-widget.d.ts.map
```
