# Chunk: 65db50c84ebe_0

- source: `notebooks/.ipynb_checkpoints/07_langchain_agents-checkpoint.ipynb`
- lines: 1-79
- chunk: 1/1

```
{
  "cells": [
    {
      "cell_type": "markdown",
      "metadata": {},
      "source": [
        "# LangChain Agents\n",
        "\n",
        "This notebook demonstrates LangChain agent patterns with MLFlow tracking.\n",
        "\n",
        "## Contents\n",
        "1. **Setup** - Initialize LangChain with Ollama\n",
        "2. **Basic Chains** - Simple LLM chains\n",
        "3. **Tool-Using Agents** - Agents with custom tools\n",
        "4. **Conversation Agents** - Chat-based agents with memory\n"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "outputs": [],
      "source": [
        "# Setup\n",
        "from agentic_assistants import AgenticConfig\n",
        "from agentic_assistants.core import MLFlowTracker\n",
        "from langchain_ollama import ChatOllama\n",
        "from langchain_core.prompts import ChatPromptTemplate\n",
        "from langchain_core.output_parsers import StrOutputParser\n",
        "\n",
        "config = AgenticConfig()\n",
        "tracker = MLFlowTracker(config)\n",
        "\n",
        "llm = ChatOllama(\n",
        "    model=config.ollama.default_model,\n",
        "    base_url=config.ollama.host,\n",
        ")\n",
        "\n",
        "print(f\"LLM: {config.ollama.default_model}\")\n"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "outputs": [],
      "source": [
        "# Create a simple chain\n",
        "import time\n",
        "\n",
        "prompt = ChatPromptTemplate.from_messages([\n",
        "    (\"system\", \"You are a helpful AI assistant.\"),\n",
        "    (\"human\", \"{input}\")\n",
        "])\n",
        "\n",
        "chain = prompt | llm | StrOutputParser()\n",
        "\n",
        "# Run with tracking\n",
        "with tracker.start_run(run_name=\"langchain-basic-chain\"):\n",
        "    tracker.log_param(\"model\", config.ollama.default_model)\n",
        "    \n",
        "    start = time.time()\n",
        "    response = chain.invoke({\"input\": \"What is the capital of France?\"})\n",
        "    duration = time.time() - start\n",
        "    \n",
        "    tracker.log_metric(\"duration_seconds\", duration)\n",
        "    print(f\"Response ({duration:.2f}s): {response}\")\n"
      ]
    }
  ],
  "metadata": {
    "language_info": {
      "name": "python"
    }
  },
  "nbformat": 4,
  "nbformat_minor": 2
}
```
