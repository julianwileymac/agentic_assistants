# Chunk: 7cf6b61c9b76_3

- source: `webui/src/app/models/training/new/page.tsx`
- lines: 213-289
- chunk: 4/6

```
</div>
                    </CardContent>
                  </Card>
                ))}
              </div>
            </div>

            <div className="space-y-2">
              <Label htmlFor="description">Description</Label>
              <Textarea
                id="description"
                placeholder="Describe your training objective..."
                value={config.description}
                onChange={(e) => setConfig({ ...config, description: e.target.value })}
              />
            </div>
          </CardContent>
        </Card>

        {/* Training Hyperparameters */}
        <Card>
          <CardHeader>
            <CardTitle>Training Hyperparameters</CardTitle>
            <CardDescription>Fine-tune the training process</CardDescription>
          </CardHeader>
          <CardContent className="space-y-6">
            <div className="grid gap-6 md:grid-cols-2">
              <div className="space-y-2">
                <Label>Number of Epochs: {config.num_epochs}</Label>
                <Slider
                  value={[config.num_epochs]}
                  onValueChange={([value]) => setConfig({ ...config, num_epochs: value })}
                  min={1}
                  max={10}
                  step={1}
                />
              </div>
              <div className="space-y-2">
                <Label>Batch Size: {config.batch_size}</Label>
                <Slider
                  value={[config.batch_size]}
                  onValueChange={([value]) => setConfig({ ...config, batch_size: value })}
                  min={1}
                  max={32}
                  step={1}
                />
              </div>
              <div className="space-y-2">
                <Label>Learning Rate</Label>
                <Input
                  type="number"
                  step="0.0001"
                  value={config.learning_rate}
                  onChange={(e) => setConfig({ ...config, learning_rate: parseFloat(e.target.value) })}
                />
              </div>
              <div className="space-y-2">
                <Label>Max Sequence Length: {config.max_seq_length}</Label>
                <Slider
                  value={[config.max_seq_length]}
                  onValueChange={([value]) => setConfig({ ...config, max_seq_length: value })}
                  min={512}
                  max={8192}
                  step={256}
                />
              </div>
              <div className="space-y-2">
                <Label>Gradient Accumulation Steps: {config.gradient_accumulation_steps}</Label>
                <Slider
                  value={[config.gradient_accumulation_steps]}
                  onValueChange={([value]) => setConfig({ ...config, gradient_accumulation_steps: value })}
                  min={1}
                  max={16}
                  step={1}
                />
              </div>
```
