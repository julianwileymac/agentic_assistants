# Chunk: f2b6e772c023_7

- source: `src/agentic_assistants/data/catalog.py`
- lines: 537-616
- chunk: 8/20

```
   
        Args:
            conf_source: Path to the conf directory
            env: Environment name (default: 'base')
            config: AgenticConfig instance
            
        Returns:
            DataCatalog instance
        """
        conf_source = Path(conf_source)
        
        # Load base catalog
        base_path = conf_source / "base" / "catalog.yaml"
        if not base_path.exists():
            base_path = conf_source / "base" / "catalog.yml"
        
        base_data = {}
        if base_path.exists():
            with open(base_path, "r") as f:
                base_data = yaml.safe_load(f) or {}
        
        # Load environment-specific overrides
        if env != "base":
            env_path = conf_source / env / "catalog.yaml"
            if not env_path.exists():
                env_path = conf_source / env / "catalog.yml"
            
            if env_path.exists():
                with open(env_path, "r") as f:
                    env_data = yaml.safe_load(f) or {}
                # Merge environment data (overrides base)
                base_data = cls._merge_configs(base_data, env_data)
        
        # Load credentials
        credentials = {}
        for cred_env in [env, "local", "base"]:
            cred_path = conf_source / cred_env / "credentials.yaml"
            if not cred_path.exists():
                cred_path = conf_source / cred_env / "credentials.yml"
            
            if cred_path.exists():
                with open(cred_path, "r") as f:
                    credentials = yaml.safe_load(f) or {}
                break
        
        # Parse datasets and patterns
        datasets = {}
        patterns = []
        
        for name, ds_config in base_data.items():
            if name.startswith("_"):
                continue
            
            if "{" in name:
                patterns.append(DatasetPattern(
                    pattern=name,
                    type=ds_config.get("type", ""),
                    filepath_template=ds_config.get("filepath", ""),
                    load_args=ds_config.get("load_args", {}),
                    save_args=ds_config.get("save_args", {}),
                    versioned=ds_config.get("versioned", False),
                ))
            else:
                datasets[name] = DatasetConfig.from_dict(name, ds_config)
        
        return cls(
            datasets=datasets,
            patterns=patterns,
            credentials=credentials,
            config=config,
        )
    
    @staticmethod
    def _merge_configs(base: Dict, override: Dict) -> Dict:
        """Deep merge two configuration dictionaries."""
        result = copy.deepcopy(base)
        
        for key, value in override.items():
            if key in result and isinstance(result[key], dict) and isinstance(value, dict):
```
