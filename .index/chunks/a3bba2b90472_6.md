# Chunk: a3bba2b90472_6

- source: `src/agentic_assistants/crews/tasks.py`
- lines: 529-540
- chunk: 7/7

```
 Index to vector store (depends on all previous)
    index_task = IndexToVectorStoreTask.create(
        agent=agents["orchestrator"],
        repo_path=repo_path,
        collection=collection,
        config=config,
        context=[analyze_task, document_task, annotate_task],
    )
    
    return [analyze_task, document_task, annotate_task, index_task]

```
