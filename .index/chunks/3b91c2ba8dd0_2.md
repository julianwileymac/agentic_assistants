# Chunk: 3b91c2ba8dd0_2

- source: `src/agentic_assistants/data/sources.py`
- lines: 177-260
- chunk: 3/13

```
le of (is_valid, error_message)
        """
        return True, ""


# ============================================================================
# Database Handler
# ============================================================================

class DatabaseHandler(DataSourceHandler):
    """Handler for database connections via SQLAlchemy."""
    
    @property
    def source_type(self) -> str:
        return "database"
    
    @property
    def supported_drivers(self) -> List[str]:
        return ["postgresql", "mysql", "sqlite", "mssql", "oracle"]
    
    def validate_config(self, config: Dict[str, Any]) -> tuple[bool, str]:
        """Validate database configuration."""
        driver = config.get("driver", "")
        
        if driver not in self.supported_drivers:
            return False, f"Unsupported driver: {driver}. Supported: {self.supported_drivers}"
        
        if driver == "sqlite":
            if not config.get("database"):
                return False, "SQLite requires 'database' (file path)"
        else:
            required = ["host", "database"]
            missing = [r for r in required if not config.get(r)]
            if missing:
                return False, f"Missing required fields: {missing}"
        
        return True, ""
    
    def _build_connection_url(self, config: Dict[str, Any]) -> str:
        """Build SQLAlchemy connection URL from config."""
        driver = config.get("driver", "sqlite")
        
        if driver == "sqlite":
            db_path = config.get("database", ":memory:")
            return f"sqlite:///{db_path}"
        
        # Build URL for other databases
        user = config.get("username", "")
        password = config.get("password", "")
        host = config.get("host", "localhost")
        port = config.get("port", "")
        database = config.get("database", "")
        
        # Driver mapping
        driver_map = {
            "postgresql": "postgresql",
            "mysql": "mysql+pymysql",
            "mssql": "mssql+pyodbc",
            "oracle": "oracle+cx_oracle",
        }
        
        url_driver = driver_map.get(driver, driver)
        
        auth = f"{user}:{password}@" if user else ""
        port_str = f":{port}" if port else ""
        
        return f"{url_driver}://{auth}{host}{port_str}/{database}"
    
    def test_connection(self, config: Dict[str, Any]) -> ConnectionResult:
        """Test database connection."""
        import time
        
        valid, error = self.validate_config(config)
        if not valid:
            return ConnectionResult(success=False, message=error)
        
        try:
            from sqlalchemy import create_engine, text
            
            url = self._build_connection_url(config)
            engine = create_engine(url, connect_args={"connect_timeout": 10})
            
            start = time.time()
```
