# Chunk: 7e56b14d8e11_3

- source: `src/agentic_assistants/data/datasets/website_dataset.py`
- lines: 234-317
- chunk: 4/7

```
 -> Any:
        """Extract data using a selector configuration."""
        element = base_element or soup
        
        # Find elements
        if config.selector_type == "css":
            if config.multiple:
                elements = element.select(config.selector)
            else:
                elements = [element.select_one(config.selector)]
        elif config.selector_type == "xpath":
            # BeautifulSoup doesn't support XPath directly
            # Use lxml if available
            try:
                from lxml import etree
                tree = etree.HTML(str(element))
                xpath_results = tree.xpath(config.selector)
                elements = xpath_results if config.multiple else xpath_results[:1]
            except ImportError:
                raise DatasetError(
                    "lxml is required for XPath selectors. "
                    "Install with: pip install lxml"
                )
        else:
            raise DatasetError(f"Unknown selector type: {config.selector_type}")
        
        # Extract values
        results = []
        for elem in elements:
            if elem is None:
                continue
            
            # Get value
            if config.attribute:
                if hasattr(elem, "get"):
                    value = elem.get(config.attribute, "")
                elif hasattr(elem, "attrib"):
                    value = elem.attrib.get(config.attribute, "")
                else:
                    value = ""
            else:
                if hasattr(elem, "get_text"):
                    value = elem.get_text(strip=True)
                elif hasattr(elem, "text"):
                    value = elem.text or ""
                else:
                    value = str(elem)
            
            # Apply transform
            value = self._apply_transform(value, config.transform)
            results.append(value)
        
        # Return result
        if config.multiple:
            return results
        elif results:
            return results[0]
        else:
            return config.default
    
    def _extract_links(self, soup, base_url: str) -> List[str]:
        """Extract all links from the page."""
        links = []
        
        for a_tag in soup.find_all("a", href=True):
            href = a_tag["href"]
            
            # Skip anchors and javascript
            if href.startswith("#") or href.startswith("javascript:"):
                continue
            
            # Make absolute URL
            absolute_url = urljoin(base_url, href)
            
            # Filter by pattern if specified
            if self.follow_links:
                if not re.search(self.follow_links, absolute_url):
                    continue
            
            links.append(absolute_url)
        
        return list(set(links))
    
```
