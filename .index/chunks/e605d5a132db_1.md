# Chunk: e605d5a132db_1

- source: `src/agentic_assistants/data/layers.py`
- lines: 78-170
- chunk: 2/7

```
ed intermediate data",
            DataLayer.PRIMARY: "Domain models and business entities",
            DataLayer.FEATURE: "Engineered features for ML",
            DataLayer.MODEL_INPUT: "Training and inference datasets",
            DataLayer.MODEL_OUTPUT: "Model artifacts and predictions",
            DataLayer.REPORTING: "Reports, dashboards, and summaries",
            DataLayer.EXTERNAL: "Data prepared for external systems",
        }
        return descriptions.get(self, "")


@dataclass
class LayerConfig:
    """Configuration for a data layer."""
    name: str
    path: str
    description: str = ""
    read_only: bool = False  # For RAW layer
    versioned: bool = False
    default_format: str = "parquet"
    
    def to_dict(self) -> Dict[str, Any]:
        return {
            "name": self.name,
            "path": self.path,
            "description": self.description,
            "read_only": self.read_only,
            "versioned": self.versioned,
            "default_format": self.default_format,
        }


class DataLayerManager:
    """
    Manages data organization across convention layers.
    
    The DataLayerManager provides:
    - Layer path management
    - Dataset listing and discovery
    - Dataset promotion between layers
    - Layer-based catalog generation
    
    Attributes:
        data_root: Root directory for data storage
        layers: Dictionary mapping layer names to configs
        
    Example:
        >>> manager = DataLayerManager(data_root="data")
        >>> 
        >>> # Initialize layer structure
        >>> manager.create_layer_structure()
        >>> 
        >>> # Get path for a layer
        >>> raw_path = manager.get_layer_path(DataLayer.RAW)
        >>> 
        >>> # List datasets
        >>> for ds in manager.list_layer_datasets(DataLayer.PRIMARY):
        ...     print(ds)
    """
    
    DEFAULT_LAYER_CONFIGS = {
        DataLayer.RAW: LayerConfig(
            name="raw",
            path="01_raw",
            description="Immutable raw data",
            read_only=True,
            default_format="csv",
        ),
        DataLayer.INTERMEDIATE: LayerConfig(
            name="intermediate",
            path="02_intermediate",
            description="Cleaned data",
            default_format="parquet",
        ),
        DataLayer.PRIMARY: LayerConfig(
            name="primary",
            path="03_primary",
            description="Domain models",
            versioned=True,
            default_format="parquet",
        ),
        DataLayer.FEATURE: LayerConfig(
            name="feature",
            path="04_feature",
            description="ML features",
            versioned=True,
            default_format="parquet",
        ),
        DataLayer.MODEL_INPUT: LayerConfig(
            name="model_input",
            path="05_model_input",
            description="Training data",
```
