# Chunk: a219e7349210_0

- source: `notebooks/06_smolagents_intro.ipynb`
- lines: 1-92
- chunk: 1/2

```
{
  "cells": [
    {
      "cell_type": "markdown",
      "metadata": {},
      "source": [
        "# Introduction to smolagents\n",
        "\n",
        "This notebook demonstrates using HuggingFace's smolagents library for building tool-calling AI agents.\n",
        "\n",
        "## Contents\n",
        "1. **Setup** - Initialize the adapter\n",
        "2. **Creating Tools** - Define custom tools for agents\n",
        "3. **Building Agents** - Create CodeAgent and ToolCallingAgent\n",
        "4. **Running Tasks** - Execute agent tasks with tracking\n"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "outputs": [],
      "source": [
        "# Import dependencies\n",
        "from agentic_assistants import AgenticConfig\n",
        "from agentic_assistants.adapters import SmolAgentsAdapter\n",
        "from agentic_assistants.core import MLFlowTracker\n",
        "\n",
        "# Initialize\n",
        "config = AgenticConfig()\n",
        "adapter = SmolAgentsAdapter(config)\n",
        "tracker = MLFlowTracker(config)\n",
        "\n",
        "print(f\"MLFlow enabled: {tracker.enabled}\")\n",
        "print(f\"Default model type: {adapter.default_model_type}\")\n"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "outputs": [],
      "source": [
        "# Create tools\n",
        "from smolagents import tool\n",
        "\n",
        "@tool\n",
        "def calculator(expression: str) -> float:\n",
        "    \"\"\"Evaluate a mathematical expression.\"\"\"\n",
        "    allowed_chars = set('0123456789+-*/.() ')\n",
        "    if not all(c in allowed_chars for c in expression):\n",
        "        raise ValueError(\"Invalid characters in expression\")\n",
        "    return float(eval(expression))\n",
        "\n",
        "@tool\n",
        "def get_current_time() -> str:\n",
        "    \"\"\"Get the current date and time.\"\"\"\n",
        "    from datetime import datetime\n",
        "    return datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")\n",
        "\n",
        "print(\"Tools created!\")\n"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "outputs": [],
      "source": [
        "# Create model and agent\n",
        "model = adapter.create_model(model_type=\"HfApiModel\")\n",
        "\n",
        "code_agent = adapter.create_code_agent(\n",
        "    tools=[calculator, get_current_time],\n",
        "    model=model,\n",
        ")\n",
        "\n",
        "# Run the agent\n",
        "result = adapter.run_agent(\n",
        "    code_agent,\n",
        "    \"What is 25 multiplied by 17, plus 42?\",\n",
        "    experiment_name=\"smolagents-demo\",\n",
        "    run_name=\"math-task\",\n",
        ")\n",
        "\n",
        "print(f\"Result: {result}\")\n"
      ]
    }
  ],
  "metadata": {
    "language_info": {
```
