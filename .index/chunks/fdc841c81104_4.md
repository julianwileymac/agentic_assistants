# Chunk: fdc841c81104_4

- source: `src/agentic_assistants/data/datasets/s3_public_dataset.py`
- lines: 322-416
- chunk: 5/6

```
,
    ) -> List[Dict[str, Any]]:
        """
        List objects in the bucket with the configured prefix.
        
        Args:
            max_keys: Maximum number of keys to return
            delimiter: Delimiter for grouping (e.g., "/" for directories)
            
        Returns:
            List of object metadata dictionaries
        """
        client = self._get_client()
        
        objects = []
        continuation_token = None
        
        while len(objects) < max_keys:
            kwargs = {
                "Bucket": self.bucket,
                "Prefix": self.prefix,
                "MaxKeys": min(1000, max_keys - len(objects)),
            }
            
            if delimiter:
                kwargs["Delimiter"] = delimiter
            if continuation_token:
                kwargs["ContinuationToken"] = continuation_token
            
            try:
                response = client.list_objects_v2(**kwargs)
            except Exception as e:
                raise DatasetError(f"Failed to list objects: {e}")
            
            for obj in response.get("Contents", []):
                objects.append({
                    "key": obj["Key"],
                    "size": obj["Size"],
                    "last_modified": obj["LastModified"].isoformat(),
                    "etag": obj.get("ETag", "").strip('"'),
                })
            
            # Handle pagination
            if response.get("IsTruncated"):
                continuation_token = response.get("NextContinuationToken")
            else:
                break
        
        return objects
    
    def iter_objects(
        self,
        max_keys: Optional[int] = None,
    ) -> Iterator[Dict[str, Any]]:
        """
        Iterate over objects in the bucket.
        
        Args:
            max_keys: Maximum number of keys to iterate
            
        Yields:
            Object metadata dictionaries
        """
        client = self._get_client()
        
        continuation_token = None
        count = 0
        
        while True:
            if max_keys and count >= max_keys:
                break
            
            kwargs = {
                "Bucket": self.bucket,
                "Prefix": self.prefix,
                "MaxKeys": 1000,
            }
            
            if continuation_token:
                kwargs["ContinuationToken"] = continuation_token
            
            response = client.list_objects_v2(**kwargs)
            
            for obj in response.get("Contents", []):
                if max_keys and count >= max_keys:
                    break
                
                yield {
                    "key": obj["Key"],
                    "size": obj["Size"],
                    "last_modified": obj["LastModified"].isoformat(),
                }
                count += 1
            
```
