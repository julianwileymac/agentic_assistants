# Chunk: 8d5067b3fd33_2

- source: `src/agentic_assistants/data/rag/annotators.py`
- lines: 176-271
- chunk: 3/7

```
""
        self.use_llm = use_llm
        self.llm_fn = llm_fn
        self.return_score = return_score
    
    def _lexicon_sentiment(self, text: str) -> Tuple[str, float]:
        """Calculate sentiment using lexicon."""
        words = set(re.findall(r'\b\w+\b', text.lower()))
        
        positive_count = len(words & self.POSITIVE_WORDS)
        negative_count = len(words & self.NEGATIVE_WORDS)
        
        total = positive_count + negative_count
        if total == 0:
            return "neutral", 0.0
        
        score = (positive_count - negative_count) / total
        
        if score > 0.2:
            label = "positive"
        elif score < -0.2:
            label = "negative"
        else:
            label = "neutral"
        
        return label, score
    
    def _llm_sentiment(self, text: str) -> Tuple[str, float]:
        """Analyze sentiment using LLM."""
        if not self.llm_fn:
            raise ValueError("LLM function required")
        
        prompt = f"""Analyze the sentiment of the following text.
Return the sentiment (positive, negative, or neutral) and a score from -1 (very negative) to 1 (very positive).
Format: sentiment,score

Text:
{text[:2000]}

Analysis:"""
        
        response = self.llm_fn(prompt).strip()
        
        try:
            parts = response.split(",")
            label = parts[0].strip().lower()
            score = float(parts[1].strip()) if len(parts) > 1 else 0.0
            
            if label not in ("positive", "negative", "neutral"):
                label = "neutral"
            score = max(-1.0, min(1.0, score))
            
            return label, score
        except Exception:
            return "neutral", 0.0
    
    def annotate(self, chunk: Chunk, context: Dict[str, Any]) -> Chunk:
        if self.use_llm and self.llm_fn:
            label, score = self._llm_sentiment(chunk.content)
        else:
            label, score = self._lexicon_sentiment(chunk.content)
        
        chunk.metadata["sentiment"] = label
        if self.return_score:
            chunk.metadata["sentiment_score"] = round(score, 3)
        
        return chunk


class TopicAnnotator(Annotator):
    """
    Identify topics in chunk content.
    
    Uses topic modeling or LLM-based topic extraction.
    """
    
    name = "topics"
    
    def __init__(
        self,
        num_topics: int = 3,
        predefined_topics: Optional[List[str]] = None,
        use_llm: bool = True,
        llm_fn: Optional[Callable] = None,
    ):
        """
        Initialize topic annotator.
        
        Args:
            num_topics: Number of topics to extract
            predefined_topics: List of predefined topics to choose from
            use_llm: Use LLM for topic extraction
            llm_fn: Custom LLM function
        """
        self.num_topics = num_topics
```
