# Chunk: 3de876d90055_5

- source: `webui/src/app/kubernetes/models/page.tsx`
- lines: 399-469
- chunk: 6/7

```
          type="number"
                  min={1}
                  max={5}
                  value={deployForm.replicas}
                  onChange={(e) => setDeployForm({ ...deployForm, replicas: parseInt(e.target.value) || 1 })}
                />
              </div>
              <div>
                <Label htmlFor="context">Context Length</Label>
                <Input
                  id="context"
                  type="number"
                  value={deployForm.context_length}
                  onChange={(e) => setDeployForm({ ...deployForm, context_length: parseInt(e.target.value) || 4096 })}
                />
              </div>
            </div>
            <div className="grid grid-cols-2 gap-4">
              <div>
                <Label htmlFor="cpu">CPU Request</Label>
                <Input
                  id="cpu"
                  placeholder="500m"
                  value={deployForm.cpu_request}
                  onChange={(e) => setDeployForm({ ...deployForm, cpu_request: e.target.value })}
                />
              </div>
              <div>
                <Label htmlFor="memory">Memory Request</Label>
                <Input
                  id="memory"
                  placeholder="2Gi"
                  value={deployForm.memory_request}
                  onChange={(e) => setDeployForm({ ...deployForm, memory_request: e.target.value })}
                />
              </div>
            </div>
          </div>
          <DialogFooter>
            <Button variant="outline" onClick={() => setDeployDialog(false)}>
              Cancel
            </Button>
            <Button onClick={handleDeploy} disabled={isDeploying || !deployForm.name || !deployForm.model_name}>
              {isDeploying ? <Loader2 className="size-4 mr-2 animate-spin" /> : null}
              Deploy
            </Button>
          </DialogFooter>
        </DialogContent>
      </Dialog>

      {/* Endpoint Dialog */}
      <Dialog open={endpointDialog.open} onOpenChange={(open) => setEndpointDialog({ ...endpointDialog, open })}>
        <DialogContent>
          <DialogHeader>
            <DialogTitle>Model Endpoint</DialogTitle>
            <DialogDescription>
              Connection details for {endpointDialog.name}
            </DialogDescription>
          </DialogHeader>
          <div className="space-y-4 py-4">
            <div>
              <Label className="text-xs text-muted-foreground">Internal Endpoint (Cluster)</Label>
              <div className="mt-1 p-3 bg-muted rounded-lg font-mono text-sm break-all">
                {endpointDialog.internal || "Not available"}
              </div>
            </div>
            {endpointDialog.external && (
              <div>
                <Label className="text-xs text-muted-foreground">External Endpoint</Label>
                <div className="mt-1 p-3 bg-muted rounded-lg font-mono text-sm break-all">
```
