# Chunk: de096403bc90_0

- source: `k8s/cronjobs/data-sync-cronjob.yaml`
- lines: 1-113
- chunk: 1/2

```
apiVersion: batch/v1
kind: CronJob
metadata:
  name: data-sync
  namespace: agentic
  labels:
    app: data-sync
    app.kubernetes.io/component: scheduler
spec:
  schedule: "0 */6 * * *"  # Every 6 hours
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: data-sync
        spec:
          restartPolicy: OnFailure
          containers:
            - name: data-sync
              image: agentic-assistants:latest
              imagePullPolicy: IfNotPresent
              command:
                - python
                - -m
                - agentic_assistants.scheduling.run_job
                - --job-type
                - data-sync
              envFrom:
                - configMapRef:
                    name: agentic-config
              env:
                - name: REDIS_URL
                  value: "redis://redis:6379"
              resources:
                requests:
                  memory: "256Mi"
                  cpu: "100m"
                limits:
                  memory: "512Mi"
                  cpu: "500m"
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: rss-monitor
  namespace: agentic
  labels:
    app: rss-monitor
    app.kubernetes.io/component: scheduler
spec:
  schedule: "*/30 * * * *"  # Every 30 minutes
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 5
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: rss-monitor
        spec:
          restartPolicy: OnFailure
          containers:
            - name: rss-monitor
              image: agentic-assistants:latest
              imagePullPolicy: IfNotPresent
              command:
                - python
                - -m
                - agentic_assistants.scheduling.run_job
                - --job-type
                - rss-monitor
              envFrom:
                - configMapRef:
                    name: agentic-config
              env:
                - name: REDIS_URL
                  value: "redis://redis:6379"
              resources:
                requests:
                  memory: "256Mi"
                  cpu: "100m"
                limits:
                  memory: "512Mi"
                  cpu: "500m"
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: feature-materialization
  namespace: agentic
  labels:
    app: feature-materialization
    app.kubernetes.io/component: scheduler
spec:
  schedule: "0 2 * * *"  # Daily at 2 AM
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: feature-materialization
        spec:
          restartPolicy: OnFailure
          containers:
```
