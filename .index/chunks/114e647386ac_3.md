# Chunk: 114e647386ac_3

- source: `src/agentic_assistants/data/rag/chunkers.py`
- lines: 237-318
- chunk: 4/9

```

        metadata = metadata or {}
        
        sentences = self._split_into_sentences(text)
        
        if not sentences:
            return []
        
        chunks = []
        current_sentences = []
        current_length = 0
        chunk_start = sentences[0][1]
        
        for sentence, start, end in sentences:
            sentence_length = len(sentence)
            
            if current_length + sentence_length + 1 <= self.chunk_size:
                current_sentences.append(sentence)
                current_length += sentence_length + 1
            else:
                if current_sentences:
                    chunk_text = " ".join(current_sentences)
                    chunks.append(Chunk(
                        content=chunk_text,
                        index=len(chunks),
                        start_char=chunk_start,
                        end_char=chunk_start + len(chunk_text),
                        metadata=metadata.copy(),
                    ))
                    
                    # Apply overlap - keep last few sentences
                    overlap_sentences = []
                    overlap_length = 0
                    for s in reversed(current_sentences):
                        if overlap_length + len(s) <= self.chunk_overlap:
                            overlap_sentences.insert(0, s)
                            overlap_length += len(s) + 1
                        else:
                            break
                    
                    current_sentences = overlap_sentences + [sentence]
                    current_length = sum(len(s) + 1 for s in current_sentences)
                    chunk_start = start - overlap_length
                else:
                    current_sentences = [sentence]
                    current_length = sentence_length
                    chunk_start = start
        
        if current_sentences:
            chunk_text = " ".join(current_sentences)
            chunks.append(Chunk(
                content=chunk_text,
                index=len(chunks),
                start_char=chunk_start,
                end_char=chunk_start + len(chunk_text),
                metadata=metadata.copy(),
            ))
        
        return self._merge_small_chunks(chunks)


class MarkdownChunker(Chunker):
    """
    Chunker for Markdown documents that respects structure.
    
    Splits on headers and preserves hierarchy information.
    """
    
    HEADER_PATTERN = re.compile(r'^(#{1,6})\s+(.+)$', re.MULTILINE)
    
    def __init__(
        self,
        chunk_size: int = 1024,
        chunk_overlap: int = 128,
        include_headers: bool = True,
        **kwargs,
    ):
        super().__init__(chunk_size, chunk_overlap, **kwargs)
        self.include_headers = include_headers
    
    def chunk(self, text: str, metadata: Optional[Dict[str, Any]] = None) -> List[Chunk]:
```
