# Chunk: f2b6e772c023_3

- source: `src/agentic_assistants/data/catalog.py`
- lines: 230-321
- chunk: 4/20

```
   def create_config(self, name: str) -> DatasetConfig:
        """Create a DatasetConfig for a matched name."""
        params = self.extract_params(name)
        
        # Substitute parameters in filepath
        filepath = self.filepath_template
        for key, value in params.items():
            filepath = filepath.replace(f"{{{key}}}", value)
        
        return DatasetConfig(
            name=name,
            type=self.type,
            filepath=filepath,
            load_args=self.load_args.copy(),
            save_args=self.save_args.copy(),
            versioned=self.versioned,
            layer=params.get("layer"),
        )


# ============================================================================
# Catalog Entry Types
# ============================================================================

@dataclass
class CatalogEntry:
    """Base class for catalog entries."""
    id: str
    name: str
    entry_type: str  # source, table, column
    source_id: str
    description: str = ""
    tags: List[str] = field(default_factory=list)
    metadata: Dict[str, Any] = field(default_factory=dict)
    created_at: datetime = field(default_factory=datetime.utcnow)
    updated_at: datetime = field(default_factory=datetime.utcnow)
    
    def to_dict(self) -> Dict[str, Any]:
        return {
            "id": self.id,
            "name": self.name,
            "entry_type": self.entry_type,
            "source_id": self.source_id,
            "description": self.description,
            "tags": self.tags,
            "metadata": self.metadata,
            "created_at": self.created_at.isoformat(),
            "updated_at": self.updated_at.isoformat(),
        }


@dataclass
class TableEntry(CatalogEntry):
    """Catalog entry for a table or collection."""
    schema_name: Optional[str] = None
    columns: List["ColumnEntry"] = field(default_factory=list)
    row_count: Optional[int] = None
    size_bytes: Optional[int] = None
    
    def __post_init__(self):
        self.entry_type = "table"
    
    def to_dict(self) -> Dict[str, Any]:
        base = super().to_dict()
        base.update({
            "schema_name": self.schema_name,
            "columns": [c.to_dict() for c in self.columns],
            "row_count": self.row_count,
            "size_bytes": self.size_bytes,
        })
        return base


@dataclass
class ColumnEntry(CatalogEntry):
    """Catalog entry for a column or field."""
    table_id: str = ""
    data_type: str = ""
    nullable: bool = True
    primary_key: bool = False
    sample_values: List[Any] = field(default_factory=list)
    
    def __post_init__(self):
        self.entry_type = "column"
    
    def to_dict(self) -> Dict[str, Any]:
        base = super().to_dict()
        base.update({
            "table_id": self.table_id,
            "data_type": self.data_type,
            "nullable": self.nullable,
```
