# Chunk: 456d9c708749_2

- source: `src/agentic_assistants/data/datasets/kaggle_dataset.py`
- lines: 157-233
- chunk: 3/6

```
  if self.dataset_ref:
            # datasets/username/dataset-name/
            return self.download_dir / "datasets" / self.dataset_ref.replace("/", "_")
        else:
            # competitions/competition-name/
            return self.download_dir / "competitions" / self.competition
    
    def _download_dataset(self, api, force: bool = False) -> Path:
        """Download dataset files."""
        download_path = self._get_download_path()
        download_path.mkdir(parents=True, exist_ok=True)
        
        # Check if already downloaded
        if not force and any(download_path.iterdir()):
            logger.debug(f"Dataset already downloaded: {download_path}")
            return download_path
        
        unzip = self._load_args.get("unzip", True)
        
        if self.dataset_ref:
            logger.info(f"Downloading Kaggle dataset: {self.dataset_ref}")
            api.dataset_download_files(
                self.dataset_ref,
                path=str(download_path),
                unzip=unzip,
            )
        else:
            logger.info(f"Downloading Kaggle competition data: {self.competition}")
            api.competition_download_files(
                self.competition,
                path=str(download_path),
            )
            
            # Manually unzip if needed
            if unzip:
                for zip_file in download_path.glob("*.zip"):
                    with zipfile.ZipFile(zip_file, "r") as zf:
                        zf.extractall(download_path)
                    zip_file.unlink()
        
        return download_path
    
    def _find_file(self, download_path: Path) -> Path:
        """Find the specific file to load."""
        if self.file_name:
            file_path = download_path / self.file_name
            if not file_path.exists():
                # Search recursively
                matches = list(download_path.rglob(self.file_name))
                if matches:
                    file_path = matches[0]
                else:
                    raise DatasetError(f"File not found: {self.file_name}")
            return file_path
        else:
            # Return first data file found
            for ext in [".csv", ".parquet", ".json", ".xlsx"]:
                files = list(download_path.glob(f"*{ext}"))
                if files:
                    return files[0]
            
            # Return first file
            files = list(download_path.iterdir())
            if files:
                return files[0]
            
            raise DatasetError(f"No files found in {download_path}")
    
    def _load_file(self, file_path: Path) -> Any:
        """Load data from the file based on extension."""
        suffix = file_path.suffix.lower()
        
        if suffix == ".csv":
            try:
                import pandas as pd
                csv_args = {
```
