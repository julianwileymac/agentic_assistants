# Chunk: f8d6390d57a9_54

- source: `webui/.next/dev/server/chunks/ssr/[root-of-the-server]__630d161a._.js.map`
- lines: 21-21
- chunk: 55/62

```
.has(id)) {\r\n        newSet.delete(id);\r\n      } else {\r\n        newSet.add(id);\r\n      }\r\n      return { selectedComponents: newSet };\r\n    }),\r\n\r\n  selectAllProjects: (ids) => set({ selectedProjects: new Set(ids) }),\r\n  selectAllAgents: (ids) => set({ selectedAgents: new Set(ids) }),\r\n  selectAllFlows: (ids) => set({ selectedFlows: new Set(ids) }),\r\n  selectAllComponents: (ids) => set({ selectedComponents: new Set(ids) }),\r\n\r\n  clearSelection: () =>\r\n    set({\r\n      selectedProjects: new Set(),\r\n      selectedAgents: new Set(),\r\n      selectedFlows: new Set(),\r\n      selectedComponents: new Set(),\r\n    }),\r\n}));\r\n\r\n// ============================================================================\r\n// Draft Store (for unsaved changes)\r\n// ============================================================================\r\n\r\ninterface Draft {\r\n  type: 'project' | 'agent' | 'flow' | 'component';\r\n  id?: string; // undefined for new items\r\n  data: Partial<Project | Agent | Flow | Component>;\r\n  isDirty: boolean;\r\n  savedAt?: Date;\r\n}\r\n\r\ninterface DraftStore {\r\n  drafts: Map<string, Draft>;\r\n  \r\n  setDraft: (key: string, draft: Draft) => void;\r\n  getDraft: (key: string) => Draft | undefined;\r\n  removeDraft: (key: string) => void;\r\n  markDirty: (key: string) => void;\r\n  markClean: (key: string) => void;\r\n  clearAllDrafts: () => void;\r\n  hasDirtyDrafts: () => boolean;\r\n}\r\n\r\nexport const useDraftStore = create<DraftStore>((set, get) => ({\r\n  drafts: new Map(),\r\n\r\n  setDraft: (key, draft) =>\r\n    set((state) => {\r\n      const newDrafts = new Map(state.drafts);\r\n      newDrafts.set(key, draft);\r\n      return { drafts: newDrafts };\r\n    }),\r\n\r\n  getDraft: (key) => get().drafts.get(key),\r\n\r\n  removeDraft: (key) =>\r\n    set((state) => {\r\n      const newDrafts = new Map(state.drafts);\r\n      newDrafts.delete(key);\r\n      return { drafts: newDrafts };\r\n    }),\r\n\r\n  markDirty: (key) =>\r\n    set((state) => {\r\n      const draft = state.drafts.get(key);\r\n      if (draft) {\r\n        const newDrafts = new Map(state.drafts);\r\n        newDrafts.set(key, { ...draft, isDirty: true });\r\n        return { drafts: newDrafts };\r\n      }\r\n      return state;\r\n    }),\r\n\r\n  markClean: (key) =>\r\n    set((state) => {\r\n      const draft = state.drafts.get(key);\r\n      if (draft) {\r\n        const newDrafts = new Map(state.drafts);\r\n        newDrafts.set(key, { ...draft, isDirty: false, savedAt: new Date() });\r\n        return { drafts: newDrafts };\r\n      }\r\n      return state;\r\n    }),\r\n\r\n  clearAllDrafts: () => set({ drafts: new Map() }),\r\n\r\n  hasDirtyDrafts: () => {\r\n    const drafts = get().drafts;\r\n    for (const draft of drafts.values()) {\r\n      if (draft.isDirty) return true;\r\n    }\r\n    return false;\r\n  },\r\n}));\r\n\r\n// ========================================================================
```
