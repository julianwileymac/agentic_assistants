# Chunk: c75ee782b9c8_5

- source: `src/agentic_assistants/data/training/tagging.py`
- lines: 382-472
- chunk: 6/8

```
gs:
            logger.warning(f"Tag {tag_id} not found")
            return None
        
        # Check if already assigned
        for assignment in self._assignments.values():
            if (assignment.tag_id == tag_id and 
                assignment.resource_type == resource_type and
                assignment.resource_id == resource_id):
                return assignment
        
        assignment_id = f"assign-{uuid.uuid4().hex[:8]}"
        assignment = TagAssignment(
            id=assignment_id,
            tag_id=tag_id,
            resource_type=resource_type,
            resource_id=resource_id,
            confidence=confidence,
            notes=notes,
            assigned_by=assigned_by,
        )
        
        self._assignments[assignment_id] = assignment
        
        # Update resource index
        if resource_id not in self._resource_tags:
            self._resource_tags[resource_id] = set()
        self._resource_tags[resource_id].add(tag_id)
        
        # Update tag usage count
        self._tags[tag_id].usage_count += 1
        
        self._save_data()
        
        return assignment
    
    def assign_tags(
        self,
        tag_ids: List[str],
        resource_type: str,
        resource_id: str,
        assigned_by: Optional[str] = None,
    ) -> List[TagAssignment]:
        """Assign multiple tags to a resource."""
        assignments = []
        for tag_id in tag_ids:
            assignment = self.assign_tag(
                tag_id=tag_id,
                resource_type=resource_type,
                resource_id=resource_id,
                assigned_by=assigned_by,
            )
            if assignment:
                assignments.append(assignment)
        return assignments
    
    def remove_tag(
        self,
        tag_id: str,
        resource_type: str,
        resource_id: str,
    ) -> bool:
        """Remove a tag from a resource."""
        assignment_to_remove = None
        
        for assignment in self._assignments.values():
            if (assignment.tag_id == tag_id and
                assignment.resource_type == resource_type and
                assignment.resource_id == resource_id):
                assignment_to_remove = assignment
                break
        
        if not assignment_to_remove:
            return False
        
        del self._assignments[assignment_to_remove.id]
        
        # Update resource index
        if resource_id in self._resource_tags:
            self._resource_tags[resource_id].discard(tag_id)
        
        # Update tag usage count
        if tag_id in self._tags:
            self._tags[tag_id].usage_count = max(0, self._tags[tag_id].usage_count - 1)
        
        self._save_data()
        return True
    
    def get_resource_tags(self, resource_id: str) -> List[DataTag]:
        """Get all tags for a resource."""
```
