# Chunk: 4e38ddc67619_5

- source: `webui/src/lib/types.ts`
- lines: 573-659
- chunk: 6/6

```
=========================================================
// API Response Types
// ============================================================================

export interface ApiResponse<T> {
  data?: T;
  error?: string;
  status: number;
}

export interface PaginatedResponse<T> {
  items: T[];
  total: number;
  page: number;
  page_size: number;
  has_more: boolean;
}

// ============================================================================
// Flow YAML Schema Types
// ============================================================================

export interface FlowDefinition {
  name: string;
  description?: string;
  type: FlowType;
  agents: FlowAgentRef[];
  tasks: FlowTask[];
  process?: 'sequential' | 'hierarchical';
  verbose?: boolean;
  memory?: boolean;
}

export interface FlowAgentRef {
  id?: string;       // Reference to existing agent
  name: string;
  role?: string;
  goal?: string;
  backstory?: string;
  tools?: string[];
  model?: string;
}

export interface FlowTask {
  name: string;
  description: string;
  agent: string;     // Agent name reference
  expected_output: string;
  context?: string[];
  async_execution?: boolean;
}

// ============================================================================
// Form & UI Types
// ============================================================================

export interface FormField {
  name: string;
  label: string;
  type: 'text' | 'textarea' | 'select' | 'multiselect' | 'code' | 'yaml';
  placeholder?: string;
  required?: boolean;
  options?: { value: string; label: string }[];
  defaultValue?: string;
}

export interface TableColumn<T> {
  key: keyof T | string;
  label: string;
  sortable?: boolean;
  render?: (value: unknown, row: T) => React.ReactNode;
}

// ============================================================================
// Auth Types
// ============================================================================

export interface AuthState {
  isAuthenticated: boolean;
  apiKey?: string;
  user?: {
    id: string;
    name: string;
  };
}
```
