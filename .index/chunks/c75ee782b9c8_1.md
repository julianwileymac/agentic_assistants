# Chunk: c75ee782b9c8_1

- source: `src/agentic_assistants/data/training/tagging.py`
- lines: 94-165
- chunk: 2/8

```
""Assignment of a tag to a resource."""
    
    id: str
    tag_id: str
    resource_type: str  # dataset, sample, model, etc.
    resource_id: str
    
    # Optional context
    confidence: Optional[float] = None  # For auto-tagging
    notes: Optional[str] = None
    
    # Metadata
    assigned_at: str = field(default_factory=lambda: datetime.utcnow().isoformat())
    assigned_by: Optional[str] = None  # user or "auto"
    
    def to_dict(self) -> Dict[str, Any]:
        return {
            "id": self.id,
            "tag_id": self.tag_id,
            "resource_type": self.resource_type,
            "resource_id": self.resource_id,
            "confidence": self.confidence,
            "notes": self.notes,
            "assigned_at": self.assigned_at,
            "assigned_by": self.assigned_by,
        }


class DataTagManager:
    """
    Manager for data tags.
    
    Provides:
    - Tag CRUD operations
    - Tag assignment to resources
    - Tag-based filtering and search
    - Tag statistics
    """
    
    # Predefined system tags
    SYSTEM_TAGS = [
        DataTag(id="tag-instruct", name="instruct", category=TagCategory.DATA_TYPE,
                description="Instruction-following data", color="#4CAF50"),
        DataTag(id="tag-preference", name="preference", category=TagCategory.DATA_TYPE,
                description="Preference/comparison data for RLHF/DPO", color="#2196F3"),
        DataTag(id="tag-completion", name="completion", category=TagCategory.DATA_TYPE,
                description="Text completion data", color="#9C27B0"),
        DataTag(id="tag-chat", name="chat", category=TagCategory.DATA_TYPE,
                description="Conversational data", color="#FF9800"),
        DataTag(id="tag-high-quality", name="high_quality", category=TagCategory.QUALITY,
                description="High quality, verified data", color="#4CAF50"),
        DataTag(id="tag-needs-review", name="needs_review", category=TagCategory.QUALITY,
                description="Requires manual review", color="#FFC107"),
        DataTag(id="tag-synthetic", name="synthetic", category=TagCategory.SOURCE,
                description="Synthetically generated data", color="#00BCD4"),
        DataTag(id="tag-human", name="human", category=TagCategory.SOURCE,
                description="Human-created data", color="#E91E63"),
        DataTag(id="tag-code", name="code", category=TagCategory.DOMAIN,
                description="Programming and code-related", color="#607D8B"),
        DataTag(id="tag-general", name="general", category=TagCategory.DOMAIN,
                description="General domain data", color="#795548"),
    ]
    
    def __init__(self, storage_path: Optional[str] = None):
        """
        Initialize the tag manager.
        
        Args:
            storage_path: Path to store tag data
        """
        self.storage_path = Path(storage_path or "./data/tags")
```
