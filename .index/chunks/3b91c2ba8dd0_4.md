# Chunk: 3b91c2ba8dd0_4

- source: `src/agentic_assistants/data/sources.py`
- lines: 321-395
- chunk: 5/13

```
File Store Handler
# ============================================================================

class FileStoreHandler(DataSourceHandler):
    """Handler for file storage sources (local, S3, Azure Blob)."""
    
    @property
    def source_type(self) -> str:
        return "file_store"
    
    @property
    def supported_drivers(self) -> List[str]:
        return ["local", "s3", "azure_blob", "gcs"]
    
    def validate_config(self, config: Dict[str, Any]) -> tuple[bool, str]:
        """Validate file store configuration."""
        driver = config.get("driver", "local")
        
        if driver not in self.supported_drivers:
            return False, f"Unsupported driver: {driver}. Supported: {self.supported_drivers}"
        
        if driver == "local":
            if not config.get("path"):
                return False, "Local file store requires 'path'"
        elif driver == "s3":
            required = ["bucket"]
            missing = [r for r in required if not config.get(r)]
            if missing:
                return False, f"S3 requires: {missing}"
        elif driver == "azure_blob":
            required = ["container", "connection_string"]
            missing = [r for r in required if not config.get(r)]
            if missing:
                return False, f"Azure Blob requires: {missing}"
        
        return True, ""
    
    def test_connection(self, config: Dict[str, Any]) -> ConnectionResult:
        """Test file store connection."""
        import time
        
        valid, error = self.validate_config(config)
        if not valid:
            return ConnectionResult(success=False, message=error)
        
        driver = config.get("driver", "local")
        start = time.time()
        
        try:
            if driver == "local":
                path = Path(config.get("path", "."))
                if path.exists():
                    latency = (time.time() - start) * 1000
                    return ConnectionResult(
                        success=True,
                        message=f"Path exists: {path}",
                        latency_ms=latency,
                        details={"file_count": len(list(path.iterdir())) if path.is_dir() else 1},
                    )
                return ConnectionResult(success=False, message=f"Path does not exist: {path}")
            
            elif driver == "s3":
                try:
                    import boto3
                    
                    s3 = boto3.client(
                        "s3",
                        aws_access_key_id=config.get("access_key"),
                        aws_secret_access_key=config.get("secret_key"),
                        region_name=config.get("region"),
                    )
                    bucket = config.get("bucket")
                    s3.head_bucket(Bucket=bucket)
                    latency = (time.time() - start) * 1000
```
