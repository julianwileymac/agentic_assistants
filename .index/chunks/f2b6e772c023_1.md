# Chunk: f2b6e772c023_1

- source: `src/agentic_assistants/data/catalog.py`
- lines: 88-166
- chunk: 2/20

```
ataset,
        )
        
        register_dataset_type("pandas.CSVDataset", CSVDataset)
        register_dataset_type("pandas.ParquetDataset", ParquetDataset)
        register_dataset_type("pandas.JSONDataset", JSONDataset)
        register_dataset_type("pandas.ExcelDataset", ExcelDataset)
        register_dataset_type("pickle.PickleDataset", PickleDataset)
        register_dataset_type("api.APIDataset", APIDataset)
        
        # Short aliases
        register_dataset_type("csv", CSVDataset)
        register_dataset_type("parquet", ParquetDataset)
        register_dataset_type("json", JSONDataset)
        register_dataset_type("excel", ExcelDataset)
        register_dataset_type("pickle", PickleDataset)
        register_dataset_type("api", APIDataset)
    except ImportError as e:
        logger.debug(f"Could not register all builtin types: {e}")
    
    # Register remote data source types
    _register_remote_types()


def _register_remote_types():
    """Register remote data source dataset types."""
    try:
        from agentic_assistants.data.datasets import (
            URLDataset,
            WebsiteDataset,
            KaggleDataset,
            S3PublicDataset,
            RSSDataset,
            ScraperDataset,
        )
        
        # Full names
        register_dataset_type("remote.URLDataset", URLDataset)
        register_dataset_type("remote.WebsiteDataset", WebsiteDataset)
        register_dataset_type("remote.KaggleDataset", KaggleDataset)
        register_dataset_type("remote.S3PublicDataset", S3PublicDataset)
        register_dataset_type("remote.RSSDataset", RSSDataset)
        register_dataset_type("remote.ScraperDataset", ScraperDataset)
        
        # Short aliases
        register_dataset_type("url", URLDataset)
        register_dataset_type("website", WebsiteDataset)
        register_dataset_type("kaggle", KaggleDataset)
        register_dataset_type("s3_public", S3PublicDataset)
        register_dataset_type("rss", RSSDataset)
        register_dataset_type("scraper", ScraperDataset)
        
        logger.debug("Registered remote dataset types")
    except ImportError as e:
        logger.debug(f"Could not register remote types: {e}")


_register_builtin_types()


# ============================================================================
# Catalog Configuration
# ============================================================================

@dataclass
class DatasetConfig:
    """Configuration for a single dataset in the catalog."""
    name: str
    type: str
    filepath: Optional[str] = None
    load_args: Dict[str, Any] = field(default_factory=dict)
    save_args: Dict[str, Any] = field(default_factory=dict)
    credentials: Optional[str] = None  # Reference to credentials key
    versioned: bool = False
    layer: Optional[str] = None
    metadata: Dict[str, Any] = field(default_factory=dict)
    
    @classmethod
```
