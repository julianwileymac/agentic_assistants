# Chunk: ede33c1e9246_5

- source: `src/agentic_assistants/data/datasets/url_dataset.py`
- lines: 371-440
- chunk: 6/8

```
         all_headers["If-None-Match"] = self._etag
        if self._last_modified:
            all_headers["If-Modified-Since"] = self._last_modified
        
        last_error = None
        
        for attempt in range(retries):
            try:
                start_time = time.time()
                
                with httpx.Client(timeout=timeout, verify=verify_ssl) as client:
                    if self.method == "GET":
                        response = client.get(self.url, headers=all_headers, params=self.params)
                    elif self.method == "POST":
                        response = client.post(
                            self.url, 
                            headers=all_headers, 
                            params=self.params,
                            json=self.data
                        )
                    else:
                        raise DatasetError(f"Unsupported HTTP method: {self.method}")
                
                elapsed_ms = (time.time() - start_time) * 1000
                
                # Handle 304 Not Modified
                if response.status_code == 304 and self._last_result:
                    logger.debug(f"URL not modified: {self.url}")
                    return self._last_result
                
                # Check for errors
                response.raise_for_status()
                
                # Store cache headers
                self._etag = response.headers.get("ETag")
                self._last_modified = response.headers.get("Last-Modified")
                
                # Detect content type
                actual_content_type = self.content_type
                if actual_content_type == "auto":
                    actual_content_type = self._detect_content_type(
                        dict(response.headers), self.url
                    )
                
                # Parse response
                encoding = response.encoding or "utf-8"
                parsed_content = self._parse_response(
                    response.content, 
                    actual_content_type, 
                    encoding
                )
                
                result = URLFetchResult(
                    content=parsed_content,
                    status_code=response.status_code,
                    headers=dict(response.headers),
                    url=str(response.url),
                    content_type=actual_content_type,
                    encoding=encoding,
                    elapsed_ms=elapsed_ms,
                )
                
                self._last_result = result
                return result
                
            except httpx.HTTPStatusError as e:
                last_error = DatasetError(f"HTTP error {e.response.status_code}: {e}")
                logger.warning(f"HTTP error on attempt {attempt + 1}: {e}")
            except httpx.RequestError as e:
```
