# Chunk: 6fc3a531e405_2

- source: `src/agentic_assistants/data/datasets/base.py`
- lines: 164-255
- chunk: 3/6

```
the filepath."""
        if "://" in filepath:
            return filepath.split("://")[0]
        return "file"
    
    @property
    def filepath(self) -> str:
        """Get the filepath, resolving version if needed."""
        if not self._versioned:
            return self._filepath
        
        # For versioned datasets, return the versioned path
        version = self._get_load_version() if self._version else None
        if version:
            return self._get_versioned_path(version)
        return self._filepath
    
    def _get_versioned_path(self, version: str) -> str:
        """Get the path for a specific version."""
        base_path = Path(self._filepath)
        # Insert version directory before the filename
        return str(base_path.parent / base_path.stem / version / base_path.name)
    
    def _get_load_version(self) -> Optional[str]:
        """Get the version to load."""
        if self._version and self._version.load_version:
            return self._version.load_version
        
        # Find latest version
        if self._versioned:
            versions = self._list_versions()
            if versions:
                return sorted(versions)[-1]
        return None
    
    def _get_save_version(self) -> str:
        """Get the version to save."""
        if self._version and self._version.save_version:
            return self._version.save_version
        return Version.generate_timestamp()
    
    def _list_versions(self) -> list[str]:
        """List all available versions."""
        if not self._versioned:
            return []
        
        base_path = Path(self._filepath)
        version_dir = base_path.parent / base_path.stem
        
        if not version_dir.exists():
            return []
        
        return [
            d.name for d in version_dir.iterdir()
            if d.is_dir() and not d.name.startswith(".")
        ]
    
    @property
    def versioned(self) -> bool:
        """Whether this dataset is versioned."""
        return self._versioned
    
    @property
    def metadata(self) -> DatasetMetadata:
        """Get dataset metadata."""
        return self._metadata
    
    def load(self) -> T:
        """
        Load data from the dataset.
        
        Returns:
            The loaded data
            
        Raises:
            DatasetNotFoundError: If the dataset doesn't exist
            DatasetError: If loading fails
        """
        logger.debug(f"Loading dataset from: {self.filepath}")
        
        try:
            data = self._load()
            self._update_metadata_on_load(data)
            return data
        except FileNotFoundError as e:
            raise DatasetNotFoundError(
                f"Dataset not found at {self.filepath}"
            ) from e
        except Exception as e:
            raise DatasetError(f"Failed to load dataset: {e}") from e
    
```
