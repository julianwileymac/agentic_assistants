# Chunk: 7e56b14d8e11_5

- source: `src/agentic_assistants/data/datasets/website_dataset.py`
- lines: 377-463
- chunk: 6/7

```
   """Scrape a single page."""
        # Fetch HTML
        html, status_code, elapsed_ms = self._fetch_html(url)
        
        # Parse HTML
        soup = self._get_parser(html)
        
        # Get base element if specified
        base_element = None
        if self.base_selector:
            base_element = soup.select_one(self.base_selector)
        
        # Extract data using selectors
        data = {}
        for name, config in self.selectors.items():
            data[name] = self._extract_with_selector(soup, config, base_element)
        
        # Extract links if requested
        links = []
        if self.extract_links or self.follow_links:
            links = self._extract_links(soup, url)
        
        return ScrapedPage(
            url=url,
            data=data,
            html=html,
            status_code=status_code,
            elapsed_ms=elapsed_ms,
            links=links,
        )
    
    def _load(self) -> Dict[str, Any]:
        """Load data by scraping the website."""
        self._scraped_pages = []
        self._visited_urls = set()
        
        # URLs to visit
        to_visit = [self.url]
        delay = self._load_args.get("delay_between_requests", 1.0)
        
        while to_visit and len(self._visited_urls) < self.max_pages:
            url = to_visit.pop(0)
            
            # Skip if already visited
            if url in self._visited_urls:
                continue
            
            self._visited_urls.add(url)
            
            try:
                # Scrape the page
                page = self._scrape_page(url)
                self._scraped_pages.append(page)
                
                logger.info(f"Scraped: {url} ({page.elapsed_ms:.0f}ms)")
                
                # Add links to visit
                if self.follow_links and page.links:
                    for link in page.links:
                        if link not in self._visited_urls and link not in to_visit:
                            to_visit.append(link)
                
                # Rate limiting
                if to_visit and delay > 0:
                    time.sleep(delay)
                    
            except Exception as e:
                logger.error(f"Failed to scrape {url}: {e}")
        
        # Return results
        if len(self._scraped_pages) == 1:
            # Single page - return data directly
            page = self._scraped_pages[0]
            result = page.data.copy()
            result["_url"] = page.url
            result["_scraped_at"] = page.scraped_at.isoformat()
            if self.extract_links:
                result["_links"] = page.links
            return result
        else:
            # Multiple pages - return list
            return {
                "pages": [
                    {
                        **page.data,
                        "_url": page.url,
```
