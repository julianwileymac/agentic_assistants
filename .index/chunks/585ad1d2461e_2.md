# Chunk: 585ad1d2461e_2

- source: `src/agentic_assistants/data/datasets/rss_dataset.py`
- lines: 174-257
- chunk: 3/6

```
        self._last_fetch: Optional[datetime] = None
        self._last_entry_ids: set = set()
        self._feed_info: Optional[FeedInfo] = None
    
    def _get_feedparser(self):
        """Get feedparser module."""
        try:
            import feedparser
            return feedparser
        except ImportError:
            raise DatasetError(
                "feedparser is required for RSS feeds. "
                "Install with: pip install feedparser"
            )
    
    def _parse_datetime(self, time_struct) -> Optional[datetime]:
        """Parse feedparser time struct to datetime."""
        if not time_struct:
            return None
        
        try:
            from time import mktime
            return datetime.fromtimestamp(mktime(time_struct))
        except Exception:
            return None
    
    def _sanitize_html(self, html: str) -> str:
        """Remove HTML tags from content."""
        if not html:
            return ""
        
        try:
            from bs4 import BeautifulSoup
            soup = BeautifulSoup(html, "html.parser")
            return soup.get_text(separator=" ", strip=True)
        except ImportError:
            # Simple regex fallback
            import re
            clean = re.sub(r"<[^>]+>", " ", html)
            return " ".join(clean.split())
    
    def _parse_entry(self, entry) -> FeedEntry:
        """Parse a feedparser entry into FeedEntry."""
        # Get unique ID
        entry_id = entry.get("id") or entry.get("link") or entry.get("title", "")
        
        # Get dates
        published = self._parse_datetime(entry.get("published_parsed"))
        updated = self._parse_datetime(entry.get("updated_parsed"))
        
        # Get content
        content = ""
        if entry.get("content"):
            content = entry.content[0].get("value", "")
        elif entry.get("summary"):
            content = entry.summary
        
        # Sanitize if requested
        if self._load_args.get("sanitize_html", True):
            summary = self._sanitize_html(entry.get("summary", ""))
            if self._load_args.get("include_content", True):
                content = self._sanitize_html(content)
        else:
            summary = entry.get("summary", "")
        
        # Get categories/tags
        categories = [tag.get("term", "") for tag in entry.get("tags", [])]
        
        # Get enclosures (media attachments)
        enclosures = []
        for enc in entry.get("enclosures", []):
            enclosures.append({
                "url": enc.get("href", enc.get("url", "")),
                "type": enc.get("type", ""),
                "length": enc.get("length", ""),
            })
        
        # Get author
        author = entry.get("author", "")
        if not author and entry.get("authors"):
            author = entry.authors[0].get("name", "")
        
        return FeedEntry(
```
