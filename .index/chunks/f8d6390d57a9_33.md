# Chunk: f8d6390d57a9_33

- source: `webui/.next/dev/server/chunks/ssr/[root-of-the-server]__630d161a._.js.map`
- lines: 17-17
- chunk: 34/62

```
er;\n      cpu_request?: string;\n      cpu_limit?: string;\n      memory_request?: string;\n      memory_limit?: string;\n      context_length?: number;\n    } }) => {\n      return apiFetch<K8sDeploymentInfo>(url, {\n        method: 'POST',\n        body: JSON.stringify(arg),\n      });\n    }\n  );\n}\n\nexport function useDeleteModelDeployment() {\n  return useSWRMutation(\n    '/api/v1/kubernetes/models',\n    async (_url: string, { arg }: { arg: { name: string; namespace?: string } }) => {\n      const params = arg.namespace ? `?namespace=${arg.namespace}` : '';\n      return apiFetch<{ status: string }>(`/api/v1/kubernetes/models/${arg.name}${params}`, {\n        method: 'DELETE',\n      });\n    }\n  );\n}\n\nexport function useScaleModelDeployment() {\n  return useSWRMutation(\n    '/api/v1/kubernetes/models',\n    async (_url: string, { arg }: { arg: { name: string; replicas: number; namespace?: string } }) => {\n      const params = arg.namespace ? `?namespace=${arg.namespace}` : '';\n      return apiFetch<K8sDeploymentInfo>(`/api/v1/kubernetes/models/${arg.name}/scale${params}`, {\n        method: 'POST',\n        body: JSON.stringify({ replicas: arg.replicas }),\n      });\n    }\n  );\n}\n\nexport function useModelEndpoint() {\n  return useSWRMutation(\n    '/api/v1/kubernetes/models',\n    async (_url: string, { arg }: { arg: { name: string; namespace?: string } }) => {\n      const params = arg.namespace ? `?namespace=${arg.namespace}` : '';\n      return apiFetch<{ internal_endpoint?: string; external_endpoint?: string }>(`/api/v1/kubernetes/models/${arg.name}/endpoint${params}`, {\n        method: 'GET',\n      });\n    }\n  );\n}\n\nexport function useSetModelAsCodeAssistant() {\n  return useSWRMutation(\n    '/api/v1/kubernetes/models',\n    async (_url: string, { arg }: { arg: { name: string; namespace?: string } }) => {\n      const params = arg.namespace ? `?namespace=${arg.namespace}` : '';\n      return apiFetch<{ success: boolean; endpoint?: string; model_name?: string; message?: string }>(`/api/v1/kubernetes/models/${arg.name}/use${params}`, {\n        method: 'POST',\n      });\n    }\n  );\n}\n\n// Storage\nexport function useStorageStatus() {\n  return useSWR<{ connected: boolean; endpoint?: string; buckets?: string[]; error?: string }>(\n    '/api/v1/kubernetes/storage/status',\n    fetcher,\n    defaultSwrConfig\n  );\n}\n\nexport function useStorageBuckets() {\n  return useSWR<{ buckets: string[] }>(\n    '/api/v1/kubernetes/storage/buckets',\n    fetcher,\n    defaultSwrConfig\n  );\n}\n\nexport function useStorageObjects(bucket: string, prefix?: string) {\n  const params = new URLSearchParams();\n  if (prefix) params.set('prefix', prefix);\n  params.set('recursive', 'false');\n  \n  return useSWR<{ objects: Array<{ name: string; size: number; last_modified: string | null; is_dir: boolean }>; bucket: string; prefix?: string }>(\n    bucket ? `/api/v1/kubernetes/storage/buckets/${bucket}/objects?${params.toString(
```
