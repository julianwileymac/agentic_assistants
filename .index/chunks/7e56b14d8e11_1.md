# Chunk: 7e56b14d8e11_1

- source: `src/agentic_assistants/data/datasets/website_dataset.py`
- lines: 89-172
- chunk: 2/7

```

    - Link discovery for crawling
    - JavaScript rendering (via playwright, optional)
    - Rate limiting and polite crawling
    
    Attributes:
        url: The URL to scrape
        selectors: Dictionary mapping field names to selectors
        render_js: Whether to render JavaScript
        follow_links: Pattern for links to follow
        max_pages: Maximum pages to crawl
        
    Example:
        >>> # Simple scraping
        >>> dataset = WebsiteDataset(
        ...     url="https://news.ycombinator.com",
        ...     selectors={
        ...         "titles": {"selector": "a.titlelink", "multiple": True},
        ...         "scores": {"selector": "span.score", "multiple": True, "transform": "int"},
        ...     }
        ... )
        >>> data = dataset.load()
        
        >>> # With JavaScript rendering
        >>> dataset = WebsiteDataset(
        ...     url="https://spa-example.com",
        ...     selectors={"content": "#app"},
        ...     render_js=True
        ... )
    """
    
    DEFAULT_LOAD_ARGS: Dict[str, Any] = {
        "timeout": 30,
        "retries": 3,
        "delay_between_requests": 1.0,
        "verify_ssl": True,
        "user_agent": "Mozilla/5.0 (compatible; AgenticBot/1.0)",
    }
    
    def __init__(
        self,
        url: str,
        selectors: Optional[Dict[str, Union[str, Dict, SelectorConfig]]] = None,
        render_js: bool = False,
        follow_links: Optional[str] = None,
        max_pages: int = 1,
        extract_links: bool = False,
        base_selector: Optional[str] = None,
        load_args: Optional[Dict[str, Any]] = None,
        save_args: Optional[Dict[str, Any]] = None,
        credentials: Optional[Dict[str, Any]] = None,
        cache_dir: Optional[str] = None,
        metadata: Optional[DatasetMetadata] = None,
        **kwargs,
    ):
        """
        Initialize the website dataset.
        
        Args:
            url: URL to scrape
            selectors: Dict mapping field names to selector configs
            render_js: Whether to render JavaScript (requires playwright)
            follow_links: Regex pattern for links to follow for multi-page
            max_pages: Maximum pages to scrape
            extract_links: Whether to extract all links from page
            base_selector: Base selector to scope all other selectors
            load_args: Loading arguments
            save_args: Saving arguments
            credentials: Credentials for authenticated pages
            cache_dir: Directory for response cache
            metadata: Dataset metadata
        """
        super().__init__(
            filepath=url,
            load_args=load_args,
            save_args=save_args,
            credentials=credentials,
            metadata=metadata,
        )
        
        self.url = url
        self.render_js = render_js
        self.follow_links = follow_links
```
