# Chunk: 7f1aa0ee653a_1

- source: `.venv-lab/Lib/site-packages/debugpy/_vendored/pydevd/pydevd_plugins/extensions/types/pydevd_plugin_numpy_types.py`
- lines: 71-91
- chunk: 2/2

```
["min"] = obj.min()
                ret["max"] = obj.max()
            else:
                ret["min"] = "not a numeric object"
                ret["max"] = "not a numeric object"
        ret["shape"] = obj.shape
        ret["dtype"] = obj.dtype
        ret["size"] = obj.size
        try:
            ret["[0:%s] " % (len(obj))] = list(obj[0 : pydevd_constants.PYDEVD_CONTAINER_NUMPY_MAX_ITEMS])
        except:
            # This may not work depending on the array shape.
            pass
        return ret


import sys

if not sys.platform.startswith("java"):
    TypeResolveProvider.register(NDArrayTypeResolveProvider)
```
