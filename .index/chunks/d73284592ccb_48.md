# Chunk: d73284592ccb_48

- source: `webui/.next/dev/server/chunks/ssr/[root-of-the-server]__630d161a._.js`
- lines: 1542-1626
- chunk: 49/138

```
 ...fetchOptions } = options;
    const baseUrl = getBackendUrl();
    const apiKey = getApiKey();
    // Build URL with query params
    let url = `${baseUrl}${endpoint}`;
    if (params) {
        const searchParams = new URLSearchParams();
        Object.entries(params).forEach(([key, value])=>{
            if (value !== undefined) {
                searchParams.append(key, String(value));
            }
        });
        const queryString = searchParams.toString();
        if (queryString) {
            url += `?${queryString}`;
        }
    }
    // Build headers
    const headers = {
        'Content-Type': 'application/json',
        ...fetchOptions.headers
    };
    if (apiKey) {
        headers['X-API-Key'] = apiKey;
    }
    const response = await fetch(url, {
        ...fetchOptions,
        headers
    });
    if (!response.ok) {
        const errorData = await response.json().catch(()=>({}));
        throw new Error(errorData.detail || `API Error: ${response.status}`);
    }
    return response.json();
}
// SWR fetcher
const fetcher = (url)=>apiFetch(url);
// Default SWR config
const defaultSwrConfig = {
    revalidateOnFocus: false,
    dedupingInterval: 5000
};
function useSystemHealth() {
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$Documents$2f$GitHub$2f$agentic_assistants$2f$webui$2f$node_modules$2f$swr$2f$dist$2f$index$2f$index$2e$mjs__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__$3c$locals$3e$__["default"])('/api/internal/health', async ()=>{
        const backendUrl = getBackendUrl();
        const checkService = async (url, name)=>{
            try {
                const start = Date.now();
                const response = await fetch(`${url}/health`, {
                    method: 'GET',
                    signal: AbortSignal.timeout(5000)
                });
                const latency = Date.now() - start;
                return {
                    status: response.ok ? 'healthy' : 'unhealthy',
                    url,
                    latency_ms: latency
                };
            } catch  {
                return {
                    status: 'unhealthy',
                    url,
                    message: `${name} not reachable`
                };
            }
        };
        const [backend, ollama, mlflow, jupyter] = await Promise.all([
            checkService(backendUrl, 'Backend'),
            checkService('http://localhost:11434', 'Ollama'),
            checkService(DEFAULT_MLFLOW_URL, 'MLFlow'),
            checkService(DEFAULT_JUPYTER_URL, 'JupyterLab')
        ]);
        return {
            backend: backend,
            ollama: ollama,
            mlflow: mlflow,
            jupyter: jupyter
        };
    }, {
        ...defaultSwrConfig,
        refreshInterval: 30000
    });
}
function useSystemStats() {
```
