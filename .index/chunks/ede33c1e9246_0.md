# Chunk: ede33c1e9246_0

- source: `src/agentic_assistants/data/datasets/url_dataset.py`
- lines: 1-96
- chunk: 1/8

```
"""
URL Dataset for fetching data from remote URLs.

This module provides a dataset implementation for loading data from URLs,
supporting various content types including HTML, JSON, CSV, and binary files.

Example:
    >>> from agentic_assistants.data.datasets import URLDataset
    >>> 
    >>> # Fetch JSON data
    >>> dataset = URLDataset(
    ...     url="https://api.example.com/data.json",
    ...     content_type="json"
    ... )
    >>> data = dataset.load()
    >>> 
    >>> # Fetch with authentication
    >>> dataset = URLDataset(
    ...     url="https://api.example.com/protected",
    ...     headers={"Authorization": "Bearer token"}
    ... )
"""

import hashlib
import time
from dataclasses import dataclass, field
from datetime import datetime
from pathlib import Path
from typing import Any, Dict, List, Optional, Union
from urllib.parse import urlparse

from agentic_assistants.data.datasets.base import (
    AbstractDataset,
    DatasetError,
    DatasetMetadata,
)
from agentic_assistants.utils.logging import get_logger

logger = get_logger(__name__)


@dataclass
class URLFetchResult:
    """Result of a URL fetch operation."""
    
    content: Any
    status_code: int
    headers: Dict[str, str]
    url: str
    fetched_at: datetime = field(default_factory=datetime.utcnow)
    content_type: Optional[str] = None
    encoding: Optional[str] = None
    elapsed_ms: float = 0.0
    
    @property
    def content_hash(self) -> str:
        """Compute hash of content for change detection."""
        if isinstance(self.content, bytes):
            return hashlib.sha256(self.content).hexdigest()
        elif isinstance(self.content, str):
            return hashlib.sha256(self.content.encode()).hexdigest()
        else:
            return hashlib.sha256(str(self.content).encode()).hexdigest()


class URLDataset(AbstractDataset[Any]):
    """
    Dataset for fetching data from remote URLs.
    
    This dataset supports:
    - Multiple content types (HTML, JSON, CSV, XML, binary)
    - HTTP authentication (Basic, Bearer, API Key)
    - Custom headers and request parameters
    - Retry logic with exponential backoff
    - Response caching with ETag/Last-Modified support
    - Content type auto-detection
    
    Attributes:
        url: The URL to fetch data from
        content_type: Expected content type (auto-detected if None)
        method: HTTP method (GET, POST)
        headers: Custom HTTP headers
        params: URL query parameters
        timeout: Request timeout in seconds
        retries: Number of retry attempts
        
    Example:
        >>> # Simple JSON fetch
        >>> dataset = URLDataset(url="https://api.github.com/users/octocat")
        >>> user = dataset.load()
        >>> print(user["login"])
        
        >>> # CSV with custom parsing
        >>> dataset = URLDataset(
        ...     url="https://example.com/data.csv",
```
