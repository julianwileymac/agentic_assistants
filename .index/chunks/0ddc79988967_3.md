# Chunk: 0ddc79988967_3

- source: `src/agentic_assistants/data/rag/augmenters.py`
- lines: 248-343
- chunk: 4/6

```
ion")
        
        prompt = f"""Summarize the following text in {self.max_length} characters or less.
Be concise and capture the main points.

Text:
{text[:3000]}

Summary:"""
        
        summary = self.llm_fn(prompt).strip()
        
        if len(summary) > self.max_length:
            summary = summary[:self.max_length - 3] + "..."
        
        return summary
    
    def augment(self, chunk: Chunk, context: Dict[str, Any]) -> Chunk:
        if len(chunk.content) <= self.max_length:
            chunk.metadata["summary"] = chunk.content
        elif self.use_llm and self.llm_fn:
            chunk.metadata["summary"] = self._llm_summary(chunk.content)
        else:
            chunk.metadata["summary"] = self._extractive_summary(chunk.content)
        
        return chunk


class EntityAugmenter(Augmenter):
    """
    Extract named entities from chunk content.
    
    Can use regex patterns or optional NER model.
    """
    
    name = "entities"
    
    # Common entity patterns
    EMAIL_PATTERN = re.compile(r'\b[\w.+-]+@[\w-]+\.[\w.-]+\b')
    URL_PATTERN = re.compile(r'https?://[^\s<>"{}|\\^`\[\]]+')
    DATE_PATTERN = re.compile(
        r'\b\d{1,2}[-/]\d{1,2}[-/]\d{2,4}\b|\b\d{4}[-/]\d{1,2}[-/]\d{1,2}\b'
    )
    MONEY_PATTERN = re.compile(r'\$[\d,]+\.?\d*|\d+\.?\d*\s*(USD|EUR|GBP)')
    
    def __init__(
        self,
        use_ner: bool = False,
        ner_model: Optional[str] = None,
        extract_patterns: bool = True,
    ):
        """
        Initialize entity augmenter.
        
        Args:
            use_ner: Use NER model for entity extraction
            ner_model: Spacy model name for NER
            extract_patterns: Extract entities using regex patterns
        """
        self.use_ner = use_ner
        self.ner_model = ner_model or "en_core_web_sm"
        self.extract_patterns = extract_patterns
        self._nlp = None
    
    def _get_nlp(self):
        """Get or create spacy model."""
        if self._nlp is not None:
            return self._nlp
        
        try:
            import spacy
            self._nlp = spacy.load(self.ner_model)
            return self._nlp
        except ImportError:
            raise ImportError(
                "spacy is required for NER. "
                "Install with: pip install spacy && python -m spacy download en_core_web_sm"
            )
    
    def _extract_patterns(self, text: str) -> Dict[str, List[str]]:
        """Extract entities using regex patterns."""
        entities = {}
        
        emails = self.EMAIL_PATTERN.findall(text)
        if emails:
            entities["emails"] = list(set(emails))
        
        urls = self.URL_PATTERN.findall(text)
        if urls:
            entities["urls"] = list(set(urls))
        
        dates = self.DATE_PATTERN.findall(text)
        if dates:
            entities["dates"] = list(set(dates))
        
```
