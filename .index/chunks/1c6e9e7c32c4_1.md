# Chunk: 1c6e9e7c32c4_1

- source: `src/agentic_assistants/data/datasets/scraper_dataset.py`
- lines: 98-185
- chunk: 2/9

```
 ...     "title": "h1.title",
        ...     "prices": {"selector": "span.price", "multiple": True},
        ...     "link": {"selector": "a.detail", "attribute": "href"},
        ... })
    """
    
    def __init__(
        self,
        selectors: Dict[str, Union[str, Dict[str, Any]]],
        next_page_selector: Optional[str] = None,
    ):
        """
        Initialize CSS extractor.
        
        Args:
            selectors: Dict mapping field names to CSS selectors or selector configs
            next_page_selector: CSS selector for next page link
        """
        self.selectors = selectors
        self.next_page_selector = next_page_selector
    
    def _extract_value(self, element, config: Dict[str, Any]) -> Any:
        """Extract value from element based on config."""
        if element is None:
            return config.get("default")
        
        attribute = config.get("attribute")
        if attribute:
            return element.get(attribute, config.get("default"))
        else:
            return element.get_text(strip=True)
    
    def extract(self, soup, url: str) -> Dict[str, Any]:
        """Extract data using CSS selectors."""
        result = {}
        
        for name, selector_config in self.selectors.items():
            # Normalize config
            if isinstance(selector_config, str):
                config = {"selector": selector_config}
            else:
                config = selector_config
            
            selector = config.get("selector", "")
            multiple = config.get("multiple", False)
            
            if multiple:
                elements = soup.select(selector)
                result[name] = [self._extract_value(el, config) for el in elements]
            else:
                element = soup.select_one(selector)
                result[name] = self._extract_value(element, config)
        
        return result
    
    def get_next_page(self, soup, current_url: str) -> Optional[str]:
        """Get next page URL from selector."""
        if not self.next_page_selector:
            return None
        
        next_link = soup.select_one(self.next_page_selector)
        if next_link and next_link.get("href"):
            return urljoin(current_url, next_link["href"])
        
        return None


class JSONExtractor(Extractor):
    """
    Extractor for pages with embedded JSON data.
    
    Many modern websites embed structured data as JSON-LD or in script tags.
    
    Example:
        >>> extractor = JSONExtractor(
        ...     script_selector='script[type="application/ld+json"]',
        ...     json_path="$.product"
        ... )
    """
    
    def __init__(
        self,
        script_selector: str = 'script[type="application/ld+json"]',
        json_path: Optional[str] = None,
        variable_name: Optional[str] = None,
    ):
        """
```
