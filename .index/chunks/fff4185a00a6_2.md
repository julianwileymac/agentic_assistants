# Chunk: fff4185a00a6_2

- source: `src/agentic_assistants/data/datasets/pickle_dataset.py`
- lines: 167-184
- chunk: 3/3

```
.load(f)
    
    def _save(self, data: Any) -> None:
        """Save object to compressed pickle file."""
        compressor = self._get_compressor()
        
        # Build open kwargs based on compression type
        open_kwargs = {"mode": "wb"}
        if self._compression == "gzip":
            open_kwargs["compresslevel"] = self._compression_level
        elif self._compression == "bz2":
            open_kwargs["compresslevel"] = self._compression_level
        elif self._compression == "lzma":
            open_kwargs["preset"] = self._compression_level
        
        with compressor.open(self._filepath, **open_kwargs) as f:
            pickle.dump(data, f, **self._save_args)
```
