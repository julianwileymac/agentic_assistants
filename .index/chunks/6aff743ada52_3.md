# Chunk: 6aff743ada52_3

- source: `.venv-lab/Lib/site-packages/jsonschema/cli.py`
- lines: 278-293
- chunk: 4/4

```

    for each in instances:
        try:
            instance = load(each)
        except _CannotLoadFile:
            exit_code = 1
        else:
            exit_code |= _validate_instance(
                instance_path=each,
                instance=instance,
                validator=validator,
                outputter=outputter,
            )

    return exit_code
```
