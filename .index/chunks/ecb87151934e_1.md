# Chunk: ecb87151934e_1

- source: `src/agentic_assistants/data/templates/builtin/api_templates.py`
- lines: 91-186
- chunk: 2/4

```
scription = "Access Hugging Face models, datasets, and spaces"
    category = TemplateCategory.API
    dataset_type = "url"
    documentation_url = "https://huggingface.co/docs/hub/api"
    tags = ["huggingface", "ml", "models", "datasets"]
    
    parameters = [
        TemplateParameter(
            name="endpoint",
            description="API endpoint (models, datasets, spaces)",
            param_type="string",
            required=True,
            choices=["models", "datasets", "spaces"],
            default="models",
        ),
        TemplateParameter(
            name="search",
            description="Search query",
            param_type="string",
            required=False,
        ),
        TemplateParameter(
            name="author",
            description="Filter by author/organization",
            param_type="string",
            required=False,
        ),
        TemplateParameter(
            name="token",
            description="Hugging Face API token",
            param_type="string",
            required=False,
            secret=True,
        ),
    ]
    
    def create_dataset(self, **params) -> URLDataset:
        endpoint = params["endpoint"]
        url = f"https://huggingface.co/api/{endpoint}"
        
        url_params = {}
        if params.get("search"):
            url_params["search"] = params["search"]
        if params.get("author"):
            url_params["author"] = params["author"]
        
        auth = None
        if params.get("token"):
            auth = {"type": "bearer", "token": params["token"]}
        
        return URLDataset(
            url=url,
            content_type="json",
            params=url_params,
            auth=auth,
        )


class OpenAIAPITemplate(DataSourceTemplate):
    """Template for OpenAI API endpoints."""
    
    id = "openai-api"
    name = "OpenAI API"
    description = "Access OpenAI models list and other read-only endpoints"
    category = TemplateCategory.API
    dataset_type = "url"
    documentation_url = "https://platform.openai.com/docs/api-reference"
    tags = ["openai", "llm", "ai", "api"]
    
    parameters = [
        TemplateParameter(
            name="endpoint",
            description="API endpoint",
            param_type="string",
            required=True,
            choices=["models", "models/{model_id}"],
            default="models",
        ),
        TemplateParameter(
            name="model_id",
            description="Model ID (for single model endpoint)",
            param_type="string",
            required=False,
        ),
        TemplateParameter(
            name="api_key",
            description="OpenAI API key",
            param_type="string",
            required=True,
            secret=True,
        ),
    ]
    
    def create_dataset(self, **params) -> URLDataset:
        endpoint = params["endpoint"]
```
