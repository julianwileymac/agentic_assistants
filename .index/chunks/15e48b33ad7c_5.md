# Chunk: 15e48b33ad7c_5

- source: `src/agentic_assistants/data/shared_storage.py`
- lines: 383-481
- chunk: 6/11

```
 f"{artifact_id}_{metadata.name}"
            if group_link.exists():
                group_link.unlink()
        
        # Remove artifact directory
        artifact_dir = Path(metadata.shared_path).parent
        if artifact_dir.exists():
            shutil.rmtree(artifact_dir)
        
        # Remove from index
        del self._index[artifact_id]
        self._save_index()
        
        logger.info(f"Deleted shared artifact: {metadata.name}")
        
        return True
    
    def get_artifact_path(self, artifact_id: str) -> Optional[Path]:
        """Get the file path for a shared artifact."""
        metadata = self._index.get(artifact_id)
        if metadata:
            return Path(metadata.shared_path)
        return None
    
    def copy_to_session(
        self,
        artifact_id: str,
        session_id: str,
        user_id: str = "default_user",
    ) -> Optional[Path]:
        """
        Copy a shared artifact to a session's artifact directory.
        
        Args:
            artifact_id: ID of the shared artifact
            session_id: Target session ID
            user_id: User ID
        
        Returns:
            Path to the copied artifact, or None if failed
        """
        metadata = self._index.get(artifact_id)
        if not metadata:
            return None
        
        source = Path(metadata.shared_path)
        if not source.exists():
            return None
        
        # Determine destination path
        session_dir = Path(self.config.data_dir) / "sessions" / session_id / "artifacts"
        session_dir.mkdir(parents=True, exist_ok=True)
        dest = session_dir / metadata.name
        
        # Copy file
        shutil.copy2(source, dest)
        
        logger.info(f"Copied artifact {metadata.name} to session {session_id}")
        
        return dest
    
    def get_stats(self) -> dict:
        """Get storage statistics."""
        total_size = sum(m.size_bytes for m in self._index.values())
        all_tags = set()
        all_groups = set()
        
        for metadata in self._index.values():
            all_tags.update(metadata.tags)
            all_groups.update(metadata.groups)
        
        return {
            "total_artifacts": len(self._index),
            "total_size_bytes": total_size,
            "total_tags": len(all_tags),
            "total_groups": len(all_groups),
            "tags": list(all_tags),
            "groups": list(all_groups),
        }


class SessionStorageManager:
    """
    Manages session-specific storage with persistence.
    
    Provides:
    - Session artifact storage
    - Interaction logging
    - Context persistence
    """
    
    def __init__(
        self,
        session_id: str,
        user_id: str = "default_user",
        config: Optional[AgenticConfig] = None,
    ):
        self.session_id = session_id
```
