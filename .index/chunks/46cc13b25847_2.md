# Chunk: 46cc13b25847_2

- source: `notebooks/10_multiagent_evaluation.ipynb`
- lines: 139-167
- chunk: 3/3

```
       "runs = mlflow.search_runs(\n",
        "    experiment_names=[\"agent-evaluation\"],\n",
        "    max_results=50,\n",
        ")\n",
        "\n",
        "if not runs.empty:\n",
        "    print(\"Evaluation Results Summary:\")\n",
        "    print(\"-\" * 50)\n",
        "    \n",
        "    summary = runs.groupby(\"params.adapter\").agg({\n",
        "        \"metrics.score\": [\"mean\", \"std\"],\n",
        "        \"metrics.duration_seconds\": \"mean\",\n",
        "    }).round(3)\n",
        "    \n",
        "    display(summary)\n",
        "else:\n",
        "    print(\"No evaluation runs found. Run the evaluation cells above first.\")\n"
      ]
    }
  ],
  "metadata": {
    "language_info": {
      "name": "python"
    }
  },
  "nbformat": 4,
  "nbformat_minor": 2
}
```
