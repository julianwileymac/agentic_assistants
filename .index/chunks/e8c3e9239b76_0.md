# Chunk: e8c3e9239b76_0

- source: `src/agentic_assistants/data/rag/ingestion_pipeline.py`
- lines: 1-101
- chunk: 1/8

```
"""
RAG Ingestion Pipeline for processing and storing data.

This pipeline handles the complete flow from data fetching through
chunking, augmentation, annotation, and storage.
"""

import hashlib
from dataclasses import dataclass, field
from datetime import datetime
from pathlib import Path
from typing import Any, Callable, Dict, List, Optional, Union

from agentic_assistants.data.rag.chunkers import (
    Chunk,
    Chunker,
    TextChunker,
    get_chunker,
)
from agentic_assistants.data.rag.augmenters import (
    Augmenter,
    ChainedAugmenter,
    MetadataAugmenter,
    create_default_augmenter,
)
from agentic_assistants.data.rag.annotators import (
    Annotator,
    ChainedAnnotator,
    create_default_annotator,
)
from agentic_assistants.utils.logging import get_logger

logger = get_logger(__name__)


@dataclass
class IngestionConfig:
    """Configuration for the ingestion pipeline."""
    
    # Chunking settings
    chunk_size: int = 1024
    chunk_overlap: int = 128
    chunking_strategy: str = "text"  # text, sentence, markdown, code, semantic
    
    # Processing settings
    augmenters: List[str] = field(default_factory=lambda: ["metadata", "keywords"])
    annotators: List[str] = field(default_factory=lambda: ["language", "quality"])
    
    # Storage settings
    collection: str = "default"
    store_as_files: bool = True
    store_in_vectordb: bool = True
    
    # File storage settings
    artifacts_dir: str = "./data/artifacts"
    
    # LLM settings (for augmenters/annotators that need it)
    llm_enabled: bool = False
    llm_model: Optional[str] = None
    
    # Filtering
    min_quality_score: float = 0.3
    deduplicate: bool = True
    
    def to_dict(self) -> Dict[str, Any]:
        return {
            "chunk_size": self.chunk_size,
            "chunk_overlap": self.chunk_overlap,
            "chunking_strategy": self.chunking_strategy,
            "augmenters": self.augmenters,
            "annotators": self.annotators,
            "collection": self.collection,
            "store_as_files": self.store_as_files,
            "store_in_vectordb": self.store_in_vectordb,
            "artifacts_dir": self.artifacts_dir,
            "llm_enabled": self.llm_enabled,
            "min_quality_score": self.min_quality_score,
            "deduplicate": self.deduplicate,
        }


@dataclass
class IngestionResult:
    """Result of an ingestion operation."""
    
    success: bool
    source: str
    source_type: str
    chunks_created: int
    chunks_stored: int
    chunks_filtered: int
    file_path: Optional[str] = None
    collection: Optional[str] = None
    duration_seconds: float = 0.0
    errors: List[str] = field(default_factory=list)
    metadata: Dict[str, Any] = field(default_factory=dict)
    
    def to_dict(self) -> Dict[str, Any]:
        return {
            "success": self.success,
```
