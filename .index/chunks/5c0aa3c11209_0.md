# Chunk: 5c0aa3c11209_0

- source: `.venv-lab/share/jupyter/nbconvert/templates/base/celltags.j2`
- lines: 1-8
- chunk: 1/1

```
{%- macro celltags(cell) -%}
    {% if cell.metadata.tags | length > 0 -%}
        {% for tag in (cell.metadata.tags) -%}
            {{ (' celltag_' ~ tag) | escape_html -}}
        {%- endfor -%}
    {%- endif %}
{%- endmacro %}
```
