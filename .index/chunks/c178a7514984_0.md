# Chunk: c178a7514984_0

- source: `notebooks/09_llamaindex_rag.ipynb`
- lines: 1-85
- chunk: 1/2

```
{
  "cells": [
    {
      "cell_type": "markdown",
      "metadata": {},
      "source": [
        "# LlamaIndex RAG Pipeline\n",
        "\n",
        "Build retrieval-augmented generation pipelines with LlamaIndex and track experiments with MLFlow.\n",
        "\n",
        "## Contents\n",
        "1. **Setup** - Configure LlamaIndex with Ollama\n",
        "2. **Document Loading** - Load and index documents\n",
        "3. **Query Engine** - Create and use query engines\n",
        "4. **Tracking** - Log experiments to MLFlow\n"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "outputs": [],
      "source": [
        "# Setup\n",
        "from agentic_assistants import AgenticConfig\n",
        "from agentic_assistants.core import MLFlowTracker\n",
        "\n",
        "config = AgenticConfig()\n",
        "tracker = MLFlowTracker(config)\n",
        "\n",
        "print(f\"Vector DB backend: {config.vectordb.backend}\")\n",
        "print(f\"Embedding model: {config.vectordb.embedding_model}\")\n",
        "print(f\"MLFlow: {config.mlflow.tracking_uri}\")\n"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "outputs": [],
      "source": [
        "# Configure LlamaIndex with Ollama embeddings\n",
        "from llama_index.core import VectorStoreIndex, Document, Settings\n",
        "from llama_index.embeddings.ollama import OllamaEmbedding\n",
        "\n",
        "Settings.embed_model = OllamaEmbedding(\n",
        "    model_name=config.vectordb.embedding_model,\n",
        "    base_url=config.ollama.host,\n",
        ")\n",
        "\n",
        "print(\"Embeddings configured!\")\n"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "outputs": [],
      "source": [
        "# Create sample documents and build index\n",
        "documents = [\n",
        "    Document(text=\"Agentic AI systems can autonomously plan and execute tasks.\"),\n",
        "    Document(text=\"RAG combines retrieval with generation for better accuracy.\"),\n",
        "    Document(text=\"MLFlow helps track experiments and model performance.\"),\n",
        "    Document(text=\"OpenTelemetry provides distributed tracing for observability.\"),\n",
        "]\n",
        "\n",
        "index = VectorStoreIndex.from_documents(documents)\n",
        "print(f\"Indexed {len(documents)} documents\")\n"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "outputs": [],
      "source": [
        "# Query with tracking\n",
        "query_engine = index.as_query_engine()\n",
        "\n",
        "with tracker.start_run(run_name=\"rag-query\"):\n",
        "    tracker.log_param(\"index_type\", \"VectorStoreIndex\")\n",
        "    tracker.log_param(\"num_documents\", len(documents))\n",
        "    \n",
```
