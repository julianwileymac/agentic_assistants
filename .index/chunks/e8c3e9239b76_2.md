# Chunk: e8c3e9239b76_2

- source: `src/agentic_assistants/data/rag/ingestion_pipeline.py`
- lines: 169-243
- chunk: 3/8

```
rs:
            self._augmenters = ChainedAugmenter(augmenters)
        elif self.config.augmenters:
            self._augmenters = self._build_augmenters(self.config.augmenters)
        else:
            self._augmenters = create_default_augmenter()
        
        # Initialize annotators
        if annotators:
            self._annotators = ChainedAnnotator(annotators)
        elif self.config.annotators:
            self._annotators = self._build_annotators(self.config.annotators)
        else:
            self._annotators = create_default_annotator()
        
        # Vector store (lazy loaded)
        self._vector_store = vector_store
        
        # Tracking
        self._seen_hashes: set = set()
    
    def _build_augmenters(self, augmenter_names: List[str]) -> ChainedAugmenter:
        """Build augmenters from names."""
        from agentic_assistants.data.rag.augmenters import get_augmenter
        
        augmenters = []
        for name in augmenter_names:
            try:
                kwargs = {}
                if name in ("summary", "keywords", "qa") and self._llm_fn:
                    kwargs["llm_fn"] = self._llm_fn
                    kwargs["use_llm"] = self.config.llm_enabled
                augmenters.append(get_augmenter(name, **kwargs))
            except Exception as e:
                logger.warning(f"Failed to create augmenter '{name}': {e}")
        
        return ChainedAugmenter(augmenters)
    
    def _build_annotators(self, annotator_names: List[str]) -> ChainedAnnotator:
        """Build annotators from names."""
        from agentic_assistants.data.rag.annotators import get_annotator
        
        annotators = []
        for name in annotator_names:
            try:
                kwargs = {}
                if name in ("classification", "topics", "sentiment") and self._llm_fn:
                    kwargs["llm_fn"] = self._llm_fn
                    kwargs["use_llm"] = self.config.llm_enabled
                annotators.append(get_annotator(name, **kwargs))
            except Exception as e:
                logger.warning(f"Failed to create annotator '{name}': {e}")
        
        return ChainedAnnotator(annotators)
    
    @property
    def vector_store(self):
        """Get or create vector store."""
        if self._vector_store is None:
            try:
                from agentic_assistants.vectordb import LanceDBStore
                from agentic_assistants.config import AgenticConfig
                
                config = AgenticConfig()
                self._vector_store = LanceDBStore(
                    db_path=str(config.vectordb.db_path),
                    embedding_model=config.vectordb.embedding_model,
                )
            except Exception as e:
                logger.warning(f"Failed to initialize vector store: {e}")
        
        return self._vector_store
    
    def _fetch_content(
```
