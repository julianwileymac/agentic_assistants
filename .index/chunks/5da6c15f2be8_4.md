# Chunk: 5da6c15f2be8_4

- source: `.venv-lab/Lib/site-packages/tornado/wsgi.py`
- lines: 256-269
- chunk: 5/5

```
st.uri is not None
        summary = (
            request.method  # type: ignore[operator]
            + " "
            + request.uri
            + " ("
            + request.remote_ip
            + ")"
        )
        log_method("%d %s %.2fms", status_code, summary, request_time)


HTTPRequest = httputil.HTTPServerRequest
```
