# Chunk: bcf3586b7399_4

- source: `.venv-lab/Lib/site-packages/colorama/ansitowin32.py`
- lines: 265-278
- chunk: 5/5

```
    if command == BEL:
                if paramstring.count(";") == 1:
                    params = paramstring.split(";")
                    # 0 - change title and icon (we will only change title)
                    # 1 - change icon (we don't support this)
                    # 2 - change title
                    if params[0] in '02':
                        winterm.set_title(params[1])
        return text


    def flush(self):
        self.wrapped.flush()
```
