# Chunk: f2b6e772c023_2

- source: `src/agentic_assistants/data/catalog.py`
- lines: 158-237
- chunk: 3/20

```
efault_factory=dict)
    save_args: Dict[str, Any] = field(default_factory=dict)
    credentials: Optional[str] = None  # Reference to credentials key
    versioned: bool = False
    layer: Optional[str] = None
    metadata: Dict[str, Any] = field(default_factory=dict)
    
    @classmethod
    def from_dict(cls, name: str, config: Dict[str, Any]) -> "DatasetConfig":
        """Create DatasetConfig from dictionary."""
        return cls(
            name=name,
            type=config.get("type", ""),
            filepath=config.get("filepath"),
            load_args=config.get("load_args", {}),
            save_args=config.get("save_args", {}),
            credentials=config.get("credentials"),
            versioned=config.get("versioned", False),
            layer=config.get("layer"),
            metadata=config.get("metadata", {}),
        )
    
    def to_dict(self) -> Dict[str, Any]:
        """Convert to dictionary."""
        return {
            "type": self.type,
            "filepath": self.filepath,
            "load_args": self.load_args,
            "save_args": self.save_args,
            "credentials": self.credentials,
            "versioned": self.versioned,
            "layer": self.layer,
            "metadata": self.metadata,
        }


@dataclass
class DatasetPattern:
    """
    Pattern-based dataset factory configuration.
    
    Allows defining datasets using patterns like '{layer}_{name}'
    that can match multiple dataset names.
    """
    pattern: str
    type: str
    filepath_template: str  # e.g., "data/{layer}/{name}.parquet"
    load_args: Dict[str, Any] = field(default_factory=dict)
    save_args: Dict[str, Any] = field(default_factory=dict)
    versioned: bool = False
    
    def matches(self, name: str) -> bool:
        """Check if the pattern matches a dataset name."""
        # Convert pattern to regex
        regex_pattern = self.pattern.replace("{", "(?P<").replace("}", ">[^_]+)")
        try:
            return bool(re.fullmatch(regex_pattern, name))
        except re.error:
            # Fall back to glob-style matching
            return fnmatch.fnmatch(name, self.pattern.replace("{", "*").replace("}", ""))
    
    def extract_params(self, name: str) -> Dict[str, str]:
        """Extract parameter values from a matched name."""
        regex_pattern = self.pattern.replace("{", "(?P<").replace("}", ">[^_]+)")
        try:
            match = re.fullmatch(regex_pattern, name)
            if match:
                return match.groupdict()
        except re.error:
            pass
        return {}
    
    def create_config(self, name: str) -> DatasetConfig:
        """Create a DatasetConfig for a matched name."""
        params = self.extract_params(name)
        
        # Substitute parameters in filepath
        filepath = self.filepath_template
        for key, value in params.items():
```
