# Chunk: 8f01be7c5221_32

- source: `webui/.next/server/chunks/ssr/Documents_GitHub_agentic_assistants_webui_9122ee9b._.js.map`
- lines: 1-1
- chunk: 33/43

```
==================================================================\r\n\r\nexport function useTags() {\r\n  return useSWR<Tag[]>('/api/v1/tags', fetcher, defaultSwrConfig);\r\n}\r\n\r\nexport function useAddTag(resourceType: string, resourceId: string) {\r\n  return useSWRMutation(\r\n    `/api/v1/resources/${resourceType}/${resourceId}/tags`,\r\n    async (url: string, { arg }: { arg: { tag: string } }) => {\r\n      return apiFetch<{ status: string }>(url, {\r\n        method: 'POST',\r\n        body: JSON.stringify(arg),\r\n      });\r\n    }\r\n  );\r\n}\r\n\r\n// ============================================================================\r\n// MLFlow Experiments API\r\n// ============================================================================\r\n\r\nexport function useExperiments(viewType: string = 'ACTIVE_ONLY') {\r\n  return useSWR<{ experiments: Experiment[]; total: number }>(\r\n    `/api/v1/experiments?view_type=${viewType}`,\r\n    fetcher,\r\n    defaultSwrConfig\r\n  );\r\n}\r\n\r\nexport function useExperiment(id: string | null) {\r\n  return useSWR<Experiment>(\r\n    id ? `/api/v1/experiments/${id}` : null,\r\n    fetcher,\r\n    defaultSwrConfig\r\n  );\r\n}\r\n\r\nexport function useExperimentRuns(experimentId: string | null) {\r\n  return useSWR<{ runs: Run[]; total: number }>(\r\n    experimentId ? `/api/v1/experiments/${experimentId}/runs` : null,\r\n    fetcher,\r\n    defaultSwrConfig\r\n  );\r\n}\r\n\r\nexport function useRun(runId: string | null) {\r\n  return useSWR<Run>(\r\n    runId ? `/api/v1/experiments/runs/${runId}` : null,\r\n    fetcher,\r\n    defaultSwrConfig\r\n  );\r\n}\r\n\r\nexport function useCreateExperiment() {\r\n  return useSWRMutation(\r\n    '/api/v1/experiments',\r\n    async (url: string, { arg }: { arg: { name: string; description?: string; tags?: Record<string, string> } }) => {\r\n      return apiFetch<Experiment>(url, {\r\n        method: 'POST',\r\n        body: JSON.stringify(arg),\r\n      });\r\n    }\r\n  );\r\n}\r\n\r\n// ============================================================================\r\n// Sessions API\r\n// ============================================================================\r\n\r\nexport function useSessions() {\r\n  return useSWR<Session[]>('/sessions', fetcher, defaultSwrConfig);\r\n}\r\n\r\nexport function useSession(name: string | null) {\r\n  return useSWR<Session>(\r\n    name ? `/sessions/${name}` : null,\r\n    fetcher,\r\n    defaultSwrConfig\r\n  );\r\n}\r\n\r\n// ============================================================================\r\n// Collections & Search API\r\n// ============================================================================\r\n\r\nexport function useCollections() {\r\n  return useSWR<{ collections: Collection[] }>('/collections', fetcher, defaultSwrConfig);\r\n}\r\n\r\nexport function useSearch(query: string, collection: string = 'default', topK: number = 5) {\r\n  return useSWRMutation(\r\n    '/search',\r\n    async (url
```
