# Chunk: e8c3e9239b76_4

- source: `src/agentic_assistants/data/rag/ingestion_pipeline.py`
- lines: 303-398
- chunk: 5/8

```
           
            filtered.append(chunk)
        
        return filtered
    
    def _save_to_files(
        self,
        chunks: List[Chunk],
        source: str,
        collection: str,
    ) -> str:
        """Save chunks to file artifacts."""
        import json
        
        artifacts_dir = Path(self.config.artifacts_dir) / collection
        artifacts_dir.mkdir(parents=True, exist_ok=True)
        
        # Create filename from source
        source_hash = hashlib.sha256(source.encode()).hexdigest()[:12]
        timestamp = datetime.utcnow().strftime("%Y%m%d_%H%M%S")
        filename = f"{timestamp}_{source_hash}.json"
        
        filepath = artifacts_dir / filename
        
        # Save as JSON
        data = {
            "source": source,
            "ingested_at": datetime.utcnow().isoformat(),
            "collection": collection,
            "chunk_count": len(chunks),
            "chunks": [chunk.to_dict() for chunk in chunks],
        }
        
        with open(filepath, "w") as f:
            json.dump(data, f, indent=2)
        
        logger.info(f"Saved {len(chunks)} chunks to {filepath}")
        return str(filepath)
    
    def _store_in_vectordb(
        self,
        chunks: List[Chunk],
        collection: str,
    ) -> int:
        """Store chunks in vector database."""
        if not self.vector_store:
            logger.warning("Vector store not available")
            return 0
        
        try:
            # Prepare documents for vector store
            documents = []
            metadatas = []
            
            for chunk in chunks:
                documents.append(chunk.content)
                metadatas.append(chunk.metadata)
            
            # Add to collection
            self.vector_store.add(
                collection=collection,
                documents=documents,
                metadatas=metadatas,
            )
            
            logger.info(f"Stored {len(chunks)} chunks in collection '{collection}'")
            return len(chunks)
            
        except Exception as e:
            logger.error(f"Failed to store in vector DB: {e}")
            return 0
    
    def ingest(
        self,
        source: str,
        source_type: str = "url",
        collection: Optional[str] = None,
        metadata: Optional[Dict[str, Any]] = None,
        **kwargs,
    ) -> IngestionResult:
        """
        Ingest content from a source.
        
        Args:
            source: Source URL, path, or text
            source_type: Type of source (url, website, rss, file, text)
            collection: Vector store collection name
            metadata: Additional metadata to attach to chunks
            **kwargs: Additional arguments for the data source
            
        Returns:
            IngestionResult with statistics
        """
        import time
        
```
