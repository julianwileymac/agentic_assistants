# Chunk: bc5f93f507e6_50

- source: `webui/.next/dev/static/chunks/Documents_GitHub_agentic_assistants_webui_src_09956f45._.js`
- lines: 1720-1803
- chunk: 51/153

```

function setApiKey(key) {
    if ("TURBOPACK compile-time truthy", 1) {
        localStorage.setItem('api_key', key);
    }
}
function clearApiKey() {
    if ("TURBOPACK compile-time truthy", 1) {
        localStorage.removeItem('api_key');
    }
}
async function apiFetch(endpoint, options = {}) {
    const { params, ...fetchOptions } = options;
    const baseUrl = getBackendUrl();
    const apiKey = getApiKey();
    // Build URL with query params
    let url = `${baseUrl}${endpoint}`;
    if (params) {
        const searchParams = new URLSearchParams();
        Object.entries(params).forEach(([key, value])=>{
            if (value !== undefined) {
                searchParams.append(key, String(value));
            }
        });
        const queryString = searchParams.toString();
        if (queryString) {
            url += `?${queryString}`;
        }
    }
    // Build headers
    const headers = {
        'Content-Type': 'application/json',
        ...fetchOptions.headers
    };
    if (apiKey) {
        headers['X-API-Key'] = apiKey;
    }
    const response = await fetch(url, {
        ...fetchOptions,
        headers
    });
    if (!response.ok) {
        const errorData = await response.json().catch(()=>({}));
        throw new Error(errorData.detail || `API Error: ${response.status}`);
    }
    return response.json();
}
// SWR fetcher
const fetcher = (url)=>apiFetch(url);
// Default SWR config
const defaultSwrConfig = {
    revalidateOnFocus: false,
    dedupingInterval: 5000
};
function useSystemHealth() {
    _s();
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$Documents$2f$GitHub$2f$agentic_assistants$2f$webui$2f$node_modules$2f$swr$2f$dist$2f$index$2f$index$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["default"])('/api/internal/health', {
        "useSystemHealth.useSWR": async ()=>{
            const backendUrl = getBackendUrl();
            const checkService = {
                "useSystemHealth.useSWR.checkService": async (url, name)=>{
                    try {
                        const start = Date.now();
                        const response = await fetch(`${url}/health`, {
                            method: 'GET',
                            signal: AbortSignal.timeout(5000)
                        });
                        const latency = Date.now() - start;
                        return {
                            status: response.ok ? 'healthy' : 'unhealthy',
                            url,
                            latency_ms: latency
                        };
                    } catch  {
                        return {
                            status: 'unhealthy',
                            url,
                            message: `${name} not reachable`
                        };
                    }
                }
            }["useSystemHealth.useSWR.checkService"];
            const [backend, ollama, mlflow, jupyter] = await Promise.all([
```
