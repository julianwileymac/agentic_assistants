# Chunk: f2b6e772c023_19

- source: `src/agentic_assistants/data/catalog.py`
- lines: 1493-1548
- chunk: 20/20

```
    if entry_type == "table":
                    entry = TableEntry(
                        id=entry_data["id"],
                        name=entry_data["name"],
                        source_id=entry_data["source_id"],
                        description=entry_data.get("description", ""),
                        tags=entry_data.get("tags", []),
                        metadata=entry_data.get("metadata", {}),
                        schema_name=entry_data.get("schema_name"),
                        row_count=entry_data.get("row_count"),
                        size_bytes=entry_data.get("size_bytes"),
                    )
                elif entry_type == "column":
                    entry = ColumnEntry(
                        id=entry_data["id"],
                        name=entry_data["name"],
                        source_id=entry_data["source_id"],
                        description=entry_data.get("description", ""),
                        tags=entry_data.get("tags", []),
                        metadata=entry_data.get("metadata", {}),
                        table_id=entry_data.get("table_id", ""),
                        data_type=entry_data.get("data_type", ""),
                        nullable=entry_data.get("nullable", True),
                        primary_key=entry_data.get("primary_key", False),
                    )
                else:
                    continue
                
                self._entries[entry.id] = entry
                imported["entries"] += 1
            except Exception as e:
                imported["errors"] += 1
                logger.error(f"Failed to import entry {entry_id}: {e}")
        
        # Import lineage
        for lin_id, lin_data in data.get("lineage", {}).items():
            try:
                lineage = DataLineage(
                    id=lin_data["id"],
                    source_entry_id=lin_data["source_entry_id"],
                    target_entry_id=lin_data["target_entry_id"],
                    relationship_type=lin_data["relationship_type"],
                    transformation=lin_data.get("transformation"),
                    metadata=lin_data.get("metadata", {}),
                )
                self._lineage[lineage.id] = lineage
                imported["lineage"] += 1
            except Exception as e:
                imported["errors"] += 1
                logger.error(f"Failed to import lineage {lin_id}: {e}")
        
        return imported



```
