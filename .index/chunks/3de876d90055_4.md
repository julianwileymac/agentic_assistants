# Chunk: 3de876d90055_4

- source: `webui/src/app/kubernetes/models/page.tsx`
- lines: 331-407
- chunk: 5/7

```
             </DropdownMenuContent>
                      </DropdownMenu>
                    </TableCell>
                  </TableRow>
                ))}
              </TableBody>
            </Table>
          ) : (
            <div className="text-center py-12 text-muted-foreground">
              <Box className="size-12 mx-auto mb-4 opacity-50" />
              <p>No models deployed</p>
              <p className="text-sm">Deploy a model to enable LLM inference on your cluster</p>
            </div>
          )}
        </CardContent>
      </Card>

      {/* Deploy Dialog */}
      <Dialog open={deployDialog} onOpenChange={setDeployDialog}>
        <DialogContent className="max-w-lg">
          <DialogHeader>
            <DialogTitle>Deploy Model</DialogTitle>
            <DialogDescription>
              Deploy an LLM model to your Kubernetes cluster
            </DialogDescription>
          </DialogHeader>
          <div className="space-y-4 py-4">
            <div className="grid grid-cols-2 gap-4">
              <div>
                <Label htmlFor="name">Deployment Name</Label>
                <Input
                  id="name"
                  placeholder="llama-server"
                  value={deployForm.name}
                  onChange={(e) => setDeployForm({ ...deployForm, name: e.target.value })}
                />
              </div>
              <div>
                <Label htmlFor="model">Model Name</Label>
                <Input
                  id="model"
                  placeholder="llama3.2"
                  value={deployForm.model_name}
                  onChange={(e) => setDeployForm({ ...deployForm, model_name: e.target.value })}
                />
              </div>
            </div>
            <div>
              <Label>Serving Type</Label>
              <Select
                value={deployForm.serving_type}
                onValueChange={(v) => setDeployForm({ ...deployForm, serving_type: v })}
              >
                <SelectTrigger>
                  <SelectValue />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="ollama">Ollama</SelectItem>
                  <SelectItem value="vllm">vLLM</SelectItem>
                  <SelectItem value="tgi">TGI (HuggingFace)</SelectItem>
                </SelectContent>
              </Select>
            </div>
            <div className="grid grid-cols-2 gap-4">
              <div>
                <Label htmlFor="replicas">Replicas</Label>
                <Input
                  id="replicas"
                  type="number"
                  min={1}
                  max={5}
                  value={deployForm.replicas}
                  onChange={(e) => setDeployForm({ ...deployForm, replicas: parseInt(e.target.value) || 1 })}
                />
              </div>
              <div>
```
