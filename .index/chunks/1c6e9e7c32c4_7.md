# Chunk: 1c6e9e7c32c4_7

- source: `src/agentic_assistants/data/datasets/scraper_dataset.py`
- lines: 558-640
- chunk: 8/9

```
e URL (handles pagination internally)
            url_results = self._scrape_url(url)
            self._results.extend(url_results)
            
            # Rate limiting between different URLs
            if i < len(self.urls) - 1 and delay > 0:
                time.sleep(delay)
        
        # Update metadata
        successful = [r for r in self._results if r.success]
        self._metadata.custom["urls_scraped"] = len(self.urls)
        self._metadata.custom["pages_scraped"] = len(self._results)
        self._metadata.custom["successful"] = len(successful)
        self._metadata.custom["failed"] = len(self._results) - len(successful)
        
        logger.info(
            f"Scraping complete: {len(successful)}/{len(self._results)} pages successful"
        )
        
        # Return data from successful scrapes
        return [
            {
                "url": r.url,
                "scraped_at": r.scraped_at.isoformat(),
                **r.data
            }
            for r in self._results if r.success
        ]
    
    def _save(self, data: Any) -> None:
        """Scraper datasets are read-only."""
        raise DatasetError("ScraperDataset does not support saving.")
    
    def exists(self) -> bool:
        """Check if any URL is accessible."""
        try:
            import httpx
            with httpx.Client(timeout=10) as client:
                response = client.head(self.urls[0])
                return response.status_code < 400
        except Exception:
            return False
    
    def get_results(self) -> List[ScrapeResult]:
        """Get all scrape results from last load."""
        return self._results.copy()
    
    def get_failed(self) -> List[ScrapeResult]:
        """Get failed scrape results."""
        return [r for r in self._results if not r.success]
    
    def load_as_dataframe(self):
        """Load data as a pandas DataFrame."""
        try:
            import pandas as pd
        except ImportError:
            raise DatasetError("pandas is required for DataFrame output")
        
        data = self._load()
        return pd.DataFrame(data)
    
    def _describe(self) -> Dict[str, Any]:
        """Describe the dataset configuration."""
        return {
            "urls": self.urls[:5],  # First 5 URLs
            "url_count": len(self.urls),
            "extractor": self.extractor.__class__.__name__,
            "render_js": self.render_js,
            "max_pages": self.max_pages,
        }
    
    def __repr__(self) -> str:
        return f"ScraperDataset(urls={len(self.urls)}, extractor={self.extractor.__class__.__name__})"


# Convenience functions for creating extractors
def css_extractor(selectors: Dict[str, Any], **kwargs) -> CSSExtractor:
    """Create a CSS extractor."""
    return CSSExtractor(selectors, **kwargs)


def json_extractor(**kwargs) -> JSONExtractor:
```
