# Local RAG Studio

This starter is a local playground for document ingestion, retrieval, and answer generation.

Use when:
- you want a single-project RAG baseline
- you are tuning chunking and top-k settings

Primary components:
- `IngestionPipeline`
- `RAGPattern`
- local LanceDB/Chroma backend

