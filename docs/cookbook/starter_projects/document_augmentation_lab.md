# Document Augmentation Lab

Use this starter to compare metadata-only augmentation against richer pipelines.

Recommended flow:
1. baseline indexing
2. metadata + keywords
3. summary + entities
4. retrieval quality comparison

