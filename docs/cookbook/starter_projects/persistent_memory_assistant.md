# Persistent Memory Assistant

This starter combines semantic retrieval with mem0 conversation memory.

Use when:
- interactions span multiple sessions
- user preferences should persist across tasks

