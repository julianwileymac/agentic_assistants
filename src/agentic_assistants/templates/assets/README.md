# Template Assets

This directory contains concrete file-based assets used by the template catalog.

If a template entry has no matching asset folder, the template loader generates
a default scaffold at runtime so `agentic init` still works.

