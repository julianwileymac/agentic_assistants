# {{project_name}}

Multi-level storage pattern starter:

- file artifacts (`./data/artifacts`)
- scoped vector storage (`ScopedVectorStore`)
- SQLite usage analytics (`UsageTracker`)

## Run

```bash
python app.py
```

